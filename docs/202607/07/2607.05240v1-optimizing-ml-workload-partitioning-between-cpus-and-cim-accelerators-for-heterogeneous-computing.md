---
title: Optimizing ML Workload Partitioning between CPUs and CIM Accelerators for Heterogeneous Computing
title_zh: 面向异构计算优化CPU与CIM加速器间的机器学习工作负载划分
authors: "Joel Klein, Rebecca Pelke, Roberto Laudani, Jan Moritz Joseph, Rainer Leupers"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05240v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 基于整数线性规划的CPU与CIM加速器异构推理工作负载划分
tldr: 现有ML工作负载划分方法对RRAM约束和CPU互补能力考虑不足。本文提出基于ILP的异构CPU-CIM划分框架，最小化端到端推理延迟，同时考虑内存、写入延迟和耐久性限制。实验表明，异构执行相比CPU-only实现加速高达30.9倍。该框架为未来CIM加速器设计提供了重要指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05240v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 924, \"height\": 384, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05240v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 920, \"height\": 396, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05240v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 889, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05240v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 381, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05240v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 895, \"height\": 383, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05240v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 930, \"height\": 261, \"label\": \"Table\"}]"
motivation: 现有方法忽略了RRAM的有限内存、高写入延迟和有限寿命，且未充分利用CPU作为补充计算资源。
method: 提出基于整数线性规划（ILP）的工作负载划分框架，结合经验分析和解析模型，优化推理延迟。
result: 异构CPU-CIM执行在边缘CPU上最高加速30.9倍，在高性能CPU上加速7.3倍。
conclusion: 该框架有效提升了异构系统性能，并为CIM加速器设计空间探索提供了洞见。
---

## 摘要
存内计算（CIM）加速器在内存中执行矩阵向量乘法（MVM），使其成为机器学习（ML）工作负载的极具吸引力的解决方案。然而，现有针对CIM加速器的ML工作负载划分方法未能充分考虑阻变随机存取存储器（RRAM）的约束，例如有限的内存、高写入延迟和有限的耐久性。它们还忽略了并行性、底层架构效应或将中央处理单元（CPU）作为互补计算资源。为解决这些局限，我们提出一种基于整数线性规划（ILP）的异构CPU-CIM系统工作负载划分框架。该框架在RRAM约束下最小化端到端推理延迟，捕捉并行性，并将经验性性能分析与分析模型相结合。使用我们的框架，异构CPU-CIM执行相比边缘CPU上的纯CPU执行可实现高达30.9倍的加速，相比高性能CPU上的执行加速7.3倍。设计空间探索（DSE）进一步为未来CIM加速器的设计提供了洞见。

## Abstract
Computing-in-Memory (CIM) accelerators execute Matrix-Vector Multiplications (MVMs) in memory, making them a compelling solution for Machine Learning (ML) workloads. However, existing ML workload partitioning approaches for CIM accelerators do not fully account for Resistive Random Access Memory (RRAM) constraints such as limited memory, high write latency, and limited endurance. They also neglect parallelism, low-level architectural effects, or the Central Processing Unit (CPU) as a complementary compute resource. To address these limitations, we propose an Integer Linear Programming (ILP)-based workload partitioning framework for heterogeneous CPU-CIM systems. It minimizes end-to-end inference latency under RRAM constraints, captures parallelism, and combines empirical profiling with analytical models. Using our framework, heterogeneous CPU-CIM execution achieves speedups of up to 30.9x over CPU-only execution on an edge CPU and 7.3x over a high-performance CPU. A Design Space Exploration (DSE) yields further design insights for future CIM accelerators.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：机器学习（ML）推理任务在资源受限的边缘设备上部署时，面临存算瓶颈。存内计算（CIM）加速器通过在内存中执行矩阵向量乘法（MVM）来缓解这一问题，尤其基于阻变随机存取存储器（RRAM）的CIM具有高密度、低功耗等优势。然而，现有ML工作负载划分方法存在以下不足：
  - 未充分考量RRAM的有限内存容量、高写入延迟和有限耐久性（重量编程成本高）。
  - 忽视了CPU作为互补计算资源的潜力，以及算子间的并行性和底层架构影响。
  - 已有CIM方法要么假定整个模型能完整放入CIM，要么依赖动态权重替换（损害耐久性）。
- **核心问题**：如何在一个由CPU和RRAM-based CIM加速器组成的异构系统中，实现**静态**的算子划分，以最小化端到端推理延迟，同时满足RRAM的内存预算和算子支持约束。
- **整体含义**：提出一个系统级的、结合经验分析与分析模型的整数线性规划（ILP）框架，能够自动决定将哪些算子放置于CPU或CIM，并利用并行性，显著加速推理。该框架也为未来CIM加速器的设计提供了设计空间探索（DSE）指导。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将工作负载划分建模为一个ILP优化问题，目标是最小化DAG（有向无环图）表示的计算图上的makespan（总推理延迟）。结合**经验性CPU性能分析**和**分析性CIM与互连延迟模型**，并引入并行性约束和RRAM资源约束。
- **关键技术细节**：
  1. **混合延迟表征**：
     - CPU延迟：通过ONNX Runtime在实际目标硬件上对每个算子进行经验性性能分析（多次迭代取中位数，包含预热）。
     - CIM延迟：基于分析模型，包括加载、计算（MVM）和存储时间。MVM时间与输入向量数成正比，与跨条数无关（跨条并行）。考虑跨条映射（公式1）和im2col转换卷积。
     - 跨设备传输延迟：基于共享总线带宽和数据量计算（公式4）。
  2. **ILP公式**：
     - 决策变量：每个算子v的二进制变量xv（=1表示CPU，=0表示CIM）。
     - 目标：最小化所有终端节点的完成时间最大值（makespan）。
     - 约束：设备分配约束、完成时间计算（公式7）、前置依赖与传输延迟（公式8-9）、并行算子间的顺序序列化（公式11-12，利用大M法）、RRAM跨条总数限制（公式14）。
  3. **并行性处理**：通过传递闭包找出无数据依赖的算子对（P），在两者都分配到CPU时引入二元排序变量ou,v强制串行化；若分配到CIM则自动并行（CIM核心内部可并行）。
- **算法流程**（文字描述）：
  1. 输入：ONNX模型、硬件规格（CPU、CIM参数、总线带宽）。
  2. 阶段I（延迟数据生成）：对每个算子，通过CPU经验分析获得Thost,v，通过分析模型获得Tacc,v（公式2-3）和传输延迟（公式4）。
  3. 阶段II（ILP求解）：构建ILP模型（公式5-14），使用Gurobi求解器找到最优或近似最优的xv分配和排序。
  4. 阶段III（输出）：得到划分后的模型及性能统计。

### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

- **数据集/场景**：未明确说明输入数据集（如图像分类/检测任务），但评估了多种CNN架构在实际硬件上的推理延迟。
- **Benchmark模型**：ResNet-18、ResNet-50（卷积密集型）、MobileNetV2（深度可分离卷积）、YOLOv5n（复杂分支结构）。所有模型量化至8-bit整数精度。
- **对比方法**：主要与**纯CPU执行**（CPU-only）比较加速比。没有直接对比其他划分方法（如Neurosurgeon、COMPASS等），但讨论了与相关工作的定性差异。
- **硬件平台**：两种CPU主机：AMD Ryzen 9 3900X（x86高性能）和ARM Cortex-A72（边缘部署）。CIM参数：跨条大小256×256，MVM延迟从50ns到1000ns，跨条数量从25到200。总线带宽：x86 20.48 GB/s，ARM 10.24 GB/s。

### 4. 资源与算力

- **算力资源**：论文未明确说明使用的GPU型号、数量或训练时长。所有延迟测量基于CPU（ARM/x86）和CIM加速器分析模型。ILP求解使用Gurobi，在单核CPU上运行（AMD Ryzen 9 3900X，32 GB DDR4）。未涉及模型训练，仅推理。
- **备注**：该论文属于系统优化工作，不涉及大规模GPU训练资源。

### 5. 实验数量与充分性

- **实验数量**：主要包含两部分：（1）不同MVM延迟和跨条数量下的加速比（图3-4）；（2）YOLOv5n的算子分配情况和延迟分布（图5）。还有ILP求解时间和MIP gap统计（表I）。总实验组数：4种模型 × 2种CPU × 4种MVM延迟 × 4种跨条数量 = 128种配置，加上跨条数量不变时不同MVM延迟重复，实际报告了关键组合。
- **消融实验**：没有显式的消融实验（如去掉并行约束、去掉CPU profiling等），但通过不同硬件参数（MVM延迟、跨条数、CPU类型）的对比，间接验证了各因素的影响。
- **充分性与公平性**：
  - 优点：覆盖了多种典型CNN架构（包括深度可分离、残差、检测模型），对比了纯CPU基线，且考虑了边缘与高性能两种CPU，体现了对实际平台差异的考量。
  - 不足：未与同类划分方法（如Neurosurgeon、COMPASS）进行直接定量比较，缺乏ablation study；YOLOv5n由于分支复杂导致ILP求解时间较长（最长600s），但论文承认了这一点并分析了原因。

### 6. 论文的主要结论与发现

1. **异构加速显著**：相比纯CPU，在ARM边缘CPU上最高加速30.9倍（50ns MVM延迟，≥100个跨条），在x86高性能CPU上最高加速7.3倍。
2. **MVM延迟是关键瓶颈**：当MVM延迟高于500ns时，x86上CIM加速不再有益（因为CPU本身更快）。ARM上加速比随MVM延迟增加而下降。
3. **跨条数量存在饱和点**：通常100个跨条即可覆盖所有计算密集型层，继续增加跨条不会带来额外收益。
4. **不同架构敏感性不同**：ResNet系列加速效果最好；MobileNetV2因深度可分离卷积不能有效映射到crossbar（向量-向量乘法，利用率低），加速有限；YOLOv5n因分支多导致划分复杂度高，但仍能获得1.9-3.1倍加速。
5. **ILP可扩展性**：对于线性或稀疏分支网络（ResNet-18/50, MobileNetV2），求解时间<1s且MIP gap<0.5%；对于高度分支网络（YOLOv5n），求解时间可达500s，MIP gap约7%，但仍可得到实用解。

### 7. 优点

- **方法创新**：首次系统地将CPU和CIM加速器作为互补资源进行静态划分优化，同时考虑了RRAM的写延迟和耐久性限制（采用权重固定数据流，避免动态编程）。
- **综合建模**：混合经验性CPU性能分析和分析性CIM/互连模型，兼具准确性和可扩展性（可快速探索多种CIM配置）。
- **并行性显式建模**：在ILP中引入算子间并行约束，避免不必要的串行化，更真实反映实际执行。
- **设计空间探索**：通过扫描MVM延迟和跨条数量，为未来CIM加速器设计提供了定量指导（如MVM延迟需低于特定阈值才能带来收益）。

### 8. 不足与局限

- **实验对比不足**：未与现有的ML划分方法（如Neurosurgeon、COMPASS）进行定量基准对比，仅定性讨论。结果中加速比仅与纯CPU比较，未与异构CPU+GPU或其他通用加速器比较。
- **ILP可扩展性局限**：对于高度分支的模型（如YOLOv5n），求解时间过长（≥500s），且MIP gap较大（7%），可能不适用于实时在线划分场景。论文未探索启发式或近似方法来提升效率。
- **缺乏实际CIM硬件验证**：所有CIM延迟基于分析模型，未在真实CIM芯片上验证。模型准确性依赖于假设（如跨条全并行、GPEU流水线），可能与实际硬件有偏差。
- **RRAM耐久性建模简化**：仅通过禁止动态重量编程来避免耐久性问题，但未考虑编程次数对寿命的影响量化模型。
- **未考虑更复杂的调度与内存层次**：假设静态分配一次完成，未考虑多层缓存、DMA等实际系统的复杂时序。

**（完）**
