---
title: Transition-Aware Backend Dispatch for Edge LLM Inference
title_zh: 面向边缘LLM推断的过渡感知后端调度
authors: "Alaaddin Goktug Ayar, Martin Margala"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17415v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 边缘LLM推理的过渡感知后端调度，硬件感知调度
tldr: "边缘平台上的大语言模型推理受执行后端性能差异影响，静态分配无法适应形状依赖变化，独立逐算子选择会导致高昂的框架切换成本。本文提出转换感知调度，结合当前算子特征与先前后端选择，在NVIDIA Jetson平台上对七种Transformer模型评估，相较最优静态策略减少17.4%延迟、14.4%能耗和28.5%能耗延迟积，并降低切换次数。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17415v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1841, \"height\": 874, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17415v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 465, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17415v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 848, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17415v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 914, \"height\": 480, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17415v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 969, \"height\": 331, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17415v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 468, \"label\": \"Table\"}]"
motivation: 现有后端调度方法要么固定分配无法利用形状差异，要么独立选择引入过多切换开销，亟需兼顾算子形状与转换代价的调度策略。
method: 通过组合当前算子特征与先前后端选择，在算子级别动态调度四种常见算子，并测量实际转换成本用于回放评估。
result: "在9584个算子实例上，转换感知调度相比最优静态策略平均降低延迟17.4%、能耗14.4%和能耗延迟积28.5%。"
conclusion: 融入算子形状与后端转换上下文可有效改进边缘Transformer推理的调度，在七个模型中六个的性能均有提升。
---

## 摘要
在边缘平台上高效执行大型语言模型（LLM）推断不仅受限于模型大小，还受到不同执行后端之间形状相关性能差异的影响。静态后端分配无法利用这种差异，而独立的每算子选择可能引入昂贵的设备和框架切换。本文提出了一种用于边缘Transformer推断的过渡感知后端调度方法。该方法将当前算子特征与先前选择的后端相结合，以保留有益的形状特定选择，同时避免不必要的过渡。从七个Transformer模型的完整模型推断运行中收集有序迹，并在NVIDIA Jetson平台上对四个常见算子类别在PyTorch eager CPU、PyTorch eager CUDA和ONNX Runtime CPU上进行基准测试。通过使用从实际后端切换中测量的观测算子成本和过渡成本的测量支持迹回放来评估调度策略。支持的算子被动态选择，而调度范围之外的算子保持静态分配。在9,584个有序算子实例和278个精确形状组上，相对于最佳静态策略，过渡感知调度平均将回放延迟、能量和能量延迟乘积分别降低了17.4%、14.4%和28.5%。它相对于算子局部选择也减少了切换。留一模型评估在七个保留模型中的六个上改善了所有三个目标，并在所有七个上改善了能量。这些结果表明，结合算子形状和后端过渡上下文可以改善边缘Transformer工作负载的选择性后端调度。

## Abstract
Efficient large language model (LLM) inference on edge platforms is limited not only by model size, but also by shape-dependent performance differences across execution backends. Static backend assignment cannot exploit this variation, while independent per-operator selection can introduce costly device and framework switches. This paper presents a transition-aware backend dispatch approach for edge transformer inference. The approach combines current operator features with the previously selected backend to preserve beneficial shape-specific choices while avoiding unnecessary transitions. Ordered traces are collected from full-model inference runs of seven transformer models, and four common operator classes are benchmarked across PyTorch eager CPU, PyTorch eager CUDA, and ONNX Runtime CPU on an NVIDIA Jetson platform. The dispatch policies are evaluated through measurement-backed trace replay using observed operator costs and transition costs measured from actual backend switches. Supported operators are selected dynamically, while operators outside the dispatch scope retain a static assignment. Across 9,584 ordered operator instances and 278 exact shape groups, transition-aware dispatch reduces replayed latency, energy, and energy-delay product relative to the best static policy by 17.4%, 14.4%, and 28.5% on average, respectively. It also reduces switching relative to operator-local selection. Leave-one-model-out evaluation improves all three objectives for six of seven held-out models and improves energy for all seven. These results demonstrate that incorporating operator shape and backend-transition context can improve selective backend dispatch for edge transformer workloads.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在资源受限的边缘平台（如 NVIDIA Jetson）上部署大语言模型（LLM）时，不同执行后端（PyTorch CPU、PyTorch CUDA、ONNX Runtime CPU）的性能高度依赖于算子形状（tensor 维度、序列长度等）。静态后端分配无法适应这种形状依赖变化，而独立逐算子选择最优后端虽能局部最优，却可能引入昂贵的设备/框架切换开销（数据移动、同步、张量转换等）。
- **整体含义**：需要一种“过渡感知”调度策略，在利用形状特定的后端优势的同时，避免不必要的切换代价，从而提升整体延迟、能耗和能耗延迟积（EDP）效率。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：结合**当前算子特征**与**先前选择的后端**，进行因果、贪心的逐算子调度。对于每个支持的算子，评估候选后端 \(b_i\) 的总代价为 \(C_i(b_i) = C_{op}(b_i) + C_{trans}(b_{i-1}, b_i)\)，其中 \(C_{trans}\) 在切换时取测量值，不切换时为0。选择总代价最小的后端。
- **关键技术细节**：
    - 从7个Transformer模型的完整推断运行中提取有序算子迹（共9,584个支持算子），保留模型、阶段、形状等元数据。
    - 对四种常见算子（Linear、MLP activation、RMSNorm、LayerNorm）进行精确形状分组（278个唯一形状组），避免冗余测量。
    - 在NVIDIA Jetson Orin Nano上独立测量每个形状-后端组合的延迟、活跃功率、能量、EDP，并测量所有9种后端对之间的实际切换代价（延迟和能量）。
    - 训练分类器（最终选用HistGBM）预测最优后端。Operator-local选择器只使用当前算子特征；Transition-aware选择器额外使用先前后端状态，并基于带切换代价的候选成本进行标签构建。
    - 通过测量驱动的迹回放（不实际运行混合后端运行时）来评估调度策略：按原始顺序检索预先测量的算子代价和切换代价，累计总延迟、总能量和EDP。

## 3. 实验设计：数据集/场景、Benchmark、对比方法
- **数据集/场景**：使用7种Transformer模型（Qwen2.5-0.5B, Qwen3-0.6B, TinyLlama-1.1B, SmolLM2-135M/360M, OLMo2-1B, ModernBERT-base），合成输入（batch size=1, FP32, 预填充序列长度16/32/64/128）。每个模型生成包含prefill和decode阶段的完整算子执行迹。
- **Benchmark**：9,584个有序算子实例，278个精确形状组，4种算子，3个后端。测量指标：延迟、能量、EDP、切换次数（后端/设备/框架）。
- **对比方法**：
    - **静态分配**（3种）：全模型使用单一后端（torch_eager_cpu / torch_eager_cuda / onnxruntime_cpu）。
    - **Operator-local选择**：独立为每个支持算子选择当前特征下的最优后端（基于测量表）。
    - **Transition-aware选择**（本文方法）：加入先前后端状态和切换代价。

## 4. 资源与算力
- 使用**单个 NVIDIA Jetson Orin Nano**，运行 JetPack 6.2，PyTorch 2.8.0，ONNX Runtime 1.23.2，CUDA 12.6。
- 功耗模式：**25W 模式**，不强制锁定时钟（反映默认行为）。
- **未明确说明模型训练或大规模集群资源**：所有工作均为边缘推理调度优化，未涉及模型训练。基准测试在单边缘设备上进行，每次测量使用3次热身+500次名义迭代（短算子自适应重复）。文中未提及总测量时间。

## 5. 实验数量与充分性
- **主实验**：7个模型 × 6种调度策略（3静态 + 1 operator-local + 2 transition-aware，分别针对延迟/能量/EDP目标）共产生约42组回放结果（图中展示每个模型每种目标的增益）。
- **消融实验**：对比 transition-aware 与 operator-local 的增益和切换减少（图4），明确区分形状专用化收益与切换规避收益。
- **泛化实验**：留一模型评估（7次，每次训练集6个模型，测试集1个），报告延迟、能量、EDP增益（表II）。
- **分类器选择**：使用 GroupKFold 交叉验证（按形状ID分组），对比4种分类器（LR, RF, ExtraTrees, HistGBM），基于验证悔度选择最佳模型。
- **充分性评价**：
    - **优点**：覆盖多种模型大小和架构（0.135B~1.1B）；支持3个后端、4种算子；使用精确形状分组避免冗余；测量实际切换代价；消融和泛化实验设计合理。
    - **不足**：仅使用F32精度，未覆盖量化、KV-cache、注意力等算子；回放模型而非集成运行时，无法验证端到端真实开销；仅一个边缘设备（Jetson Orin Nano），未推广到其他平台；合成输入而非真实文本。

## 6. 论文的主要结论与发现
- **核心结果**：Transition-aware dispatch 相比最佳静态策略，平均降低**延迟17.4%、能量14.4%、EDP 28.5%**（图3）。所有7个模型均获得三目标正向增益。
- **切换减少**：相比 operator-local 选择，transition-aware 平均减少后端切换14.9%（延迟目标）、14.8%（能量目标）、14.0%（EDP目标），设备切换减少最高19.7%，框架切换减少最高17.6%（图4）。
- **泛化能力**：留一模型评估中，能量目标在所有7个模型上均改善；延迟和EDP在6个模型上改善。主要失败案例是 ModernBERT-base（编码器架构），其形状分布与训练集（解码器模型）差异较大，导致延迟和EDP增益为负。
- **分类器质量**：transition-aware 分类器在834个决策上下文上准确率约89%~90%，悔度低于0.41%，表明预测可靠性高。

## 7. 优点：方法或实验设计上的亮点
- **实际转换代价建模**：直接测量9对后端切换的延迟和能量，而非假设固定开销。
- **精确形状分组基准**：278个形状组的测量覆盖完整算子迹，避免重复执行，提高评估效率。
- **因果+贪心策略**：简单可部署，无需全局序列优化。
- **测量驱动回放**：在没有真实混合后端运行时的前提下，科学评估调度潜力。
- **开源代码和可重复工作流**：提供Artifact[17]，支持扩展新模型、算子、后端。
- **多目标优化**：同时优化延迟、能量、能量延迟积，适应不同应用需求。

## 8. 不足与局限
- **实验覆盖不足**：
    - 仅评估4种算子（Linear、MLP activation、RMSNorm、LayerNorm），未包括Attention、KV-cache、量化算子、融合核等。
    - 仅使用FP32精度，未测试INT8/FP16，而边缘部署常需要量化。
    - 仅使用NVIDIA Jetson Orin Nano一个平台，结论的硬件泛化性有限。
- **评估偏差**：
    - 迹回放方法未实现真正的混合后端运行时，无法捕捉框架间切换的实际软件管道开销（如张量转换、上下文初始化）。
    - 合成输入（重复"hello"）不代表真实文本分布，可能遗漏变长序列的复杂形状变化。
- **泛化限制**：编码器架构（ModernBERT-base）的留一模型结果较差，表明训练数据需覆盖更多样化的形状分布。
- **静态范围外算子**：未调度算子（如Softmax、Embedding）保持静态分配，可能隐藏潜在切换机会。
- **无端到端延迟验证**：论文强调是“潜力”评估，实际部署时需集成运行时并验证。

（完）
