---
title: "Adaptive Depth Sparse Framework: Similarity-Driven Resource Allocation for Pre-Trained LLMs"
title_zh: 自适应深度稀疏框架：面向预训练大语言模型的相似性驱动资源分配
authors: "Yidu Wu, Xiang Wang, Kejie Zhao, Zhangchi Wang, Qinghai Guo, Xiaoying Tang"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21291v1"
tags: ["query:edge-llm"]
score: 8.0
evidence: 自适应深度稀疏实现资源高效的LLM推理
tldr: 大语言模型推理成本高，现有加速方法依赖任务级微调或从头训练。本文提出AdaDSF框架，通过层输入输出余弦相似度统计分配各层token保留比率，利用轻量路由器动态选择关键token，并引入特征保持对齐目标使稀疏模型近似稠密表示。在GPT-NeoX和Qwen2.5上，推理FLOPs显著降低，性能接近稠密模型，且同等稀疏度下精度退化小于MoD、D-LLM和DLO基线。该方法无需全重训练即可将预训练模型转换为深度稀疏模型，具备跨任务通用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21291v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1168, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21291v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 950, \"height\": 565, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21291v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1148, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1236, \"height\": 509, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 663, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 468, \"height\": 140, \"label\": \"Table\"}]"
motivation: 现存的LLM推理加速方法需任务级微调或重新训练，适应性成本高且难以跨任务复用，故需要一种无需全重训练的通用深度稀疏化方案。
method: 基于层输入/输出余弦相似度统计，为每层分配token保留比率；以轻量路由器动态筛选信息token；并通过特征保持对齐目标，使稀疏模型与稠密模型的中间和最终表示匹配。
result: 在GPT-NeoX和Qwen2.5上，推理FLOPs显著下降，性能接近稠密模型；相同稀疏度下精度退化低于MoD、D-LLM和DLO基线。
conclusion: AdaDSF可将预训练LLM高效转化为深度稀疏模型，无需全重训练，兼顾效率与性能，具备良好的泛用性。
---

## 摘要
大型语言模型（LLM）展现出强大的生成和推理性能，但Transformer架构的推理成本很高。现有的加速方法通常依赖于特定任务的微调或从头训练，增加了适配成本并限制了跨任务可用性。我们提出了一种自适应深度稀疏框架（AdaDSF），该框架无需完全重新训练即可将现成的预训练LLM转换为深度稀疏模型。我们的关键见解是，各层对表示变换的贡献不同，这通过层输入和输出隐藏状态之间的余弦相似度来表征。基于此，AdaDSF从相似度统计中分配逐层令牌保留比率，使用轻量级路由器每层选择信息性令牌，并引入一个特征保持对齐目标，使稀疏模型与密集模型之间的中间和最终表示相匹配。在GPT-NeoX和Qwen2.5上的语言建模和常识推理任务中，AdaDSF大幅减少了推理FLOPs，同时保持接近密集模型的性能。在可比稀疏度下，AdaDSF的精度下降始终小于包括MoD、D-LLM和DLO在内的强基线模型。

## Abstract
Large language models (LLMs) achieve strong generation and reasoning performance, but the Transformer architecture incurs high inference cost. Existing acceleration methods often rely on task-specific fine-tuning or training from scratch, increasing adaptation cost and limiting cross-task usability. We present an Adaptive Depth Sparse Framework (AdaDSF) that converts off-the-shelf pre-trained LLMs into depth-sparse models without full retraining. Our key insight is that layers contribute unequally to representation transformation, characterized by the cosine similarity between layer input and output hidden states. Based on this, AdaDSF assigns layer-wise token retention ratios from similarity statistics, uses a lightweight router to select informative tokens at each layer, and introduces a feature-preserving alignment objective to match intermediate and final representations between sparse and dense models. On GPT-NeoX and Qwen2.5 over language modeling and commonsense reasoning, AdaDSF substantially reduces inference FLOPs while preserving performance close to dense counterparts. Under comparable sparsity, AdaDSF consistently yields smaller accuracy degradation than strong baselines including MoD, D-LLM, and DLO.

---

## 论文详细总结（自动生成）

# 论文《Adaptive Depth Sparse Framework: Similarity-Driven Resource Allocation for Pre-Trained LLMs》详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大型语言模型（LLM）在生成和推理任务上表现出色，但标准的Transformer架构推理成本极高——计算量随深度线性增长、随序列长度二次增长。现有加速方法（如量化、蒸馏、轻量注意力）多依赖于特定任务的微调或从头训练，导致适配成本高、跨任务通用性差。
- **核心问题**：如何在不进行全重训练的前提下，将现成的预训练稠密LLM转换为高效推理的深度稀疏模型，同时保持性能接近原模型。
- **整体含义**：提出AdaDSF框架，通过层间隐藏状态相似度驱动计算资源分配，实现推理FLOPs大幅降低而精度退化极小，为部署场景下的资源受限LLM推理提供了一条低成本、高泛化性的路径。

## 2. 方法论：核心思想、关键技术细节、公式与流程

### 核心思想
- 各层对表示变换的贡献不均，可通过**层输入与输出隐藏状态间的余弦相似度**量化。
- 相似度高的层（变换小）应分配更少的计算资源，相似度低的层（变换大）应保留更多token。

### 关键技术细节
1. **相似度驱动的层间token保留比率分配**：
   - 对第 \(i\) 层，计算 \( s_i = \text{CosSim}(x_{in}^{(i)}, x_{out}^{(i)}) \)。
   - 通过温度归一化权重、偏差缩放、Sigmoid映射到 \( [0.05, 0.95] \) 区间，再经全局预算校正使得所有层的平均保留比率等于目标 \( t \)。

2. **轻量级token路由器**：
   - 每层新增一个MLP路由器，预测所有token重要度分数，按该层保留比率 \( r_i \) 选出Top-K个关键token（\( K_i = \lfloor r_i \cdot s \rfloor \)）。
   - 选中token执行完整Transformer层计算；未选中的token通过残差路径跳过，保持可微。

3. **特征保持对齐训练**：
   - **隐藏状态对齐**：每层稀疏输出与稠密输出的Softmax后均方误差。
   - **输出分布对齐**：稀疏模型与稠密模型的KL散度。
   - 总损失 = 输出对齐 + 逐层隐藏对齐的平均。

### 公式与算法流程（文字说明）
- 式(4)~(8)：相似度→权重→映射→预算校正→保留比率。
- 式(9): \( L_{hid}^{(l)} = \| \text{Softmax}(h_{sparse}^{(l)}) - \text{Softmax}(h_{dense}^{(l)}) \|^2 \)。
- 式(10): \( L_{align} = \sum_c P_{dense}(c|x) \log \frac{P_{dense}(c|x)}{P_{sparse}(c|x)} \)。
- 总体目标：\( L = L_{align} + \frac{1}{L} \sum_{l=1}^L L_{hid}^{(l)} \)。

## 3. 实验设计

### 数据集与场景
- **预训练场景**：GPT-NeoX-130M 在 Wikitext103 上训练。
- **指令微调场景**：Qwen2.5-0.5B 和 Qwen2.5-1.5B 在 GenQA、InfinityInstruct、OpenHermes2.5 上训练。
- **评估任务**：
  - 语言建模：Wikitext103 测试集。
  - 常识推理：ARC-Challenge、ARC-Easy、HellaSwag、PIQA、WinoGrande、OpenBookQA 共6个基准。

### 对比方法
- MoD (Mixture-of-Depths)
- D-LLM
- DLO
- 稠密基线（100% retention）

### 评估指标
- 计算量：归一化FLOPs（稀疏/稠密）。
- 语言建模：困惑度（PPL，越低越好）。
- 推理：准确率和平均准确率、与稠密基线的退化差值（Diff）。

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提及模型规模为130M~1.5B参数，属于中等规模实验，可推断算力需求不高，但无法量化。
- **（需指出）**：缺少算力细节，不利于复现和完整对比。

## 5. 实验数量与充分性

### 实验数量
- **主实验**：在Qwen2.5-0.5B/1.5B上对比MoD、DLO（表1a），以及在不同保留比率（90%/80%/70%）下的表现（表1b）。
- **GPT-NeoX实验**：在Wikitext103上对比MoD、D-LLM、DLO三种稀疏率（90%/80%/70%）（表2）。
- **消融实验**：
  - 相似度驱动分配策略（带/不带温度参数τ），表3。
  - 损失函数设计（仅因果损失 vs 全对齐损失），表4。

### 充分性与公平性
- **充分性**：覆盖了两种主流模型架构、两种训练场景（预训练和指令微调）、多个推理基准，消融实验验证了核心组件的有效性。实验设计较为全面。
- **客观性**：所有对比在相同稀疏度下进行，FLOPs和准确率同时报告，避免偏颇。但仅对比了深度稀疏类的基线（MoD、D-LLM、DLO），未与量化、蒸馏等方法对比（非同类方法，可接受）。
- **局限性**：
  - 模型最大仅1.5B参数，未验证更大尺度（7B+）的有效性。
  - D-LLM在GPT-NeoX上严重退化（PPL>1900），作者指出其不适合标准自回归预训练，但未进一步分析原因。

## 6. 主要结论与发现

- AdaDSF在几乎所有设置下均优于MoD、D-LLM、DLO，在相同稀疏度时性能退化更小（平均提升0.8%~4.7%）。
- 在Qwen2.5-0.5B上，80%保留率时平均准确率49.1%（退化-2.6），而MoD为44.4%（退化-7.3），DLO为48.3%（退化-3.4）。
- 在GPT-NeoX上，80%保留率时PPL 18.9（退化1.0），而MoD为21.6，DLO为19.6，D-LLM超过2000。
- 相似度驱动的分配策略和特征保持对齐损失均为关键，缺一不可。

## 7. 优点

1. **无需全重训练**：直接从预训练稠密模型转换，适配成本极低，跨任务通用。
2. **架构改动极小**：仅新增轻量MLP路由器，无需修改注意力或LayerNorm内部结构，易于集成。
3. **数据驱动的计算分配**：基于真实隐藏状态相似度统计，而非固定或启发式策略，更符合层间变换差异。
4. **特征保持对齐**：中间层和最终输出双重对齐，有效缓解稀疏推理引入的表示偏移。
5. **广泛的实验验证**：覆盖两种模型家族、两种训练范式、多个推理任务，并与多个强基线公平对比。

## 8. 不足与局限

1. **模型规模受限**：实验仅到1.5B参数，未在更大LLM（如7B、13B）上验证，通用性待确认。作者已将此列为未来工作。
2. **算力信息缺失**：未提供训练/推理的GPU型号、时长、能效等，不利于完整复现和成本评估。
3. **基线覆盖不够全面**：仅与深度稀疏类方法对比，未与非稀疏加速方法（如量化、蒸馏）进行端到端比较。
4. **消融实验数量偏少**：仅做了分配策略和损失函数的消融，未对路由器设计（如MLP vs 更复杂门控）、校准集大小、超参数β等进行充分探究。
5. **D-LLM异常结果未深入分析**：D-LLM在GPT-NeoX上PPL极高，论文仅以“不适合预训练场景”解释，缺乏进一步原因分析（如训练不稳定、代码复现问题等）。
6. **应用限制**：方法需要额外对齐训练（虽然轻量），对于完全无法微调的私有模型可能不适用。

（完）
