---
title: Task-Oriented Communication with Hybrid-Precision Models
title_zh: 混合精度模型的任务导向通信
authors: "Songjie Xie, Wei Guo, Shenghui Song, Jun Zhang, Ying-Jun Angela Zhang, Khaled B. Letaief"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16766v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 边缘推理，混合精度模型，直接针对边缘设备资源高效LLM推理
tldr: 边缘推理面临通信与计算分离的挑战，现有方案难以平衡传输效率、设备成本和精度。本文提出混合精度任务导向通信框架，设备端部署二值化前端提取二进制特征，通过OFDM信号传输，服务器端全精度后端推理。采用子载波特征校准和知识蒸馏训练，在ImageNet上实现通信效率、计算开销和推理精度的最优权衡，显著优于现有方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16766v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 284, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1754, \"height\": 319, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1856, \"height\": 819, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 629, \"height\": 732, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 760, \"height\": 656, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 824, \"height\": 604, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1688, \"height\": 619, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1690, \"height\": 616, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 792, \"height\": 1160, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16766v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 791, \"height\": 1726, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16766v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1860, \"height\": 675, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16766v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1867, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16766v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 825, \"height\": 615, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16766v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 841, \"height\": 615, \"label\": \"Table\"}]"
motivation: 现有边缘推理方案分别优化通信或计算，缺乏协同设计，导致无法同时兼顾传输效率、设备成本和推理精度。
method: 提出混合精度框架：设备端二值化前端提取二进制特征，通过OFDM传输，服务器端全精度后端推理；引入子载波特征校准和知识蒸馏训练策略。
result: 在ImageNet上验证，该框架在通信效率、设备计算成本和推理精度三者间达到最优平衡，性能超越现有边缘推理方法。
conclusion: 混合精度框架有效解决了边缘推理中通信与计算的协同优化问题，为资源受限场景提供高效解决方案。
---

## 摘要
边缘推理通过在网络边缘部署模型以规避云路由延迟，已成为人工智能服务普及的一种有前途的解决方案。现有的边缘推理方法主要集中在协作推理以减少延迟，或设计轻量级模型以适应资源受限设备。这些解决方案通常分别处理通信和计算挑战，因此难以在传输效率、设备端处理成本和推理精度之间实现平衡。为弥补这一差距，本文提出了一种用于边缘推理的混合精度任务导向通信框架，以全面平衡通信、设备端计算和效用。在该框架中，边缘设备上部署二值化前端，通过正交频分复用信号提取并传输二值特征，而边缘服务器上的全精度后端则执行最终推理。为确保模型一致性，我们引入了一种针对分割推理定制的设备端二值化方法，并开发了一种集成的信道感知传输方案，该方案具有基于子载波的特征校准功能。此外，还开发了一种基于知识蒸馏的训练策略，由专门的梯度估计器支持，以优化端到端系统并从全精度教师模型中继承语义知识。在大规模ImageNet数据集上的大量实验证明了所提混合系统的优越性。我们的分析证实，该设计实现了通信效率、设备端计算成本和推理精度之间的最优权衡，优于现有的边缘推理解决方案。

## Abstract
Edge inference has emerged as a promising solution for the proliferation of artificial intelligence (AI) services by deploying models at the network edge to circumvent cloud-routing latency. Existing edge inference approaches mainly focused on either cooperative inference to reduce latency or lightweight model design to fit resource-constrained devices. These solutions often address the communication and computation challenges separately, and thus struggle to achieve a balanced trade-off among transmission efficiency, on-device processing cost, and inference accuracy. To bridge this gap, this paper proposes a hybrid-precision task-oriented communication framework for edge inference to holistically balance communication, on-device computation, and utility. In this framework, a binarized front-end is deployed on the edge device to extract and transmit binary features via orthogonal frequency-division multiplexing (OFDM) signals, while a full-precision back-end on the edge server performs the final inference. To ensure model consistency, we introduce an on-device binarization method tailored for split inference and develop an integrated channel-aware transmission scheme featuring subcarrier-based feature calibration. Furthermore, a knowledge distillation (KD)-based training strategy, supported by specialized gradient estimators, is developed to optimize the end-to-end system and inherit semantic knowledge from a full-precision teacher model. Extensive experiments on the large-scale ImageNet dataset demonstrate the superiority of the proposed hybrid system. Our analysis confirms that this design achieves an optimal trade-off among communication efficiency, on-device computational cost, and inference accuracy, outperforming existing edge inference solutions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：边缘推理需要在资源受限的终端设备上完成低延迟、高精度的推理，但现有方案存在“顾此失彼”的困境：
  - **服务器端推理**（传输原始数据）通信开销大、隐私风险高；
  - **分割推理**（设备端运行部分全精度网络）虽减少传输，但设备端仍需大量浮点运算（FLOPs）；
  - **端到端二值化神经网络（BNN）** 降低设备计算量，但严重牺牲精度；
  - 已有方法未能同时优化**通信效率、设备计算开销和推理精度**三个维度。
- **整体含义**：本文提出一种**混合精度任务导向通信框架**，通过在设备端部署二值化前端（低FLOPs/低BOPs），将提取的二进制特征经OFDM系统传输至服务器，由全精度后端完成推理，从而实现三者的最优权衡。

---

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用“设备端二值化 + 服务器端全精度”的分割推理架构，结合信道感知传输与知识蒸馏训练，使二进制特征既保持通信效率，又能被服务器端有效恢复并用于精确推理。
- **关键技术细节**：
  1. **设备端二值化前端（On-Device BNN）**：
     - 采用类似XNOR-Net的权重二值化（符号函数+缩放因子α），激活二值化（符号函数）。
     - 引入可学习偏置β₀以调整二值化阈值（类似ReActNet），残差连接保留部分实值信息。
     - 输出特征图在传输前需进一步二值化：对每个空间位置，用缩放因子Υ进行特征级二值化（计算最优二进制向量与缩放因子）。
  2. **子载波特征校准（Subcarrier-Based Feature Calibration）**：
     - 在OFDM系统中，根据子载波信道增益（|hₖ|²）降序排序，将调制后的二进制特征段映射到高质量子载波上，确保关键信息优先受保护。
     - 服务器端使用与误比特率相关的掩码Ωₖ = 1 - 2pₖ（pₖ为子载波等效BSC错误概率），结合缩放因子Υ重建实值特征，抑制信道噪声。
  3. **服务器端全精度后端（Server-Based FPNN）**：
     - 接收经掩码恢复的实值特征，由标准全精度卷积网络（如ResNet后半部分）完成最终推理。
  4. **知识蒸馏（KD）端到端优化**：
     - 损失函数：交叉熵（真实标签） + KL散度（与预训练全精度教师模型输出对齐）。
     - 训练中自动满足信息瓶颈（IB）原则：二进制特征本身熵有上限，信道噪声与子载波校准进一步限制互信息，从而隐式抑制任务无关冗余。
     - 使用直通估计器（STE）处理符号函数、数字调制/解调、信道模型的不可微性，保证梯度反向传播。

---

### 3. 实验设计

- **数据集与场景**：
  - **ILSVRC12 ImageNet**（1000类，1.2M训练/50K验证），大型数据集，适合评估实际边缘推理场景。
- **基准方法（Baselines）**：
  - **Server-Based FPNN**：原始数据传输给服务器运行全精度网络。
  - **FPNN-Split-Compression**：添加瓶颈模块压缩通道数（4/5/6），浮点特征用64位双精度传输。
  - **FPNN-Split-VQ**：使用向量量化（256码本，7维特征映射）将浮点特征转为离散表示。
  - **Full BNN**：15种代表性二值化网络（XNOR-Net, ABC-Net, Bi-Real-Net, IR-Net, ReActNet, AdaBin等），全部在设备端运行。
- **模型架构**：
  - 基于ResNet-18/34/50/101，从中点分割：前半部分二值化作为设备端（B-9/B-15），后半部分全精度作为服务器端（FP-9/19/28/79）。
  - 设备端B-9/B-15参数量极小（0.68M/1.34M），BOPs约0.88×10⁹/1.57×10⁹，FLOPs约1.20×10⁸/1.22×10⁸。
- **实现细节**：
  - OFDM参数：NFFT=256，CP=16，Ψ=8径多径信道，延迟扩展γ=4。
  - 调制：16-QAM；每个图像需51个OFDM符号（49个特征+2个缩放因子）。
  - 知识蒸馏：教师模型为ResNet-101（对ResNet-101混合模型）或ResNet-50（对其他配置）。
  - 不同训练SNR（5/10/15/20 dB）和测试SNR（0~20 dB）条件。

---

### 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长等信息。
- 仅提及采用了知识蒸馏训练，但具体训练配置（如batch size、学习率调度、迭代轮数等）未给出。
- 由于使用了ImageNet（120万图像），训练大规模混合模型（ResNet-101后端）通常需要多块高端GPU，但论文未提供细节。

---

### 5. 实验数量与充分性

- **实验组数**：涵盖四个主要方向的对比实验：
  - **模型容量测试**（图6）：不同B-9/B-15与FP-9/19/28/79组合，与原始全精度模型对比（共8组）。
  - **与Full BNN对比**（表III、IV）：在ResNet-18和ResNet-34骨干上与15种SOTA BNN对比，每组报告精度、BOPs、FLOPs。
  - **与边缘协作推理对比**（图7-8）：在ResNet-18/34/101骨干上，与FPNN-Split-Comp.（三种压缩率）、FPNN-Split-VQ对比，分别在SNR=20dB和10dB下展示三个维度（误差率、OPs、符号数）。
  - **鲁棒性实验**（图9）：四种训练SNR下，测试SNR从0到20dB的变化，含B-9/FP-9和B-9/FP-79两组。
  - **消融实验**（图10）：去除子载波校准的对比，四种训练SNR条件。
- **充分性评价**：
  - **优点**：基准覆盖全面（15种BNN、多种协作推理变体）；骨干网络规模从ResNet-18到101；信道条件涵盖多径衰落与不同SNR；消融实验验证了关键模块的有效性。
  - **略有不足**：未在更多数据集（如CIFAR-10/100或真实无线信道数据）上验证；未对比轻量级浮点分割网络（如MobileNet分割）；未讨论传输过程中的延迟（OFDM符号数虽给出，但未明确计算端到端延迟）。

---

### 6. 论文的主要结论与发现

1. **混合精度框架实现了最优权衡**：在通信效率（少量OFDM符号）、设备计算开销（极低BOPs/FLOPs）和推理精度（接近全精度）三个维度均优于现有方法。
2. **强大的服务器端模型可弥补二值化损失**：当服务器后端足够大（如FP-79/ResNet-101）时，精度接近原始全精度模型（差距仅~1%），而设备端计算量仅为标准BNN的一半。
3. **子载波特征校准显著提升鲁棒性**：相比无校准版本，校准后的模型在训练SNR附近测试时精度更高；低SNR训练可作为一种噪声正则化，提升极端信道下的生存能力。
4. **知识蒸馏有效传递语义信息**：借助教师模型软标签，混合模型能更好地学习压缩表示，优于仅用交叉熵训练。
5. **协议是“设备轻、服务器重”的拆分策略**：该设计使得资源受限设备可受益于大型模型（如ResNet-101），而标准端到端BNN难以部署如此大的骨干。

---

### 7. 优点

- **系统性权衡设计**：同时优化通信、计算和精度，而非单方面追求极值。
- **硬件友好**：设备端仅需二值操作（XNOR+popcount）和极低FLOPs，适合IoT/可穿戴设备。
- **信道感知传输**：子载波校准与误比特率掩码巧妙地将信道状态纳入特征恢复，无需额外反馈。
- **训练实用化**：知识蒸馏+STE解决了不可微符号函数与数字通信模块的联合训练难题。
- **实验全面**：在ImageNet上对比大量SOTA，覆盖多种骨干和信道条件，验证充分。
- **可扩展性好**：框架不限于分类任务（论文末提及可拓展到多模态大模型），未来可推广。

---

### 8. 不足与局限

- **资源信息缺失**：未报告训练所需GPU型号、数量、时间，无法评估实际部署所需的计算成本。
- **数据集单一**：仅使用ImageNet分类，未在目标检测、分割等其他边缘推理任务上验证。
- **延迟分析不足**：虽然给出了OFDM符号数，但未测量实际端到端推理延迟（包括编码/解码/通信时间）。
- **信道模型简化**：多径信道使用指数衰落模型，未采用标准3GPP信道模型或实测信道冲激响应。
- **未考虑多用户干扰**：仅考虑点对点场景，未扩展到多设备协作或干扰环境。
- **静态子载波排序**：校准方案基于瞬时信道增益排序，可能在高移动场景下因信道快速变化而过期，论文未讨论适应性。
- **对比局限性**：未与更近期的混合精度推理方案（如INT8/INT4量化分割）或自适应量化通信方法对比。
- **代码未开源**：论文未提及代码开放计划，可复现性存疑。

（完）
