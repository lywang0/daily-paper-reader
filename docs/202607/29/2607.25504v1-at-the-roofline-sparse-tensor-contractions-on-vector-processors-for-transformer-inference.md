---
title: At-the-Roofline Sparse Tensor Contractions on Vector Processors for Transformer Inference
title_zh: 面向Transformer推理的向量处理器上近屋顶稀疏张量收缩
authors: "Bowen Wang, Chi Zhang, Diyou Shen, Renzo Andri, Navaneeth Kunhi Purayil, Luca Benini"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25504v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 面向向量处理器的Transformer推理硬件感知稀疏张量收缩
tldr: "针对Transformer推理中稀疏张量收缩在向量处理器上远低于roofline性能的问题，提出Ventaglio，通过运行时配置的稀疏执行单元和RVV扩展实现索引收集-累加-分散。在12nm FinFET实现中，加速6.9-7.4倍且面积开销仅3.1%。在LLaMA-3-8B模型上，prefill和解码分别获得2.40-5.25倍和2.06-3.16倍加速。该方法有效驱动稀疏计算接近roofline，具有实用价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25504v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1199, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25504v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 920, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25504v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 657, \"height\": 515, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25504v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 907, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.25504v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 924, \"height\": 427, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 926, \"height\": 491, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25504v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 935, \"height\": 315, \"label\": \"Table\"}]"
motivation: 现有向量处理器缺乏对稀疏张量收缩的原生索引累加支持，导致性能远低于roofline。
method: 提出Ventaglio，包含运行时配置的稀疏执行单元和RVV扩展，支持索引收集-累加-分散操作。
result: "在12nm FinFET实现中，稀疏张量收缩加速6.9-7.4倍，面积开销3.1%；LLaMA-3-8B上prefill和解码分别加速2.40-5.25倍和2.06-3.16倍。"
conclusion: Ventaglio有效利用权重和激活稀疏性，驱动稀疏张量收缩接近roofline，且面积开销小。
---

## 摘要
细粒度权重剪枝和激活稀疏化已成为减少Transformer模型推理计算和内存成本的有效方法。在中等稀疏度情况下，Gustavson数据流提供了一种通过元数据驱动的索引累加在向量处理器上同时利用激活和权重稀疏性的自然执行模型。然而，现有的RVV架构缺乏对这种模式的原生支持，迫使内核依赖软件索引解码和L1支持的索引存储操作，这使得稀疏张量收缩的性能远低于其屋顶线性能界限。我们提出了Ventaglio，一个运行时可配置的稀疏执行单元，结合RVV ISA扩展，通过索引收集-累加-散射支持将稀疏张量收缩推向其屋顶线。集成到一个开源向量处理集群中，并在12nm FinFET中实现，Ventaglio将稀疏张量收缩内核加速了6.9–7.4倍（相对于优化的RVV基线），仅增加3.1%的面积开销（针对紧密耦合L1的向量处理单元集群）。我们构建了Ventaglio扩展的性能精确指令级模型，对照RTL实现进行校准，并利用它在大型4×4多集群系统上进行扩展性能分析。使用DuoGPT剪枝的LLaMA-3-8B模型，具有实际40–60%的双重稀疏性，Ventaglio在预填充和自回归解码阶段分别实现了相比密集基线的2.40–5.25倍和2.06–3.16倍加速。

## Abstract
Fine-grained weight pruning and activation sparsification have emerged as effective approaches for reducing the compute and memory cost of inference for Transformer models. In the moderate-sparsity regime, Gustavson's dataflow provides a natural execution model for exploiting both activation and weight sparsity on vector processors through metadata-driven indexed accumulation. However, existing RVV architectures lack native support for this pattern, forcing kernels to rely on software index decoding and L1-backed indexed memory operations that keep sparse tensor contractions far below their roofline performance bound. We present Ventaglio, a runtime-configurable sparse execution unit coupled with RVV ISA extensions that drives sparse tensor contractions toward their roofline through indexed gather-accumulate-scatter support. Integrated into an open-source vector processing cluster and implemented in 12nm FinFET, Ventaglio accelerates sparse tensor contraction kernels by $6.9\text{--}7.4\times$ over optimized RVV baselines, with only $3.1\%$ area overhead for a cluster of tightly-L1 coupled vector processing elements. We build a performance-accurate instruction-level model of the Ventaglio extension, calibrate it against RTL implementation, and leverage it for scale-out performance analysis on a large $4\times4$ multi-cluster system. Using a DuoGPT-pruned LLaMA-3-8B model with practical $40\text{--}60\%$ dual sparsity, Ventaglio achieves $2.40\text{--}5.25\times$ and $2.06\text{--}3.16\times$ speedup over dense baselines during prefill and autoregressive decoding, respectively.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- Transformer模型规模的快速增长导致推理时巨大的计算和内存开销，稀疏化（权重剪枝和激活稀疏化）是降低开销的有效手段。
- 在中等稀疏度（40–75%零元素）下，Gustavson数据流提供了一种自然的执行模型，通过元数据驱动的索引累加同时利用激活和权重稀疏性。
- 然而，现有的RISC-V向量（RVV）架构缺乏对元数据驱动索引累加模式的原生支持，依赖软件索引解码和L1支持的索引内存操作，导致稀疏张量收缩性能远低于芯片的理论屋顶线（roofline）性能。
- 本文旨在通过硬件-软件协同设计，填补这一空白，提出**Ventaglio**，将元数据驱动的索引累加提升为原生RVV执行原语，使稀疏张量收缩接近屋顶线性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：解耦扩展的稀疏状态与向量寄存器文件（VRF），将稀疏累加状态存储在专用的多通道内存单元中，该单元具有集成收集/散射（gather/scatter）支持，避免与VRF端口争用，提供非对称带宽。
- **关键技术细节**：
  - **Ventaglio微架构**：包含一个多通道锁存标准单元存储器（SCM），配置C个通道（本工作中C=4），提供线性扩展的带宽；收集数据路径将宽多通道读取转换为紧凑操作数供向量算术单元（VAU）使用，散射路径执行逆映射写回。
  - **与Spatz集群集成**：Ventaglio作为运行时可配置的稀疏执行单元，通过控制状态寄存器（CSR）选择活跃的收集/散射路径；重用RVV向量寄存器命名空间，通过CSR将选定向量寄存器动态映射到Ventaglio内存单元，引入VRF旁路通道。
  - **ISA扩展**：提出三条新指令：
    - `vlx.v`：从L1 SPM加载打包的索引元数据到VRF。
    - `vfxmacc.vf`：执行融合的索引乘累加，通过`idx`字段选择索引上下文。
    - 地址后增量指令（`p.vle.v`, `p.vlx.v`, `p.fl`）：消除显式指针算术指令，减少循环开销。
- **数据流**：对于每个非零激活，取对应压缩权重的行，通过元数据确定输出坐标，执行索引收集、乘累加、散射写回。Ventaglio将此过程硬件化。

## 3. 实验设计：数据集、场景、benchmark、对比方法

- **实验场景**：
  - **内核基准测试**：三种稀疏张量收缩工作负载（矩阵-矩阵和矩阵-向量）：
    1. 密集激活 + 50%稀疏权重（2:4格式）。
    2. 密集激活 + 75%稀疏权重（1:4格式）。
    3. 50%稀疏激活 + 60%稀疏权重（位图格式）。
    逻辑内核尺寸为256(1)×256×256。
  - **端到端推理评估**：在LLaMA-3-8B模型上（FP16精度），评估预填充（prefill，序列长度S=512, 1K, 2K）和自回归解码（decode，序列长度S=1，KV缓存长度512, 1K, 2K）。
- **基准**：密集基线（无稀疏性）和优化的RVV基线。
- **对比方法**：基线Spatz集群（无加速）、Ventaglio集成Spatz（含vfxmacc扩展）、相同设计再增加地址后增量支持。
- **实现**：RTL综合、布局布线在12nm FinFET工艺，目标1GHz。

## 4. 资源与算力

- **硬件实现**：Ventaglio集成到双核Spatz集群中，在12nm FinFET工艺下综合实现，目标频率1GHz。
- **评估工具**：内核级使用QuestaSim 2022.3 RTL仿真；系统级使用GVSoC性能精确指令级模型（经RTL校准，最大偏差2.3%）。
- **多集群系统**：4×4多集群系统通过2D NoC连接，每集群接口1024位网络链路，支持组播和归约。片外内存：4×2 HBM2通道，聚合峰值带宽512 GB/s。
- **未明确说明**：未提及GPU型号、训练时长等算力信息（本文聚焦推理加速，非训练）。

## 5. 实验数量与充分性

- **内核实验**：三种稀疏配置×三组硬件配置（基线、Ventaglio、Ventaglio+后增量）=9组内核实验，另报告了屋顶线分析（图3）。
- **端到端实验**：预填充（3种序列长度）和解码（3种KV长度）×密集vs稀疏对比=12组实验，并给出了运行时分解（图4）。
- **充分性**：实验覆盖了矩阵-矩阵和矩阵-向量两种算子，以及不同稀疏度和序列长度，对比了优化的基线。消融实验（去使能后增量）展示了各组件贡献。实验设计较为全面，但消融种类（如不同Ventaglio配置、不同稀疏格式）有限。结论客观，与理论分析一致。

## 6. 论文的主要结论与发现

- Ventaglio实现稀疏张量收缩内核加速6.9–7.4倍（相对于优化RVV基线），VAU利用率达到89.3–97.4%，接近屋顶线。
- 硬件开销极低：12nm FinFET下集群面积仅增加3.1%（每核心9.1%）；无新关键路径。
- 端到端推理：在DuoGPT剪枝的LLaMA-3-8B上（40–60%双重稀疏性）：
  - 预填充阶段：2.40–5.25倍加速（因计算密集，FLOP减少直接转化）。
  - 自回归解码阶段：2.06–3.16倍加速（带宽受限，但仍通过稀疏DMA传输获得收益）。
- 地址后增量扩展进一步消除剩余开销，使所有工作负载达到屋顶线。

## 7. 优点

- **方法创新**：通过解耦稀疏累加状态与VRF，并提供可配置的多通道内存单元，以低开销实现接近屋顶线性能，为RVV处理器提供了原生稀疏支持。
- **完整性**：从微架构、ISA扩展、集成、RTL实现到系统级仿真，全栈评估。
- **开源**：代码开源（pulp-platform/spatz），促进复现与扩展。
- **实用价值**：在真实LLaMA-3-8B模型和实际稀疏度下验证，加速效果显著，面积开销极小，适用于边缘部署。
- **泛化性**：支持多种稀疏格式（N:M, 位图），运行时配置，灵活性强。

## 8. 不足与局限

- **容量限制**：Ventaglio内存单元仅1 KiB（C=4），限制了沿M维展开的深度，导致更短向量操作和更高循环开销（在更高稀疏度下更明显）。
- **仅评估中等稀疏度（40–60%）**：极稀疏或极稠密场景未覆盖；稀疏激活场景需要额外控制指令选择非零元素，开销未完全消除。
- **仅评估LLaMA-3-8B**：单一模型，其他Transformer架构（如编码器模型、多模态）未测试。
- **缺乏与其他稀疏加速器直接对比**：表I对比了相关设计（NVIDIA, AMD, Arm, IndexMAC, SCG），但无统一benchmark开源实现对比。
- **系统级评估基于性能模型**：虽经RTL校准（最大2.3%偏差），但4×4多集群NoC和HBM2模型存在简化，实际性能可能受其他因素影响。
- **未考虑功耗和能效**：仅报告面积，无功耗或能效数据。

（完）
