---
title: "Neuromorphic Diffusion Language Models: Addressing Compute and Memory Bottlenecks via Sparsity and Block Denoising"
title_zh: 神经形态扩散语言模型：通过稀疏性和块去噪解决计算与内存瓶颈
authors: "Dengyu Wu, Clement Ruah, Jiechen Chen, Bipin Rajendran, Osvaldo Simeone"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.24841v1"
tags: ["query:edge-llm"]
score: 8.0
evidence: 利用神经形态计算和块扩散解决LLM推理的计算和内存瓶颈
tldr: 自回归LLM推理时每个token需访问全部参数，效率低。掩蔽扩散语言模型（MDLM）通过每参数访问生成多token可提升内存受限场景效率。本文提出神经形态MDLM（N-MDLMs），将块扩散与脉冲神经计算结合，利用脉冲诱导的稀疏性跳过非激活通道，减少参数访问和计算。同时开发令牌级roofline模型分析协同效应。在翻译任务上，N-MDLMs即使在计算受限平台也显著提升能效和吞吐量，克服了MDLM的局限。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24841v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 817, \"height\": 511, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24841v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 773, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24841v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 635, \"height\": 460, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24841v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 643, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24841v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 643, \"height\": 456, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24841v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 151, \"label\": \"Table\"}]"
motivation: 自回归LLM推理效率低，MDLM在计算受限平台无法超越自回归模型，需进一步优化能效和吞吐量。
method: 提出N-MDLMs，集成块扩散以并行生成多token，并采用脉冲神经元实现稀疏激活，跳过非激活通道降低参数流量。
result: 翻译任务实验表明，N-MDLMs在计算受限平台实现了能效和吞吐量的显著提升，优于自回归和标准MDLM。
conclusion: 脉冲稀疏性与块扩散的协同作用有效缓解了计算和内存瓶颈，为高效语言模型推理提供了新范式。
---

## 摘要
自回归（AR）大语言模型（LLMs）在推理时本质上效率低下，因为每个生成的token都需要访问完整的模型参数，导致操作强度低且能耗高。掩码扩散语言模型（MDLMs）通过允许每次参数访问生成多个token，部分解决了内存受限场景下的这一限制。为了在具有大容量片上内存的现代平台上进一步提升推理效率，本文提出神经形态MDLMs（N-MDLMs），它将块扩散与基于脉冲的神经形态计算相结合，以共同提高吞吐量和能效。块扩散通过每次参数访问产生多个token来增加token吞吐量，而脉冲诱导的稀疏性通过跳过非活动通道来减少有效参数流量和计算量。为了分析稀疏性和扩散的协同效应，我们开发了一个受屋顶线启发的token级模型，该模型捕捉了块并行生成和脉冲稀疏性对解码效率的综合影响。在翻译任务上的实验结果表明，由于脉冲诱导的稀疏性，即使在计算受限的平台上，N-MDLMs也能显著提高能效和吞吐量，而这些平台上MDLMs无法优于AR-LLMs。

## Abstract
Autoregressive (AR) large language models (LLMs) are inherently inefficient at inference time because each generated token requires accessing the full set of model parameters, leading to low operational intensity and high energy consumption. Masked diffusion language models (MDLMs) partially address this limitation for memory-bound settings by allowing multiple tokens to be generated per parameter access. In order to further enhance inference efficiency on modern platforms with extensive in-chip memory, this work proposes neuromorphic MDLMs (N-MDLMs), which integrate block diffusion with spike-based neuromorphic computation to jointly improve throughput and energy efficiency. While block diffusion increases token throughput by producing multiple tokens per parameter access, spike-induced sparsity reduces effective parameter traffic and computations by skipping inactive channels. To analyze the synergistic effect of sparsity and diffusion, we develop a token-level roofline-inspired model that captures the combined impact of block-parallel generation and spike sparsity on decoding efficiency. Experimental results on translation tasks show that, thanks to spike-induced sparsity, N-MDLMs achieve substantial improvements in energy efficiency and throughput even in compute-bound platforms for which MDLMs would fail to improve over AR-LLMs.

---

## 论文详细总结（自动生成）

# 中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：自回归大语言模型（AR-LLMs）推理时每个生成token都需要访问全部模型参数，导致操作强度低、能耗高，尤其在计算受限的现代平台（如具有大容量片上内存的系统）上效率瓶颈更加突出。
- **背景**：掩码扩散语言模型（MDLMs）通过每次参数访问生成多个token（块并行）来缓解内存受限场景的效率问题，但在计算受限平台（如近内存计算设备）上效果有限。现有神经形态计算利用脉冲稀疏性可减少计算和内存访问，但尚未与扩散模型结合。
- **动机**：探索将块扩散与脉冲神经计算协同集成，以同时提升吞吐量和能效，特别是在MDLM原本无法超越AR-LLM的计算受限平台上。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出神经形态掩码扩散语言模型（N-MDLM），通过量化转换将预训练的编码器-解码器MDLM改造为基于积分-点火（IF）神经元的脉冲神经网络，利用块扩散并行生成多token，并利用脉冲稀疏性跳过非激活通道以减少参数流量和计算。
- **关键技术细节**：
  - **架构**：采用编码器-解码器结构，编码器处理上下文生成隐表示，解码器对初始全掩码块进行S步去噪，每步掩码B/S个token。
  - **转换方法**：将每层激活向量a通过自适应步长量化得到整数量化值â，再用时间编码（T步三元脉冲）表示。线性变换通过改进的IF神经元（软重置、带权重缩放）近似重建，实现事件驱动计算。
  - **分析模型**：开发token级roofline模型，定义每token操作数 \( N_{\text{ops}} = 2 T D^2 (1-\sigma) \) 和每token内存传输量 \( N_{\text{data}} = D^2 (1-\sigma^{BT}) b_W / B \)。令牌延迟 \( TL = \max(N_{\text{ops}}/L_{\text{ops}}, N_{\text{data}}/L_{\text{data}}) \)，吞吐量由各层延迟求和取倒数得到。能量由操作数和内存传输数乘以对应能耗常数得到。
- **算法流程**（文字描述）：
  - 预训练MDLM → 量化转换（激活量化+IF神经元模拟）→ 微调（Adam优化器，少量步数）→ 推理时：编码器稀疏前向计算，解码器迭代去噪，每步仅有非零脉冲触发的线性层参与计算和内存读取。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：WMT 14 德语→英语（DE-EN）翻译任务。
- **基准模型**：AR-LLM（自回归模型）作为基线，吞吐量归一化基准。
- **对比方法**：
  - AR-LLM（标准自回归）
  - MDLM（标准掩码扩散，块大小B=2,4,8）
  - N-AR-LLM（对AR-LLM应用相同神经形态转换）
  - N-MDLM（不同稀疏参数K=1,2,3,4）
- **评估指标**：token吞吐量（token/s）、每token能量（J/token）、BLEU翻译质量。
- **硬件参数配置**：两种系统模型——Off-Chip Memory System（OCMS，类似GPU）、In-Chip Memory System（ICMS，类似神经形态硬件），各自具有不同的计算吞吐率Lops、内存带宽Ldata、能耗参数（见表I）。

## 4. 资源与算力
- 文中明确说明所有实验在 **一台 NVIDIA DGX Spark GPU** 上进行，未提及具体GPU数量或型号细节。
- 微调阶段使用Adam优化器，训练 **1000步**，batch size 16，学习率3e-5。
- 未报告总的训练时长或推理时硬件资源占用情况，也未说明是否使用了多卡或分布式训练。

## 5. 实验数量与充分性
- **主要实验组数**：
  - 在不同块大小B（2,4,8）和不同稀疏参数K（1,2,3,4）下，分别对比AR-LLM、MDLM、N-AR-LLM、N-MDLM的吞吐量（图3）。
  - 在两种系统配置（OCMS、ICMS）下分别展示结果（共2组）。
  - 吞吐量-能量关系对比（图4），包含若干点（B和K变化）。
  - 三指标归一化对比（图5），仅展示B=4时AR-LLM、MDLM、N-MDLM的结果。
- **充分性分析**：
  - 覆盖了关键参数（块大小、稀疏度）的影响，消融实验通过改变K和B间接进行。
  - 但仅针对单一翻译任务（DE-EN），未在更多NLP任务（如文本生成、问答）上验证。
  - 实验中未使用真实神经形态硬件，而是在GPU上模拟脉冲行为，可能高估或低估实际硬件性能。
  - 与MDLM的对比在同一架构（E2D2）下进行，转换后额外微调，保证了比较的公平性。
  - 总体实验设计较为合理，但覆盖范围有限，泛化性有待进一步验证。

## 6. 论文的主要结论与发现
- 在计算受限平台（ICMS）上，标准MDLM几乎无吞吐量提升（因受计算能力限制），而N-MDLM通过脉冲稀疏性显著提升吞吐量和能效。
- 稀疏性降低了算术强度，使系统从计算受限转向内存受限，从而能有效利用块并行性；反之，块并行性也使得稀疏性带来的内存带宽节省更加明显，形成协同效应。
- 能量效率提升主要来源于内存访问的减少（内存访问能耗远高于计算能耗），通过稀疏性进一步降低。
- 与AR-LLM相比，N-MDLM在保持相近BLEU的前提下，实现了更高的吞吐量和更低的能耗。

## 7. 优点
- **方法创新**：首次将块扩散语言模型与神经形态稀疏计算结合，提出N-MDLM，突破了单一技术（MDLM）在计算受限平台上的瓶颈。
- **理论分析**：开发了token级roofline模型，清晰揭示了块大小B和稀疏度σ对吞吐量和计算/内存受限状态的联合影响，为硬件-算法协同优化提供了分析工具。
- **实验验证**：通过模拟两种系统配置（OCMS和ICMS），直观展示了N-MDLM在不同硬件特征下的优势，尤其是解决了“MDLM在计算受限平台无效”的问题。
- **实用价值**：方法基于现有预训练模型转换，仅需少量微调即可适配，无需从头训练，具有部署便利性。

## 8. 不足与局限
- **实验覆盖有限**：仅在单一翻译任务（WMT DE-EN）上验证，未涉及其他语言对或文本理解/生成任务，泛化性不确定。
- **硬件模拟局限性**：所有实验在GPU上通过模拟脉冲行为进行，未在真实神经形态硬件（如Loihi）上测试，实际能效和吞吐量可能受硬件特性影响而有所差异。
- **BLEU轻微下降**：N-MDLM相比原始AR-LLM存在BLEU小幅下降（图5显示下降但不显著），未深入分析质量与效率的权衡边界。
- **参数选择未优化**：文中未提出自适应策略联合优化B和K，仅手动选择若干值，可能未达到帕累托最优。
- **未讨论跨层稀疏性差异**：分析假设各层独立同分布稀疏性，但实际中各层稀疏度可能不同，对整体吞吐量的影响需进一步细化模型。
- **计算资源信息不完整**：未提供模型参数量、推理时的FLOPS、实际运行时间等详细数据，难以完全复现。

（完）
