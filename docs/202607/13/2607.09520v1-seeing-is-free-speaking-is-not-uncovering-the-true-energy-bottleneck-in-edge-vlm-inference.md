---
title: "Seeing is Free, Speaking is Not: Uncovering the True Energy Bottleneck in Edge VLM Inference"
title_zh: 看到是免费的，说话不是：揭示边缘VLM推理的真正能量瓶颈
authors: "Junfei Zhan, Haoxun Shen, Mingang Guo, Zixuan Huang, Tengjiao He"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09520v1"
tags: ["query:edge-llm"]
score: 8.0
evidence: 边缘硬件上VLM推理的能量剖析揭示真实瓶颈
tldr: "边缘设备上VLM推理的能量瓶颈通常被误认为是视觉处理，但本文通过系统性能分析发现：平均推理功率为模型固有常数，不随输入变化；输出token比输入token耗时11-39倍，成为延迟和能量的主导因素；图像复杂度通过影响输出长度间接导致高达4.1倍的能量差异。视觉token剪枝最多节省10%能量，而控制输出长度可节省97%能量，揭示了真正的瓶颈是“说多少”而非“看什么”。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09520v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 374, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09520v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1820, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09520v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 876, \"height\": 382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09520v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1828, \"height\": 503, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09520v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 880, \"height\": 469, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09520v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 887, \"height\": 200, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09520v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 859, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09520v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 916, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09520v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 746, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09520v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1444, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09520v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 493, \"height\": 294, \"label\": \"Table\"}]"
motivation: 现有研究聚焦于减少视觉token以降低能耗，但缺乏对边缘VLM推理能耗瓶颈的系统性理解。
method: 对五种跨架构模型、四种分辨率、两种硬件平台进行片上功耗与时间测量，分析功率、延迟与能量分解。
result: "输出token贡献11-39倍于输入token的延迟；图像复杂度通过输出长度导致4.1倍能量差异；控制输出长度可节省97%能量，而视觉裁剪最多节省10%。"
conclusion: 边缘VLM推理的真正能耗瓶颈在于输出token数量，而非视觉处理；优化应聚焦于控制输出长度。
---

## 摘要
视觉语言模型（VLM）是具身AI的感知支柱，但它们在边缘硬件上的能耗仍鲜为人知。现有的效率研究主要集中在减少视觉标记上，隐含地认为视觉处理是主要的能量消耗。我们通过首次对设备端VLM推理进行系统性能耗分析，涵盖了三种架构系列的五种模型、四种输入分辨率以及两个硬件平台（NVIDIA RTX 3070和Jetson Orin NX），推翻了这一隐含假设。我们的分析得出三个发现。首先，平均推理功率是模型固有的常数，不随输入分辨率、图像复杂度和提示类型而变化，在所有条件下变化幅度小于5%。这意味着输入间的所有能量差异必然源于推理时间的变化，而非功率消耗的变化。其次，由于预填充和解码之间的计算和内存不对称，每个输出标记的挂钟时间比每个输入标记多11到39倍，使得输出标记数量成为延迟和能量的主要驱动因素。第三，图像复杂度（由图像中物体数量衡量）在相同分辨率下导致高达4.1倍的能量差异。这种差异并非源于视觉处理成本的增加，而是源于输出长度的差异。这些发现揭示了视觉标记剪枝的根本局限性：即使去除所有视觉标记，对于固定标记模型，最多只能节省总能量的10%。在参数规模从10亿到80亿的模型中，控制输出长度最多可节省总能量的97%，且随着模型规模的增大，解码的能量主导性更强。简而言之，边缘VLM推理的真正能量瓶颈不在于模型看到了什么，而在于它说了多少。

## Abstract
Vision-Language Models (VLMs) are the perceptual backbone of embodied AI, but their energy footprint on edge hardware remains poorly understood. Existing efficiency efforts focus predominantly on reducing visual tokens, implicitly treating visual processing as the dominant energy cost. We overturn this implicit assumption through the first systematic energy profiling of on-device VLM inference, spanning five models across three architecture families, four input resolutions, and two hardware platforms (NVIDIA RTX 3070 and Jetson Orin NX). Our analysis yields three findings. First, average inference power is a model-intrinsic constant, invariant to input resolution, image complexity, and prompt type, with less than 5% variation across all conditions. This means that all energy variation across inputs must arise from variation in inference time, not from variation in power draw. Second, each output token costs 11 to 39x more wall-clock time than each input token due to the compute-bound and memory-bound asymmetry between prefill and decode, making output token count the dominant driver of both latency and energy. Third, image complexity, measured by the number of objects in an image, induces up to 4.1x energy differences at identical resolution. This variation arises not from increased visual processing cost, but from differences in output length. These findings expose a fundamental limitation of visual token pruning: even removing all visual tokens saves at most 10% of total energy for fixed-token models. Across models spanning 1 billion to 8 billion parameters, controlling output length saves up to 97% of total energy, with the energy dominance of decoding growing stronger at larger model scale. In short, the true energy bottleneck in edge VLM inference is not what the model sees, but how much it says.

---

## 论文详细总结（自动生成）

# 论文总结：Seeing is Free, Speaking is Not: Uncovering the True Energy Bottleneck in Edge VLM Inference

## 1. 论文的核心问题与整体含义

- **研究动机与背景**：视觉语言模型（VLM）是具身AI（如机器人操控、无人机巡检）的感知核心，常需部署在边缘设备（如Jetson模块、移动SoC）上。然而，边缘设备的电池容量有限（40–100 Wh），而一次VLM查询可能消耗50–500 J，因此能量效率是影响连续运行的**关键约束**。现有的大多数效率研究（如视觉token剪枝）默认视觉处理是主要能耗来源，但这一假设从未被系统验证。
- **核心问题**：边缘VLM推理中真正的能耗瓶颈是什么？是视觉token的“看到”还是输出token的“说话”？
- **整体含义**：论文通过首个系统性能耗剖析，推翻了“视觉处理为主”的假设，揭示**输出token数量是能量消耗的主导因素**，并提出控制输出长度比优化输入更有效的节能策略。标题“Seeing is Free, Speaking is Not”精炼地概括了这一反直觉发现。

## 2. 论文提出的方法论

- **核心思想**：通过细粒度功率与时间测量，将VLM推理能量分解为预填充（Prefill）和解码（Decode）两个阶段，并利用**平均功率为模型固有常数**的特性，将能量问题简化为时间问题，进而定向到输出token数量。
- **关键技术细节**：
  - **硬件与工具**：使用NVIDIA RTX 3070 Laptop GPU（8GB VRAM，频率锁定1500MHz）和NVIDIA Jetson Orin NX（15W模式，锁定时钟）。功率采样通过HWiNFO64（总系统功耗）和板载INA3221传感器（VDD_IN rail）实现，间隔100ms。
  - **模型推理**：通过llama.cpp的llama-server运行所有模型，使用贪心解码（T=0），量化等级统一（如Q8_0、Q4_K_M等）。
  - **控制变量**：系统改变三个因素——输入分辨率（224²/448²/672²/896²）、图像内容（从1个物体到16个物体分级）、任务提示词（开放式描述 vs 简答）。
- **公式与流程**：
  - 能量分解：\( E = \bar{P} \times t\)，其中\(\bar{P}\)为模型不变的平均功率（模型指纹）。
  - 推理时间分解：\( t = t_{\text{prefill}}(N_{\text{in}}) + t_{\text{decode}}(N_{\text{out}}) \)。
  - 线性延迟模型：\( t_{\text{wall}} \approx \alpha_p N_{\text{in}} + \alpha_d N_{\text{out}} + \beta \)，实测\(\alpha_d / \alpha_p = 11\)–\(39\times\)。
  - 跨模型通用能量预测器：\( \hat{E} = w_0 + w_1 S + w_2 N_{\text{in}} + w_3 N_{\text{out}} + w_4 S \cdot N_{\text{in}} + w_5 S \cdot N_{\text{out}} \)，其中\(S\)为模型参数量（B）。该模型无需逐模型校准即可预测能量，R²=0.986。

## 3. 实验设计

- **数据集与场景**：使用COCO 2017验证集图像，并按标注物体数分为6个复杂度层级（T1: 单物体 → T6: 12–16物体）。测试场景包括：
  - 多分辨率输入（四种分辨率）
  - 多提示词（“Describe this image” vs “What is the main object? Answer in one word”）
  - 多硬件平台（RTX 3070笔记本电脑GPU、Jetson Orin NX）
- **基准/对比**：由于本文是首个系统性能耗测量，没有横向对比现有效率方法的实际Joule数。但通过理论分析对比了视觉token剪枝的上界节能（最多10%）与输出长度控制的实际节能（最高97%）。
- **覆盖模型**：5种模型（InternVL3-1B/2B, Qwen2-VL-2B, Qwen2.5-VL-3B, Gemma-3-4B），并验证扩展到7B/8B模型（Qwen2.5-VL-7B, InternVL3-8B）。

## 4. 资源与算力

- 文中明确使用了**两块边缘硬件**：
  - NVIDIA RTX 3070 Laptop GPU（8GB VRAM，频率锁定1500MHz）
  - NVIDIA Jetson Orin NX（15W功率模式，时钟通过jetson_clocks锁定）
- **未明确说明**：总训练/测试时长、GPU数量、电力消耗总量等。仅提及在RTX 3070上共运行了1680次推理（5模型×4分辨率×12次，每模型至少240次），加上Jetson上的多轮对比实验（内容复杂度、提示词等）。这些推理均属**推理阶段**，不涉及训练。因此算力需求相对较低，但实验覆盖充分。

## 5. 实验数量与充分性

- **实验组数**：
  - RTX 3070：5模型×4分辨率×12次运行 = 约1680次推理（不含热身丢弃的前2次）
  - Jetson Orin NX：2模型（InternVL3-1B, Qwen2.5-VL-3B）×6内容复杂度层级×多次重复，外加提示词对比、分辨率对比等，总计数百次。
  - 交叉验证：留一个分辨率交叉验证（MAPE 9.7–12.0%）。
- **充分性**：实验设计系统严密——**控制变量**（分辨率、内容、提示词）、**硬件锁定**（频率、时钟）、**标准解码配置**（贪心）、**多模型多系列**（固定token vs 动态token）。视觉剪枝上限推导与输出长度控制对比提供了理论支持，能量预测模型在校准集与交叉验证集上表现一致。数据丰富且客观。
- **可能的公平性不足**：仅使用两种特定硬件（NVIDIA GPU），未覆盖CPU、NPU、移动SoC（如高通、联发科）；量化等级虽统一但不同模型不同（Q8_0 vs Q4_K_M），可能影响对比，但论文认为这反映了实际部署选择。

## 6. 论文的主要结论与发现

1. **平均功率是模型固有常数**：不随输入分辨率、图像复杂度、提示词变化（CV < 5%），由模型参数量线性决定（每增加1B参数约增加12W）。
2. **输出token成本远高于输入token**：每个输出token的挂钟时间比每个输入token高11–39倍，导致解码阶段占86–97%的总能量。
3. **图像复杂度间接影响能量**：通过驱动输出长度变化（更复杂的图像触发更长描述），在相同分辨率下可导致**4.1×能量差异**（104–428J）。
4. **视觉token剪枝的节能上限极低**：即使去除所有视觉token，固定token模型最多节省10%能量；而控制输出长度最多可节省**97%** 能量（如用简答提示词替代详细描述）。
5. **跨模型能量预测可行**：仅用模型大小、输入/输出token数及其交互项的线性模型即可解释98.6%的能量方差，无需逐模型校准。
6. **部署建议**：优先预算输出（降低max_tokens）；动态token架构在高分辨率下可能有“分辨率陷阱”（预填充成本激增）；内容驱动的能量变化需用最大输出长度做最坏情况预算。

## 7. 优点

- **首次系统性测量**：填补了边缘VLM能耗剖析的空白，方法可复现且结果稳健。
- **简洁的理论简化**：通过发现功率恒定，将复杂能量问题转化为时间问题，并锁定到输出token数量这一单一主导因素。
- **实践指导性强**：提出视觉剪枝的节能上限（≤10%）和输出长度控制的巨大潜力（≤97%），直接扭转社区优化方向。
- **跨模型通用预测器**：无需逐模型调参即可预测任意配置的能量，便于调度和电池预算。
- **实验变量控制严格**：使用贪心解码、锁定GPU频率、多个维度的消融，确保了结论的可靠性。

## 8. 不足与局限

- **硬件覆盖有限**：仅测试了NVIDIA RTX 3070和Jetson Orin NX两种GPU，未覆盖CPU、NPU、移动SoC（如Apple M系列、高通Snapdragon），结论的泛化性需更多平台验证。
- **测量精度**：功率采样间隔100ms，对于毫秒级事件（如切换阶段）可能不够细，但推理运行时间通常数秒至数十秒，影响可接受。
- **模型范围**：主要聚焦1B–4B模型，对7B/8B模型仅进行了辅助验证（表5），更大型模型（如13B+）或不同架构（如MoE）未涉及。
- **量化与解码策略**：统一使用特定量化等级（不同模型不同）和贪心解码，未探索其他量化级别或采样温度对能耗的影响。
- **未对比实际剪枝方法**：仅理论上界分析视觉剪枝上限，没有直接运行现有剪枝方法（如FastV、PyramidDrop）并测量实际能量，可能削弱实用性说服力。
- **输出长度控制的代价**：简单缩短输出可能牺牲任务质量（如详细描述变为简短回答），论文未量化质量-能量权衡，只展示了节能潜力。

（完）
