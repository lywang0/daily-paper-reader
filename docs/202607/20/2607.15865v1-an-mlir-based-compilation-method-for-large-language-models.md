---
title: An MLIR-Based Compilation Method for Large Language Models
title_zh: 基于MLIR的大语言模型编译方法
authors: "Pengchao Hu, Zhibin Xin, Yifan Chen, Yangyang Zhou, Liang Wang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15865v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 基于MLIR的LLM硬件部署编译方法
tldr: 大语言模型在专用硬件部署面临模型导入和推理调度两大挑战。本文提出基于MLIR的编译方法，通过高层方言TopOp表达模型语义，经逐层lower到硬件方言TpuOp以融合量化、分组等优化。将Transformer推理分为prefill等三阶段静态编译，适配并行与自回归计算差异。该方法在TPU-MLIR编译器中实现，支持Qwen、Llama等多系列模型及GPTQ等量化格式，有效提升部署效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15865v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 664, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1493, \"height\": 1230, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1375, \"height\": 1003, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 502, \"height\": 927, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15865v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1810, \"height\": 616, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15865v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1810, \"height\": 757, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15865v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1373, \"height\": 284, \"label\": \"Table\"}]"
motivation: 现有LLM部署方法难以平衡框架无关表示与硬件特定优化，且自回归推理存在内存和调度瓶颈。
method: 设计MLIR双方言架构：TopOp表示框架无关模型图，TpuOp承载硬件决策；按prefill、prefill_kv、decode三阶段分层编译。
result: 实现于TPU-MLIR和LLM-TPU，支持Qwen、Llama、InternVL等模型及GPTQ、AWQ等多种量化，生成部署二进制。
conclusion: 提出的MLIR编译方法统一了LLM从模型导入到推理调度的流程，降低了专用硬件的部署复杂度。
---

## 摘要
大语言模型已成为现代AI加速器上的主要工作负载，但在专用硬件上部署仍面临两个核心挑战：如何将训练好的模型导入编译器友好的中间表示，以及如何在有限的片上内存下高效调度自回归推理循环。本文提出一种基于MLIR（多层次中间表示）的大语言模型编译方法，通过两个算子方言TopOp和TpuOp进行说明。TopOp作为高级图方言，独立于源框架和目标芯片，负责表达模型语义；TpuOp作为目标硬件方言，承载与芯片相关的决策，如量化、层组和内存布局。模型首先表示为TopOp，然后逐层降级到TpuOp，最后生成可部署的二进制文件。此外，每个Transformer层被拆分为三个阶段进行静态编译：prefill、prefill_kv（带历史键值缓存的prefill）和decode，以适应提示并行处理和逐个token生成的不同计算特性。该方法已在TPU-MLIR编译器（https://github.com/sophgo/tpu-mlir）和LLM-TPU部署项目（https://github.com/sophgo/LLM-TPU）中实现，支持包括Qwen、Llama、InternVL和MiniCPM-V系列在内的多种生成模型，以及GPTQ、AWQ和AutoRound等多种量化和部署形式。

## Abstract
Large Language Models (LLMs) have become the dominant workload on modern AI accelerators, yet deploying them on specialized hardware still faces two core challenges: how to import a trained model into a compiler-friendly intermediate representation, and how to efficiently schedule the autoregressive inference loop under limited on-chip memory. This paper presents an MLIR (Multi-Level Intermediate Representation) based compilation method for large language models, illustrated using two dialects of operators, TopOp and TpuOp. TopOp serves as a high-level graph dialect that is independent of both the source framework and the target chip, and is responsible for expressing model semantics; TpuOp serves as the target hardware dialect, carrying chip-related decisions such as quantization, layer groups, and memory layout. A model is first represented as TopOp, then lowered layer by layer to TpuOp, and finally a deployable binary is generated. In addition, each Transformer layer is split into three stages for static compilation: prefill, prefill_kv (prefill with historical key-value cache), and decode, so as to accommodate the different computational characteristics of prompt-parallel processing and per-token generation. The method has been implemented in the TPU-MLIR compiler{https://github.com/sophgo/tpu-mlir} and the LLM-TPU deployment project\footnote{https://github.com/sophgo/LLM-TPU}, supporting a variety of generative models including the Qwen, Llama, InternVL, and MiniCPM-V series, as well as multiple quantization and deployment forms such as GPTQ, AWQ, and AutoRound.

---

## 论文详细总结（自动生成）

# 基于MLIR的大语言模型编译方法论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）在专用AI加速器上部署面临两大挑战：(1) 如何将训练好的模型（来自PyTorch/HuggingFace等框架）导入到编译器友好的中间表示；(2) 如何在有限的片上内存下高效调度自回归推理循环（prompt并行处理 vs. 逐个token生成）。
- **整体含义**：提出一种基于MLIR（Multi-Level Intermediate Representation）的编译方法，通过定义高层方言TopOp和硬件方言TpuOp，实现从模型导入、量化、层组划分到二进制生成的自动化流程；同时将每个Transformer层静态编译为prefill、prefill_kv、decode三个变体，以适配不同计算特征。
- **背景**：传统手工编写算子库成本高、难以跟上模型演进；MLIR提供可复用、可扩展的编译器基础设施，允许自定义方言表达不同抽象级别的计算。

## 2. 论文提出的方法论

### 核心思想
- 使用**两层方言**：TopOp（高层图方言，框架无关、芯片无关）表达模型语义；TpuOp（目标硬件方言）携带量化、内存布局、层组、地址等芯片相关属性。
- 编译流程：模型 → TopOp MLIR → 逐层降级（lowering）到 TpuOp → 执行层组切片、内存分配、代码生成 → 输出每个模块的二进制文件，最后合并为部署二进制。
- **三阶段静态编译**：将每个Transformer层拆分为三个独立编译的TopOp模块：
  - **Prefill**：处理输入提示的所有token（并行），无历史KV。
  - **Prefill_kv**：有历史KV的prefill（用于多轮对话或长提示分段）。
  - **Decode**：每次仅处理1个新token，需与所有历史KV计算注意力。
- 三阶段共享相同的数据流模板，仅通过`with_history`标志区分。

### 关键技术细节
- **TopOp示例算子**：MatMul、RMSNorm、Gather、Rope、FAttention、MLP等。其中MLP和FAttention保持为融合高层算子，便于后续芯片特定优化。
- **TpuOp附加属性**：数据类型（F32/BF16/F16/INT8）、量化格式（对称/非对称INT8、AWQ/GPTQ/AutoRound直通编译）、层组信息、拆分信息、存储偏移等。
- **性能优化**：
  - 因果掩码：使用固定小掩码（如128×128）复用，减少带宽。
  - 位置编码：预计算cos/sin表，运行时通过Gather取用，避免运行时计算。
  - KV缓存管理：通过地址重用或就地更新实现Concat，避免物理拷贝；根据上下文长度选择不同decode变体。
- **模块化编译**：按层和阶段拆分为多个小模块，可并行编译，运行时按序调用。

### 算法流程（文字描述）
1. 加载HuggingFace模型配置和权重分片，根据模型系列分发到对应转换器。
2. 生成每个Transformer层的TopOp模块（包含prefill、prefill_kv、decode三个变体）。
3. 对每个模块执行lowering（TopOp→TpuOp），期间进行量化校准、层组规划、内存地址分配。
4. 对每个TpuOp模块生成目标二进制（codegen）。
5. 合并所有模块二进制为最终部署文件。
6. 运行时：嵌入层→prefill/prefill_kv（每层）→LM head→采样→decode循环。

## 3. 实验设计

- **论文中无传统实验部分**（无数据集、无benchmark、无对比方法）。
- 文中仅说明该方法已在**TPU-MLIR编译器**和**LLM-TPU部署项目**中实现，支持模型包括Qwen、Llama、InternVL、MiniCPM-V系列，以及GPTQ、AWQ、AutoRound等多种量化格式。
- **验证方式**：通过实际项目（开放源代码）展示可行性，但未提供定量性能指标（如推理速度、内存占用、精度损失等）。

## 4. 资源与算力

- **文中未提及**使用的GPU型号、数量、训练时长或推理硬件细节。仅提到面向“专用AI加速器”，但未具体说明是哪款芯片或集群配置。

## 5. 实验数量与充分性

- **无传统实验组**。论文完全聚焦于方法论描述和设计决策，未提供任何消融实验、对比实验或统计分析。
- **充分性**：从方法描述角度，架构设计合理，代码开源，可复现；但缺乏定量结果，无法评估该方法相对于其他编译方案（如TVM、XLA、Triton等）的优劣。
- **客观性公平性**：无可评估对象，因此无法判断。

## 6. 论文的主要结论与发现

- 提出基于MLIR的LLM编译方法：通过TopOp+TpuOp两层方言，实现了框架无关的模型导入与硬件特定的代码生成。
- 三阶段静态编译（prefill/prefill_kv/decode）有效解决了自回归推理中prompt并行与逐token生成的计算差异，避免了动态编译开销。
- 该设计保持了编译器对Transformer系列模型的通用性（密集/分组查询/MoE/多模态），同时为运行时提供了专用函数。
- 实践证明该方法在多个模型系列和量化格式上可行，已在TPU-MLIR和LLM-TPU中落地。

## 7. 优点

- **框架无关性**：TopOp不依赖PyTorch或HuggingFace，可统一处理不同来源模型。
- **模块化编译**：按层和阶段拆分，支持并行编译，便于运行时灵活组合（如长提示分段）。
- **静态形状编译**：每个变体具有静态形状，硬件指令简单，内存分配确定，减少动态开销。
- **性能优化策略**：如因果掩码复用、预计算cos/sin表、就地KV更新等，针对专用硬件的特点设计。
- **直接支持多种量化格式**（GPTQ/AWQ/AutoRound），无需额外转换。
- **可扩展性**：可扩展到推测性解码、更长上下文、多模态模型。

## 8. 不足与局限

- **缺乏定量评估**：未提供任何数值结果（如延迟、吞吐量、精度），无法衡量方法实际效率。
- **依赖静态编译**：KV缓存管理需预先分配固定最大长度（如8K），长序列场景需分段，不如vLLM的PagedAttention灵活。
- **未讨论泛化性**：仅列举了支持的模型系列，未说明是否适用于所有Transformer变体；未考虑训练/微调场景。
- **实验验证不充分**：无对比方法（如直接使用PyTorch推理、TVM、XLA等），无法证明该方法的优越性。
- **文档局限**：部分优化细节（如解码chunk列表的具体生成方式）未深入阐述，可能需查阅代码。
- **应用限制**：面向特定专用硬件（Sophgo TPU），通用性受限；如果其他硬件架构差异大，lowering和优化策略需重新调整。

（完）
