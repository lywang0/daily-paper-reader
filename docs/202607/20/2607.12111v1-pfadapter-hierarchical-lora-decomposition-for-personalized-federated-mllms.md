---
title: "PFAdapter: Hierarchical LoRA Decomposition for Personalized Federated MLLMs"
title_zh: "PFAdapter: 用于个性化联邦多模态大语言模型的分层LoRA分解"
authors: "Jing Liu, Kun Yang, Yan Wang, Dingkang Yang, Xiaoshuai Hao, Wei Zhang, Yang Liu, Wei Zhou"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.12111v1"
tags: ["query:edge-llm"]
score: 8.0
evidence: 面向边缘设备的多模态大模型个性化联邦微调
tldr: "联邦多模态大模型面临全局知识与本地适应的平衡难题和通信开销大的挑战。本文提出PFAdapter，通过分层LoRA分解显式分离全局共享和局部私有参数，查询/键投影全局同步，值/输出投影本地保留，并引入Frobenius范数正交正则化防止冗余。选择性聚合仅同步全局组件，在多个数据集上准确率提升2.4%-4.8%，通信成本降低近50%，为资源受限的边缘网络提供了高效的个性化联邦学习方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12111v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 923, \"height\": 271, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12111v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1865, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12111v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 916, \"height\": 576, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12111v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 1441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12111v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1863, \"height\": 358, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12111v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 911, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12111v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 906, \"height\": 234, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12111v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 920, \"height\": 212, \"label\": \"Table\"}]"
motivation: 联邦微调MLLMs时，均匀参数聚合混淆全局与局部特征，导致个性化不足且通信开销大。
method: 分层LoRA分解将参数分为全局共享（Q/K）和局部私有（V/O），加Frobenius范数正交正则化，仅同步全局组件。
result: "在VQA-RAD、SLAKE等数据集上准确率提升2.4%-4.8%，通信成本降低近50%。"
conclusion: 提出通信高效的个性化联邦MLLM框架，适用于资源受限的边缘网络。
---

## 摘要
代理型人工智能系统通过部署能够协作学习并同时在网络边缘维护数据隐私的自主智能体，正在重塑通信与网络。在分布式网络环境中，多模态大语言模型作为边缘设备的认知引擎，然而联邦微调在异构网络条件下平衡全局知识聚合与本地适应面临着巨大挑战。传统的联邦协议通常依赖统一参数聚合，将域不变特征与客户端特定细节混淆，导致个性化效果欠佳且通信开销过大。为应对这些挑战，我们提出PFAdapter，一种通信高效的框架，引入分层LoRA分解，将适配器参数明确分为全局共享和本地私有两部分。查询和键投影被分配用于全局同步，以捕获跨网络的通用多模态语义，而值和输出投影则保留在本地用于边缘特定适应。此外，基于Frobenius范数的正交正则化强制这些组件之间严格分离，防止冗余特征学习。选择性聚合协议仅同步联邦网络中的全局共享组件，保留本地专业知识，并将通信成本降低近50%。在VQA-RAD、SLAKE、Hateful Memes和CrisisMMD数据集上的大量实验表明，PFAdapter始终优于最先进的基线方法，在不同边缘智能任务中实现了2.4%至4.8%的准确率提升。因此，我们的框架为资源受限的通信网络中的代理型AI部署提供了高效的解决方案。

## Abstract
Agentic AI systems are reshaping communications and networking by deploying autonomous intelligent agents capable of collaborative learning while maintaining data privacy at network edges. Within distributed network environments, Multimodal Large Language Models (MLLMs) serve as cognitive engines for edge devices, yet federated fine-tuning faces substantial challenges in balancing global knowledge aggregation with local adaptation under heterogeneous network conditions. Conventional federated protocols typically rely on uniform parameter aggregation, which conflates domain-invariant features with client-specific nuances, thereby resulting in suboptimal personalization and excessive communication overhead. To address these challenges, we propose PFAdapter, a communication-efficient framework introducing hierarchical LoRA decomposition to explicitly separate adapter parameters into global-shared and local-private components. Query and key projections are assigned to global synchronization for capturing universal multimodal semantics across the network, while value and output projections remain localized for edge-specific adaptation. Additionally, orthogonality regularization based on the Frobenius norm enforces strict separation between these components, preventing redundant feature learning. Selective aggregation protocols synchronize only global-shared components across the federated network, preserving local expertise and reducing communication costs by nearly 50%. Extensive experiments on VQA-RAD, SLAKE, Hateful Memes, and CrisisMMD datasets demonstrate that PFAdapter consistently outperforms state-of-the-art baselines, achieving accuracy improvements ranging from 2.4% to 4.8% across diverse edge intelligence tasks. Consequently, our framework establishes an efficient solution for agentic AI deployment in resource-constrained communication networks.

---

## 论文详细总结（自动生成）

# PFAdapter: 用于个性化联邦多模态大语言模型的分层LoRA分解 — 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在边缘网络和分布式通信系统中，部署自主智能代理（Agentic AI）需要利用多模态大语言模型（MLLM）作为认知引擎。联邦学习（FL）能够在不集中原始数据的情况下进行协作训练，但传统FL方法（如FedAvg）对所有模型参数进行**均匀聚合**，导致两个关键问题：  
  1. **个性化不足**：全局平均会稀释客户端特定的知识（weight washing），难以适应异构数据分布。  
  2. **通信开销大**：即使采用LoRA等参数高效微调（PEFT），传输全部适配器参数仍消耗大量带宽（在带宽受限的边缘网络中不可接受）。  
- **整体含义**：提出一种**通信高效的个性化联邦MLLM微调框架**，通过显式分离全局共享和本地私有的适配器参数，在保持全局语义共享的同时保留边缘专业知识，并降低一半的通信成本，为资源受限的Agentic AI部署提供可行路径。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **分层LoRA分解**：基于自注意力机制中不同投影模块的功能角色，将LoRA适配器参数分为两组：  
  - **全局共享（Global-shared）**：Query（查询）和Key（键）投影 → 负责捕获跨客户端通用的多模态语义关系，经联邦聚合同步。  
  - **本地私有（Local-private）**：Value（值）和Output（输出）投影 → 编码客户特定的语义细节，留在本地不参与聚合。  
- **正交正则化**：基于Frobenius范数惩罚全局与本地参数矩阵之间的内积，强制两者子空间正交，防止冗余特征学习。  
- **选择性聚合**：每轮通信仅传输全局共享参数（Q/K LoRA），通信量降低约50%。

### 关键技术细节
1. **参数分解**：  
   - 全局参数集 \(\Theta_G = \bigcup_{l=1}^L \{A_{l,m}, B_{l,m} \mid m \in \{q, k\}\}\)  
   - 本地参数集 \(\Theta_L = \bigcup_{l=1}^L \{A_{l,m}, B_{l,m} \mid m \in \{v, o\}\}\)  
2. **前向传播**（以自注意力层为例）：  
   \[
   \text{Attn}(x) = \text{Softmax}\left( \frac{(W_q + \Delta W_q^G) x \cdot ((W_k + \Delta W_k^G) x)^\top}{\sqrt{d_k}} \right) \times (W_v + \Delta W_v^L) x
   \]  
3. **正交损失**：  
   \[
   L_o = \sum_{l} \sum_{m_g \in \{q,k\}} \sum_{m_l \in \{v,o\}} \| (W_{l,m_g})^\top W_{l,m_l} \|_F^2
   \]  
4. **总损失**：  
   \[
   L_{\text{total}} = L_t(D_k; \Theta_G, \Theta_{L_k}) + \lambda_o L_o
   \]  
5. **聚合规则**：  
   \[
   \Theta_G^{t+1} = \sum_{k \in S_t} \frac{n_k}{\sum_j n_j} \Theta_{G,k}^{t+1}
   \]  
   本地参数 \(\Theta_{L_k}\) 永不上传。

## 3. 实验设计

### 数据集与场景
| 数据集 | 领域 | 规模 | 任务类型 |
|--------|------|------|----------|
| VQA-RAD | 医学影像 | 3,515 QA对 / 315张图像 | 医疗视觉问答 |
| SLAKE | 医学影像（双语） | 14,028样本 / 642张图像 | 医疗视觉问答 |
| Hateful Memes | 社交媒体 | 10,000多模态样本 | 仇恨言论检测（二分类） |
| CrisisMMD | 灾害场景 | 16,080图像-文本对 | 辅助任务（损伤评估/需求识别） |

- 所有实验采用**对齐模态场景**，Dirichlet浓度参数 \(\alpha = 0.5\) 模拟中等数据异构。

### 对比方法与基准
- **零样本（Zero-shot）**：预训练模型直接推理（下界）。  
- **本地训练（Local-only）**：每个客户端独立训练（上界，无知识共享）。  
- **联邦基线**：FedYogi、FedAdam、FedAvgM、FedAdagrad（均为标准联邦优化器）。  
- **个性化联邦LoRA方法**：pFedLoRA。  
- **PFAdapter（本文）**：使用所提框架。

### 评价指标
- 分类任务：Accuracy、F1-score；Hateful Memes额外报告AUC。

## 4. 资源与算力

- **硬件**：单块 **Nvidia L60 GPU**（48GB显存）。  
- **模型**：**MiniCPM-V-2_6-int4**（4-bit量化），Qwen2骨干网络。  
- **LoRA配置**：rank \(r=8\)，缩放因子 \(\alpha_{\text{LoRA}}=16\)，仅微调自注意力投影矩阵。  
- **训练参数**：  
  - 优化器：AdamW（学习率 \(2\times 10^{-5}\)，余弦退火）  
  - 通信轮数：50  
  - 客户端总数：10，每轮采样2个  
  - 每个客户端本地训练1 epoch，batch size=1，梯度累积16步  
  - 正交正则化权重 \(\lambda_o = 0.1\)  
- **时间与通信**（表IV）：  
  - 每轮训练时间：PFAdapter约10.8分钟 vs FedYogi约10.3分钟（仅增加5%）。  
  - 总通信量（50轮）：PFAdapter 15.75 GB vs FedYogi 30.85 GB（降低48.9%）。

## 5. 实验数量与充分性

论文进行了**多组系统实验**，覆盖以下方面：  
1. **主实验结果**：在4个数据集上对比5种以上基线，报告平均值和标准差（3次种子）。  
2. **消融实验**：分别移除分层分解、正交损失、选择性聚合，验证每个组件的贡献（表II）。  
3. **分解策略分析**：测试多种Q/K/V/O的全局-本地分配方案（表III），证明Q/K全局 + V/O局部最优。  
4. **参数敏感性**：考察正交权重\(\lambda_o\)、全局-本地比例、数据异构程度\(\alpha\)的影响（图3等）。  
5. **冷启动能力**：新加入客户端零样本及少量本地微调后的性能（图3d）。  
6. **收敛分析**：准确率与训练损失随轮次变化（图3a,b）。  
7. **计算与通信效率**：详细对比训练时间、GPU小时、峰值显存、总通信量（表IV）。  
8. **多种异构场景**：缺失模态、文本/图像主导、混合噪声等鲁棒性测试（图3等）。

- **充分性评估**：实验设计较为全面，覆盖了主要性能指标、消融、参数敏感性和实际部署场景。对比公平，所有基线使用相同LoRA设置和计算资源。但论文**未提供客户端数量扩展实验**（如50或100个客户端），且**未进行与其他个性化FL方法（如FedPer、FedRep）的直接比较**（仅在文中提及概念对比）。

## 6. 论文的主要结论与发现

- PFAdapter在所有四个数据集上**一致优于**现有联邦微调方法，准确率提升**2.4%–4.8%**，同时通信开销**降低近50%**。  
- **正交正则化**有效分离全局与本地知识，移除后性能下降1.5%左右（表II）。  
- **选择性聚合**（仅同步Q/K）是通信效率和性能的关键平衡点，若移除则性能下降且通信翻倍。  
- 在**高异构场景**（\(\alpha=0.1\)）下优势更显著（比FedYogi高3.4%），在近IID场景（\(\alpha=5.0\)）优势缩小至0.9%，说明分解策略对非IID分布尤其有效。  
- PFAdapter的收敛速度更快，训练损失更低，表明其更高效地利用了参数预算。  
- 该方法具有**良好的鲁棒性**：在缺失模态、噪声干扰等挑战下仍保持领先性能。

## 7. 优点与方法亮点

1. **理论驱动的分解**：基于注意力机制中Q/K决定注意力模式（可迁移）、V/O决定语义内容（任务特定）的功能差异，设计全局-局部分割，具有可解释性。  
2. **知识解耦**：通过正交正则化显式减少冗余，避免本地适配器重复学习全局特征，使参数利用更高效。  
3. **通信效率最大化**：仅传输一半的LoRA参数，且几乎不影响性能，在带宽受限的边缘网络中实用价值高。  
4. **工程友好**：与标准联邦训练协议兼容，额外计算开销小（仅增加正交损失计算，训练时间仅增加5%）。  
5. **全面的实验验证**：在医疗、社交、灾害等多个真实场景数据集上进行了系统性评估，覆盖多种异构条件，结果稳健。

## 8. 不足与局限

- **分解策略普适性**：论文假设Q/K全局最优，但该结论可能依赖于模型结构与任务。对于其他模态或不同注意力变体，最优分割可能不同，需要自适应调整。  
- **层内统一分解**：所有Transformer层采用相同的全局-局部比例（50:50），未考虑层间差异。某些层（如底层）可能更适合更多全局共享，高层更适合本地，引入层自适应分割可能进一步提升性能。  
- **非线性依赖**：Frobenius范数正交约束仅捕捉线性相关性，无法完全建模复杂的非线性参数交互，可能限制更精细的知识解耦。  
- **隐私假设局限**：论文明确声明隐私保护仅限于“去中心化训练不集中原始数据”，未考虑梯度反演、成员推断等攻击。对于高隐私要求的应用场景，需要额外的差分隐私等机制。  
- **实验覆盖不足**：  
  - 未与经典的**层级别个性化方法**（如FedPer、FedRep）进行直接数值对比（仅在文中定性讨论）。  
  - 客户端规模仅10个，缺少更大规模集群的验证。  
  - 未报告消融实验的标准差，结果的统计显著性说明不足。  
- **冷启动场景分析有限**：对于全新客户端，仅展示了0轮和5轮微调的性能，未探讨更复杂的动态加入（如客户端不断离开/加入）的情况。  

（完）
