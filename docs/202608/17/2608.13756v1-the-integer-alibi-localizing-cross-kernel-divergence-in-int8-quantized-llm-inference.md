---
title: "The Integer Alibi: Localizing Cross-Kernel Divergence in INT8-Quantized LLM Inference"
title_zh: 整数不在场证明：定位INT8量化LLM推理中的跨内核分歧
authors: Teng-Ruei Chen
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13756v1"
tags: ["query:edge-llm"]
score: 6.0
evidence: 分析vLLM中INT8量化LLM推理内核差异（CUTLASS与Triton），与硬件感知算子加速相关
tldr: 相同INT8 GEMM接口的不同GPU内核（CUTLASS与Triton）在vLLM推理中产生端到端分歧，但INT32累加器因无溢出而精确，差异被定位到缩放与舍入环节。通过探针检查点恢复逐位一致，并确认INT8分歧始终在单个bfloat16间隔内，与FP8随归约深度增长的模式不同。该研究为内核可互换性提供了可执行的验证流程。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13756v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 879, \"height\": 449, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13756v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 901, \"height\": 507, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13756v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 876, \"height\": 456, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13756v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 1008, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13756v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13756v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 705, \"height\": 292, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13756v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1759, \"height\": 716, \"label\": \"Table\"}]"
motivation: 相同接口的INT8 GEMM内核常被默认可互换，但实证显示仅替换内核即导致全部序列不一致，需查明分歧根源。
method: 固定checkpoint与配置，在vLLM中仅交换CUTLASS/Triton内核，利用无溢出INT32累加器的精确性，逐层对比两个内核的中间输出，定位scale应用与舍入。
result: 逐层验证196/196与252/252层在幂次缩放下逐位一致，真实缩放下差异≤1个bfloat16间隔；探针恢复端到端一致，TPR预测AUC 0.94。
conclusion: 分歧源于缩放与舍入而非累加器，INT8内核可互换性可由提出的合规流程保证，并发布预测与证据。
---

## 摘要
实现相同缩放INT8 GEMM接口的两个GPU内核通常被视为可互换的。我们检验了这一假设：在保持检查点、提示词、硬件、推理引擎、解码和量化配置不变的情况下，我们仅交换vLLM内部的INT8线性内核（CUTLASS与Triton）。在1.7B规模下，每个分支在冷重启后都能逐位复现自身，但在我们运行的所有端到端比较中，两个分支没有在任何序列上达成一致（0/8、0/16和0/64）。使其超越基准差异的是整数不在场证明：对于共享的INT8操作数，在验证过的无溢出边界下，INT32点积是精确且与顺序无关的，因此累加器不可能是任何差异的来源。将Qwen3-1.7B和8B（196层和252层）每个线性层的相同操作数馈送给两个内核，我们在2的幂次缩放下发现逐位相同的输出，确认了固定预测列表196/196和252/252（在1.7B时预注册，在8B时固定但非盲），并在检查点的实际缩放下观察到最多一个bfloat16间隔的差异。这将分歧定位到精确累加器之后的缩放应用和输出舍入。作为探针检查点应用时，相同的干预恢复了端到端的逐位一致性（8/8和16/16序列）。跨实现FP8 GEMM显示出不同的特征：差异的普遍性和幅度都随归约深度增加而增长，而INT8部分保持在百万分之一以内，并在64倍的K范围内保持在一个间隔内。教师强制重放将层与token联系起来：翻转集中在较小的logit边际上，这可以在16,384个位置上以ROC-AUC 0.94预测翻转风险。我们将发布预注册、逐层预测、包含内核选择证据的清单，以及一个将这些控制转化为内核可互换性具体检查的符合性程序。

## Abstract
Two GPU kernels implementing the same scaled INT8 GEMM interface are usually treated as interchangeable. We test that assumption: holding the checkpoint, prompts, hardware, inference engine, decoding, and quantization configuration fixed, we swap only the INT8 linear kernel (CUTLASS versus Triton) inside vLLM. At 1.7B each arm reproduces itself bit-for-bit across cold restarts, yet the arms agree on no sequence in any end-to-end comparison we ran (0/8, 0/16, and 0/64). What makes this more than a benchmark discrepancy is an integer alibi: for shared INT8 operands under a verified no-overflow bound, the INT32 dot product is exact and order-independent, so the accumulator cannot be the source of any difference. Feeding both kernels identical operands from every linear layer of Qwen3-1.7B and 8B (196 and 252 layers), we find bit-identical outputs under power-of-two scales, confirming a pinned prediction list 196/196 and 252/252 (pre-registered at 1.7B, pinned but not blind at 8B), and observed differences of at most one bfloat16 spacing under the checkpoints' real scales. This localizes the divergence to scale application and output rounding after the exact accumulator. Applied as a probe checkpoint, the same intervention restores end-to-end bitwise agreement (8/8 and 16/16 sequences). Cross-implementation FP8 GEMM shows a different signature: both the prevalence and the magnitude of differences grow with reduction depth, while the INT8 fraction stays at parts per million and within one spacing over a 64x range of K. Teacher-forced replay ties layers to tokens: flips concentrate at small logit margins, which predict flip risk with ROC-AUC 0.94 on 16,384 positions. We will release the pre-registration, per-layer predictions, manifests with kernel-selection evidence, and a conformance procedure that turns these controls into a concrete check for kernel interchangeability.