---
title: "BALANCE: Hybrid Autoregressive-Speculative LLM Inference in Wireless Edge Networks"
title_zh: BALANCE：无线边缘网络中的混合自回归-投机式LLM推理
authors: "Guanqiao Qu, Shuo Chen, Qian Chen, Kin K. Leung, Xianhao Chen"
date: 2026-08-06
pdf: "https://arxiv.org/pdf/2608.05926v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 面向边缘LLM推理的混合自回归-投机解码框架，权衡时延与内存
tldr: 边缘LLM推理面临自回归解码延迟高、推测解码内存开销大的权衡，单一模式难以满足异构用户需求。为此提出BALANCE框架，边缘服务器同时托管小模型和大模型，将用户分配到自回归或推测解码模式，联合优化用户调度与资源分配。问题NP-hard，设计多项式时间算法，将原问题分解为两个子问题，得到常数近似保证的解。实验表明BALANCE优于传统AD和SD，显著提升任务吞吐量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.05926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 796, \"height\": 416, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05926v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 648, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05926v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 848, \"height\": 409, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05926v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 839, \"height\": 417, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05926v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 851, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05926v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 864, \"height\": 417, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.05926v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 845, \"height\": 413, \"label\": \"Figure\"}]"
motivation: 自回归解码延迟高，推测解码内存开销大，单一模式难以高效服务边缘异构用户。
method: 提出BALANCE，服务器同时运行SLM和LLM，将用户分配至AD或SD，联合优化调度与资源分配。
result: 将问题分解为子问题，设计多项式时间算法，获得常数近似保证，实验显著提升任务吞吐量。
conclusion: BALANCE通过混合模式实现边缘LLM高效推理，兼顾延迟与内存，优于传统方案。
---

## 摘要
边缘推理是在下一代移动网络中提供大语言模型（LLM）推理服务的一种有前景的范式。LLM推理主要依赖两种方法：自回归解码（AD）按顺序生成输出词元，导致较长延迟；投机式解码（SD）通过使用小语言模型（SLM）生成多个草稿词元供LLM验证来加速推理，但会带来额外内存开销。由于这种延迟-内存权衡，在有限边缘计算资源下，单一方法无法高效服务具有异构需求的用户。为应对这一挑战，我们提出了一种面向边缘LLM推理的混合自回归-投机式推理（BALANCE）框架。在BALANCE中，边缘服务器同时承载SLM和LLM，将每个用户分配给AD或SD，并同时执行这两种模式。为最大化服务的用户数量，我们构建了一个任务吞吐量最大化问题，以在用户延迟要求和服务器内存约束下，联合确定用户调度以及AD与SD之间的计算资源分配。由于该问题为NP难问题，我们开发了一种多项式时间算法，将原问题转化为两个子问题，并获得具有常数近似保证的次优解。实验表明，BALANCE始终优于传统AD和SD，并显著提升了任务吞吐量。

## Abstract
Edge inference is a promising paradigm to provide large language model (LLM) inference services in next-generation mobile networks. LLM inference mainly relies on two approaches: Autoregressive decoding (AD) generates output tokens sequentially, resulting in long latency; Speculative decoding (SD) accelerates inference by using a small language model (SLM) to generate multiple draft tokens for LLM verification, but incurs extra memory costs. Due to this latency-memory tradeoff, neither approach alone can efficiently serve users with heterogeneous demands under limited edge computing resources. To address this challenge, we propose a hybrid autoregressive-speculative inference (BALANCE) framework for edge LLM inference. In BALANCE, an edge server hosts both an SLM and an LLM, assigns each user to AD or SD, and performs the two modes simultaneously. To maximize the number of served users, we formulate a task throughput maximization problem to jointly determine user scheduling and computing resource allocation between AD and SD under user latency requirements and server memory constraints. Since the problem is NP-hard, we develop a polynomial-time algorithm that transforms the original problem into two sub-problems and obtains a sub-optimal solution with a constant approximation guarantee. Experiments demonstrate that BALANCE consistently outperforms conventional AD and SD and significantly improves task throughput.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在资源受限的边缘服务器上，同时满足用户异构需求（不同时延要求、输入/输出长度）并最大化服务用户数量，面临**自回归解码（AD）高时延与投机式解码（SD）高内存开销**的两难困境。
- **背景与动机**：
  - 边缘LLM推理是下一代移动网络的重要组成部分，可降低传输延迟，但单一推理模式存在固有缺陷：
    - **AD**：逐token顺序生成，内存效率高但端到端延迟大，难以满足强时延要求。
    - **SD**：用SLM生成草稿token、LLM并行验证，显著降低延迟，但需同时加载SLM和LLM、占用更高GPU内存。
  - 实测显示（图1）：SD比AD平均延迟降低约39%，但GPU内存开销增加约18%，体现明显的**延迟-内存权衡**。
- **解决思路**：提出**BALANCE**框架，边缘服务器同时托管SLM和LLM，并行支持AD与SD两种模式，通过联合用户调度和资源分配，在满足时延与内存约束下最大化任务吞吐量。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将用户分为两批，分别以AD和SD模式运行，并联合优化以下决策变量：
  - **用户调度**：每个用户分配给AD模式（xₖ）、SD模式（yₖ）或不服务；
  - **计算资源分配**：GPU计算资源在AD和SD间的比例 z；
  - **内存资源分配**：GPU内存在AD和SD间的比例 λ。
- **系统建模**：
  - **通信模型**：上行输入上传时间、下行输出下载时间基于带宽与频谱效率建模；
  - **计算模型**：基于注意力机制量化LLM/SLM的预填充（prefill）和解码/验证的工作量（FLOPs），包括QKV投影、注意力计算、FFN等；
  - **内存模型**：考虑模型权重和KV缓存的峰值内存占用，AD和SD分别建模，SD内存为SLM与LLM KV缓存之和；
  - **延迟模型**：AD为串行解码步骤累加，SD为多个迭代（SLM草稿生成 + LLM并行验证）累加，并以线性模型关联计算工作量与GPU资源比例。
- **问题定义**：P1为最大化服务用户数（任务吞吐量）的整数规划，约束包括每用户时延、GPU内存容量、资源分配比例等。
- **算法设计（算法1-3）**：
  1. **问题等价转换**：引入内存分割变量λ，将P1等价转换为P2；
  2. **问题分解**：固定z和λ后，P2分解为AD子问题（P3.1）和SD子问题（P3.2）；
  3. **子问题求解**：
     - **算法1（AD调度）**：枚举关键参数（最大输入长度、最大输出长度、延迟阈值、上传时间等），按单位延迟贡献排序贪心选择用户；
     - **算法2（SD调度）**：类似思路，枚举SD特有参数（迭代次数、接受率、草稿长度等），按延迟和内存双约束排序选择用户；
  4. **整体求解**：算法3枚举所有(z, λ)组合，依次调用算法1和算法2，选取服务用户最多的组合作为最终方案。
- **理论保证**：
  - **NP-hard**：P1映射为多维多重背包问题（MMKP）；
  - **常数近似**：算法3提供 1/2 的近似比，即 `r(Ẋ+Ẏ) ≥ ½ r(X*+Y*)`；
  - **多项式复杂度**：算法3总体时间复杂度为 O(K⁶ log K)。

## 3. 实验设计：使用数据集 / 场景 / benchmark / 对比方法

- **实验环境与场景**：
  - 无线边缘网络单服务器场景，覆盖半径250m；
  - 多用户（K=20~70），用户输入长度和输出长度各在设定范围内随机；
  - 用户时延要求在 [T_min, T_max] 范围内（如0.5s~13s）。
- **Benchmark模型**：LLM为 Llama-2-7B，SLM为 TinyLlama-1.1B，FP16精度。
- **对比方法**：
  - **AD-only**：只运行AD模式（由算法1求最优调度）；
  - **SD-only**：只运行SD模式（由算法2求最优调度）；
  - **Exhaustive search**：穷举搜索P1的最优解（仅在小规模K=10~16下运行）；
  - **消融实验变体**：ECRA（z=0.5）、EMA（λ=0.5）、ECRMA（z=λ=0.5）。
- **性能指标**：归一化任务吞吐量（服务用户数/总用户数）。

## 4. 资源与算力

- **GPU**：NVIDIA GeForce RTX 4090（24GB显存），单卡；
- **CPU**：AMD Ryzen Threadripper PRO 5975WX；
- **内存**：256GB DDR4 RAM（非GPU显存，用于主机系统）；
- **推理精度**：FP16；
- **训练时长**：论文未涉及模型训练，仅做推理测试；具体实验总时长未报告。
- **注**：论文未明确列出具体实验运行时长（除算法运行时间对比外）。

## 5. 实验数量与充分性

- **主要实验组数**：
  - 用户数K变化（20~70）；
  - 最大时延要求T_max变化（1s~13s）；
  - 最大输入长度LI_max变化（100~1000）；
  - 最大输出长度LO_max变化（100~1000）；
  - 算法运行时间对比（K=10~16）；
  - 消融实验（K变化、T_max变化两组）。
- **总体评价**：
  - **充分性较好**：覆盖了关键系统参数（用户规模、时延要求、输入/输出长度）对性能的影响；
  - **对比公平**：与AD-only和SD-only的最优调度方案对比，基准较强；
  - **验证了理论保证**：通过穷举搜索验证了算法接近最优（平均仅0.9%差距）；
  - **消融实验合理**：分别检验计算资源分配、内存分配和两者联合分配的贡献。
- **局限性**：实验场景为单服务器单GPU，未覆盖多服务器、多GPU、异构GPU、真实流量模式等更复杂场景。

## 6. 论文的主要结论与发现

- **BALANCE显著优于单一模式**：
  - 随K变化：相比AD-only和SD-only，平均提升37.9%和33.4%；
  - 随T_max变化：平均提升31.5%和27.3%；
  - 随LI_max变化：平均提升38.8%和23.9%；
  - 随LO_max变化：平均提升30.4%和27.3%。
- **算法运行时间高效**：相比穷举搜索，提速约5100倍（最大21800倍，K=16），且吞吐量仅损失约0.9%，接近最优。
- **消融实验结论**：
  - 联合优化z和λ比固定任一参数均有提升：相比ECRA、EMA、ECRMA分别提升5.6%~9.0%、4.9%~18.7%、18.4%~23.7%；
  - 内存调度比计算调度影响更大（EMA造成的性能下降比ECRA更明显）。

## 7. 优点

- **问题定义清晰、实践意义强**：捕捉了AD和SD的本质权衡，提出混合模式框架，解决真实边缘推理场景中的资源优化问题。
- **理论分析扎实**：NP-hard证明完备；常数近似比（1/2）的证明逻辑清晰；复杂度分析明确（O(K⁶ log K)）。
- **算法设计系统**：从原问题到子问题的分解层层递进，支持任意离散资源分配粒度，工程可实施。
- **建模细致**：对计算工作量、KV缓存内存、通信延迟均有精细化数学建模，贴近真实Transformer推理行为。
- **实验对比严谨**：与各自模式的最优算法比较，结论更有说服力；运行时间与穷举对比验证实用性。

## 8. 不足与局限

- **假设简化**：
  - 假设用户输入/输出长度为已知先验（依赖预测器或任务要求），预测误差的影响未讨论；
  - 假设计算资源分配（z）可精确实现为离散档位，实际GPU资源隔离的粒度可能受限；
  - 单边缘服务器假设，未考虑多基站、多服务器协同场景。
- **实验覆盖有限**：仅使用单一LLM（Llama-2-7B）与单一SLM（TinyLlama-1.1B），未验证模型规模变化时框架的普适性。
- **SD参数设定固定**：草稿长度LD=1，接受率仅取[0.6,0.8]区间，未做更广范围的敏感性分析。
- **通信模型较理想**：无线信道采用固定频谱效率，未建模动态信道衰落和干扰。
- **理论保证的强度**：1/2近似比已有理论保障，但实际最优解差距未在更大规模问题下充分测试（穷举仅在K≤16时可行）。
- **缺少真实负载测试**：未使用真实用户请求轨迹或公开LLM推理工作负载数据集进行评测。

（完）
