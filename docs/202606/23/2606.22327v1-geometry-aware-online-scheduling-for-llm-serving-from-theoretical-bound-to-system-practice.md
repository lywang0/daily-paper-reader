---
title: "Geometry-Aware Online Scheduling for LLM Serving: From Theoretical Bound to System Practice"
title_zh: 面向LLM服务的几何感知在线调度：从理论界限到系统实践
authors: "Li Kong, Qi Qi, Yinyu Ye, Zijie Zhou"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22327v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 基于几何感知的LLM服务在线调度算法SVF
tldr: 大语言模型推理中KV缓存内存的动态管理是关键优化点。现有时间中心调度策略忽略LLM推理的几何空间特性，本文提出几何感知在线调度算法Smallest Volume First（SVF）及其高效变体1-bit SVF。理论上通过新颖证明方法将已知输出长度下SVF的最坏情况竞争比从48收紧至5，并完成不同场景的理论分类。实际中将算法作为可插拔层集成到vLLM，在Llama-3.1模型上实验显示SVF显著降低平均和尾延迟，1-bit SVF仅需单比特信息即可取得有竞争力吞吐和延迟。该工作为解决现代LLM部署中的内存约束调度提供了理论坚实且经验证的方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22327v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1219, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22327v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 804, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22327v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1423, \"height\": 602, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22327v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1452, \"height\": 329, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22327v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1349, \"height\": 241, \"label\": \"Table\"}]"
motivation: 现有LLM推理引擎依赖时间中心调度启发式（如SJF），其理论最优性基于传统调度模型，无法捕获LLM推理特有的动态二维时空几何增长特性。
method: 提出几何感知在线调度算法Smallest Volume First（SVF）及其高效变体1-bit SVF，通过引入体积度量来优先调度占用内存少的请求。
result: 理论上将SVF在已知输出长度下的竞争比从48收紧至5；在vLLM集成中，SVF显著降低平均和尾延迟，1-bit SVF仅需单比特信息即可实现竞争性性能。
conclusion: 本文建立了理论严谨且实践验证的内存约束调度方案，为LLM部署优化提供了新方向。
---

## 摘要
交互式大型语言模型服务的爆炸性需求凸显了键值缓存动态内存占用管理作为推理引擎性能优化的关键领域。现代推理系统 overwhelmingly 依赖基于时间的调度启发式算法，如最短任务优先。然而，它们的理论最优性根植于传统调度建模，未能捕捉LLM推理机制特有的高度动态的二维时空几何增长。为解决此问题，我们通过引入最小体积优先（SVF）算法及其高效变体1位SVF，提出了几何感知在线调度。理论上，我们为方法提供了严谨的数学基础。利用新颖的证明方法，我们收紧了已知输出长度下SVF的最坏情况竞争比（$\text{CR} \le 48 \rightarrow \text{CR} \le 5$）。基于这一核心突破，我们完成了全面的理论分类，分析了算法在不同流量场景和信息可用性下的表现。实践中，我们以即插即用层的方式将方法无缝集成到vLLM中。在Llama-3.1模型上的广泛评估证明了全面的性能提升：SVF显著降低了平均延迟和尾延迟，而仅需单比特信息的1位SVF实现了有竞争力的吞吐量和延迟。该工作为现代LLM部署中解决内存受限调度问题建立了一个理论可靠且实证有效的方法。为便于未来研究，我们的代码发布于https://github.com/Aurora-Kl/Geometry-Aware-Online-Scheduling.git。

## Abstract
The explosive demand for interactive Large Language Model serving has highlighted the management of the Key-Value cache's dynamic memory footprint as a critical area for performance optimization in inference engines. Modern inference systems overwhelmingly rely on time-centric scheduling heuristics, such as Shortest Job First. However, their theoretical optimality is rooted in traditional schedule modeling, failing to capture the highly dynamic, 2D spatio-temporal geometric growth specific to LLM inference mechanisms. To resolve this, we propose the geometry-aware online scheduling by introducing the Smallest Volume First (SVF) algorithm and its highly efficient variant, 1-bit SVF. Theoretically, we provide a rigorous mathematical foundation for our approach. Utilizing a novel proof methodology, we tighten the worst-case competitive ratio ($\text{CR} \le 48 \rightarrow \text{CR} \le 5$) for SVF with known output lengths. Building upon this core breakthrough, we complete a comprehensive theoretical taxonomy analyzing our algorithms across different traffic scenarios and information availability. Practically, we seamlessly integrate our approach as a plug-and-play layer in vLLM. Extensive evaluations on Llama-3.1 models demonstrate comprehensive performance gains: SVF delivers strong reductions in both average and tail latency, while 1-bit SVF, with merely a single bit information, achieves competitive throughput and latency. This work establishes a theoretically sound and empirically proven approach for resolving memory-constrained scheduling in modern LLM deployments. To facilitate future research, our code is available at https://github.com/Aurora-Kl/Geometry-Aware-Online-Scheduling.git.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）推理过程中，KV cache 的动态内存管理是性能优化的关键瓶颈。现有推理引擎广泛采用基于时间的调度启发式（如最短作业优先 SJF），但这些策略的理论最优性源于传统调度模型，无法捕捉 LLM 推理中特有的**二维时空几何增长**特性——即每个生成的 token 会线性增加 KV cache 占用，且连续批处理（continuous batching）导致并发批大小动态波动。
- **研究动机**：SJF 等时间中心策略在 LLM 场景下可能产生无界的最坏情况竞争比（已有工作证明其 CR 可无限大），因此需要从根本上重新设计调度理论，将 KV cache 的时空占用（即“体积”）作为优先级度量，以确保理论保证和实际性能。
- **整体含义**：本文旨在建立一种**几何感知的在线调度框架**，通过将调度优先级从一维时间转变为二维内存时间积分（体积），实现严格的理论界（将竞争比从48收紧至5）并在实际系统（vLLM）中获得显著的延迟与吞吐改进。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 针对 LLM 推理中每个请求的动态内存占用，定义其生命周期内的 **体积**（Volume）：
  
  \[
  v_i = s_i \cdot o_i + \frac{o_i^2 + o_i}{2}
  \]
  其中 \(s_i\) 为提示长度，\(o_i\) 为输出长度。
- 调度时优先选择体积最小的请求（**Smallest Volume First, SVF**），实现从一维时间到二维体积的范式转换。
- 为降低预测开销，提出 **1-bit SVF**：仅用 1 bit 信息（短/长分类）结合全局代理长度计算近似体积，仍能保证理论界。

### 关键技术细节
- **SVF 算法**（Algorithm 1）：
  - 两个并行进程：进程1（后台批处理预测器）负责批量预测输出长度并计算体积，将请求按体积升序插入等待队列；进程2（LLM 推理引擎）持续从队头取出最高优先级请求注入运行批，直到 KV cache 容量满。
- **1-bit SVF 算法**（Algorithm 2）：
  - 使用轻量级二分类器（BERT-tiny）判断请求为“短”或“长”，并分配代理长度 \(O_0\) 和 \(O_1\)（例如 \(O_0 = T^{1/4}, O_1 = T^{3/4}\)），计算代理体积。
- **理论证明**：
  - 在爆发到达模型下，通过新颖的宏观“体积速率证书”证明 SVF 的竞争比 \(\text{CR} \le 5\)（此前为 \(\le 48\)）。
  - 证明 1-bit SVF 在爆发到达下的竞争比为 \(O(T)\)（\(T\) 为最大模型长度常数）。
  - 在泊松到达模型下，证明 SVF 和 1-bit SVF 的期望竞争比分别有界于 \(1 + \frac{4}{(1-\mu)(1-\rho)}\) 和 \(1 + \frac{E[\text{vol}]}{E[\text{vol}]-\epsilon} \cdot \frac{4}{(1-\mu)(1-\rho)}\)。

## 3. 实验设计：使用数据集 / 场景、benchmark、对比方法

### 数据集与场景
- **数据集**：
  - **LMSYS-Chat**：模拟高并发交互式聊天流量。
  - **LongBench**：模拟内存密集型长上下文任务。
- **到达模式**：
  - **对抗性爆发（Burst）**：所有请求同时到达 \(t=0\)，测试峰值调度能力和队头阻塞弹性。
  - **随机泊松过程**：查询率 QPS ∈ {2, 8, 16, 32, 64, 128}，评估稳态性能。

### 对比方法
- **FCFS**（先来先服务，原生基线）
- **SJF**（最短作业优先，经典一维启发式）
- **Oracle-SJF / Oracle-SVF**：使用真实输出长度的理想化调度器（作为性能上界天花板）
- **1-bit SVF**：本文轻量级变体

### 评估指标
- **逐 token 延迟**：平均延迟（Avg Latency）和第95百分位尾延迟（P95 Latency）
- **Token 吞吐量**：总生成 token 数 / 总实验时长

## 4. 资源与算力
- **硬件**：单台高性能计算节点，配备 **8 块 NVIDIA A100 (80GB) GPU**。
- **模型部署**：
  - Llama-3.1-8B-Instruct：单 GPU 部署。
  - Llama-3.1-70B-Instruct：8 张 GPU 使用张量并行部署。
- **精度与配置**：FP16/BF16，上下文窗口 65536 token，最大生成长度 4096 token。
- **算力成本**：文中未给出具体训练时长或总 GPU 小时数，但提到预测器开销极低（<0.06%）。

## 5. 实验数量与充分性

- **实验组数**：涵盖了两种模型（8B/70B） × 两种数据集（LMSYS/LongBench） × 两种到达模式（爆发/泊松） × 多种 QPS 水平（6个档位） × 多种调度算法，共形成约 **24 个以上子实验**。
- **消融实验**：
  - 对比 Oracle 调度器（Oracle-SJF vs Oracle-SVF）验证理论优势。
  - 系统开销分析（预测器耗时占端到端比例）。
  - 通过不同 QPS 下的性能曲线展示算法鲁棒性。
- **充分性与公平性**：
  - 严格控制变量：使用相同预测器架构（仅分类器大小不同），公平对比调度策略本身。
  - 报告了平均和尾延迟、吞吐量，覆盖了关键性能维度。
  - 实验设计较为全面，能有效支撑结论。但缺乏跨 GPU 集群的扩展实验，以及端到端资源消耗（如显存、能耗）的详细对比。

## 6. 论文的主要结论与发现

1. **SVF 全面优于 SJF 和 FCFS**：在所有到达模式、数据集和模型规模下，SVF 取得最低的平均延迟，大部分场景下 P95 尾延迟最低，且吞吐量不低于 SJF。
2. **Oracle-SVF 稳定优于 Oracle-SJF**：甚至在 LongBench 内存密集型任务中，非全知的 SVF（使用预测值）都能优于全知的 Oracle-SJF，证明时间中心指标的根本缺陷。
3. **1-bit SVF 具有性价比**：仅需 1 bit 分类信息，即可在多数场景下超越 FCFS，并在吞吐量方面优于 SJF，在长上下文场景下平均延迟也优于 SJF。
4. **理论界有效**：实际性能曲线与理论分析一致，证明几何感知调度在爆发和稳态下均提供严格保证。
5. **系统开销极低**：长度预测器开销不到 0.06%，分类器不到 0.01%，可忽略不计。

## 7. 优点

- **理论创新**：首次将 LLM 推理调度问题严格建模为二维几何问题，并给出紧致竞争比（\(\text{CR}\le5\)），证明方法新颖（宏观体积速率证书）。
- **可部署性**：SVF 和 1-bit SVF 作为即插即用模块可直接集成到现有推理引擎（vLLM），无需改变底层执行机制。
- **轻量化**：1-bit SVF 所需信息极少，预测器只需极小的 BERT-tiny 模型，适合资源受限场景。
- **鲁棒性**：理论界和实验均证明即使在极端爆发流量下也能保持性能稳定，不易发生过拟合。

## 8. 不足与局限

- **实验覆盖不足**：
  - 未在多节点分布式集群上进行验证，仅测试单机（8 GPU）场景。
  - 未与更复杂的工程优化（如主动 swapping、preemption-aware 策略）进行联合对比。
  - 缺乏对显存使用量、能耗等系统资源成本的详细分析。
- **预测器质量依赖**：SVF 依赖于输出长度预测的准确性，虽然理论界基于已知长度，但实际性能可能受预测误差影响（文中仅使用单一预测模型）。
- **1-bit 变体的非最优性**：虽然理论界为常数，但实际在聊天工作负载下平均延迟高于 SJF，存在精度损失。
- **应用限制**：
  - 模型假设输出长度服从几何分布（为了理论分析），实际分布可能更为复杂。
  - 未考虑请求优先级（更细粒度的 SLO）或混合工作负载（如流式输出与批处理共存）场景。
- **偏差风险**：实验仅在 Llama-3.1 系列上进行，可能无法推广到其他架构（如 MoE、Mamba）或更大的模型。

（完）
