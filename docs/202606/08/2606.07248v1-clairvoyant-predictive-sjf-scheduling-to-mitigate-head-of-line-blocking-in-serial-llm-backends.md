---
title: "Clairvoyant: Predictive SJF Scheduling to Mitigate Head-of-Line Blocking in Serial LLM Backends"
title_zh: "Clairvoyant: 预测式SJF调度以缓解串行LLM后端中的队头阻塞"
authors: Aravind Sundaresan
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07248v1"
tags: ["query:edge-llm"]
score: 8.0
evidence: 为边缘设备上的串行LLM后端提供预测调度
tldr: "在串行LLM后端（如Ollama）中，FCFS调度导致短查询被长生成任务阻塞，造成队头阻塞（HOLB）。针对内存受限的边缘设备，提出Clairvoyant侧车代理，通过19个轻量级词汇特征利用ONNX导出的XGBoost分类器预测响应长度，每请求延迟仅0.029毫秒。系统优化排序保真度，在自然对话数据集上达到62-96%内分布和52-66%跨分布准确率。端到端GPU基准测试显示，在最大队列压力下短请求P50延迟降低70-76%。该方法无需修改后端，开源可用。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07248v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1532, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07248v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1589, \"height\": 380, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07248v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1661, \"height\": 959, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.07248v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1017, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07248v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1170, \"height\": 488, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07248v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1931, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07248v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1327, \"height\": 379, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07248v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1068, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07248v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1043, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07248v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 984, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07248v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1375, \"height\": 298, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07248v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1251, \"height\": 433, \"label\": \"Table\"}]"
motivation: 串行LLM后端在混合负载和高利用率下，FCFS调度导致短查询遭遇HOLB，而云级连续批处理方案需要大量显存，不适合边缘设备。
method: 提出Clairvoyant侧车代理，使用19个词汇特征通过ONNX导出的XGBoost分类器预测响应长度，以相对排序优化调度，提升排名保真度。
result: "在RTX 4090上，100并发请求时短查询P50延迟降低70-76%，稳态泊松到达下降低17%，预测延迟仅0.029毫秒。"
conclusion: Clairvoyant有效缓解串行LLM后端的HOLB，无需修改后端，为边缘部署提供实用调度方案。
---

## 摘要
串行LLM推理后端（如Ollama）在FCFS准入控制下逐个处理请求，导致高利用率混合工作负载下的队头阻塞（HOLB）：短事实查询可能被长生成任务延迟数分钟。虽然云端部署通过连续批处理（vLLM、Orca）缓解了HOLB，但这些方案需要几十GB的VRAM来存储并发KV缓存，这对于依赖串行请求分发的内存受限边缘和本地部署不可行。我们提出了Clairvoyant，一个针对任何串行OpenAI兼容后端（如Ollama、llama.cpp）的即插即用边车代理。Clairvoyant通过ONNX导出的XGBoost分类器，利用19个轻量级词汇特征预测响应长度，实现每请求0.029毫秒的延迟（比典型生成时间低四个数量级）。由于准入调度依赖于相对顺序而非精确预测，系统优化了排名保真度，在自然对话数据集上实现了62-96%的分布内准确率和52-66%的跨分布准确率。我们发现精选指令数据集是长度预测的退化训练源：GPT强加的简洁约束将长类别表示降至示例的0.02%以下，使得自然对话日志成为唯一可行的训练源。在RTX 4090上的端到端GPU基准测试显示，在最大队列压力（100个并发请求）下，短请求的P50延迟降低70-76%，在稳态泊松到达（ρ=0.74）下降低17%。Clairvoyant是开源的，且无需修改推理后端。

## Abstract
Serial LLM inference backends -- such as Ollama -- process requests one at a time under FCFS admission, causing Head-of-Line Blocking (HOLB) under mixed workloads at high utilisation: short factual queries can be delayed by minutes behind long generation jobs. While cloud-scale deployments mitigate HOLB via continuous batching (vLLM, Orca), these solutions require tens of GB of VRAM for concurrent KV-caches -- infeasible for memory-constrained edge and local deployments that rely on serial request dispatch. We present \clairvoyant, a drop-in sidecar proxy for any serial OpenAI-compatible backend (e.g., Ollama, llama.cpp). \clairvoyant predicts response length from 19 lightweight lexical features via an ONNX-exported XGBoost classifier, achieving 0.029\,ms per-request latency (four orders of magnitude below typical generation time). Because admission scheduling depends on relative ordering rather than exact prediction, the system optimises ranking fidelity, achieving 62--96\% in-distribution and 52--66\% cross-distribution accuracy across natural conversation datasets. We find that curated instruction datasets are degenerate training sources for length prediction: GPT-imposed brevity constraints reduce Long-class representation to under 0.02\% of examples, making natural conversation logs the only viable training source. End-to-end GPU benchmarks on an RTX~4090 show 70--76\% P50 latency reduction for short requests under maximum queue pressure (100 concurrent requests) and 17\% under steady-state Poisson arrivals ($ρ=0.74$). \clairvoyant is open-source and requires no modifications to the inference backend.

---

## 论文详细总结（自动生成）

# 论文《Clairvoyant: 预测式SJF调度以缓解串行LLM后端中的队头阻塞》详细中文总结

## 1. 核心问题与整体含义
- **研究动机**：串行LLM推理后端（如Ollama、llama.cpp）默认采用先来先服务（FCFS）调度。在混合工作负载下，短查询（约2秒）可能被排在前面的长生成任务（>60秒）阻塞数分钟，即队头阻塞（HOLB）问题。
- **背景**：云端方案（vLLM、Orca）通过连续批处理解决HOLB，但需要数十GB显存维护并发KV缓存，不适合内存受限的边缘设备、本地部署或单GPU环境。
- **整体含义**：Clairvoyant作为轻量级边车代理，在不修改后端的前提下，通过预测式最短作业优先（SJF）调度缓解HOLB，填补了串行后端调度优化的空白。

## 2. 方法论
- **核心思想**：在准入层实现非抢占式SJF调度，通过轻量级词汇特征预测请求的输出长度（分类为Short/Medium/Long），以P(Long)概率值作为优先级键，实现请求的正确排序。
- **关键技术细节**：
  - **特征提取**：从输入提示中计算19个词汇特征，包括6个数值特征（如近似词元长度、代码关键词、长度约束、以问号结尾、格式关键词、从句复杂度）和13个动词独热特征（what/write/explain等）。
  - **预测模型**：XGBoost分类器（3类softmax），导出为ONNX格式，在Go侧车代理中通过ONNX Runtime C API运行。每请求推理延迟0.015-0.029毫秒。
  - **调度机制**：维护一个最小堆（键=预测的P(Long)），分发器持续提取最小优先级的请求转发给后端。同时设置饥饿超时τ=3×μ_short（μ_short为短请求平均逗留时间），当请求等待超过τ时提升至队首。
  - **无修改架构**：代理透明拦截API请求，响应透传，后端无需改动。

## 3. 实验设计
- **数据集**：
  - 训练/评估：ShareGPT（52K，滤后48,312）、LMSYS-Chat-1M（ ~876K滤后）、OASST1（8,792对），均通过平衡采样得到每类2000/2000/2000（或更少）的训练集。
  - 不可用数据集（长类别样本过少）：Alpaca 52K（仅4个长样本）、CodeAlpaca 20K（3个长样本）、Dolly 15K（88个长样本，仅用于测试）、CNN/DailyMail（1个长样本）。
- **评估场景**：
  - **离线排序保真度**：三个模型（A/B/C）的分布内和跨分布测试，指标为成对排序准确率（Short vs Long对的P(Long)正确排序比例）。
  - **基线对比**：与FCFS（随机）、提示长度规则、关键词启发式对比。
  - **端到端GPU延迟**：RTX 4090上100并发请求（50短+50长）的突发测试；稳态泊松到达（ρ≈0.74）的模拟。
  - **饥饿超时敏感性**：离散事件模拟（DES）改变τ值（1×μ_short至∞）。
  - **消融研究**：逐一移除19个特征组衡量贡献。
- **基准（Baseline）**：FCFS（先来先服务），以及提示长度规则、关键词启发式。

## 4. 资源与算力
- **GPU型号**：NVIDIA RTX 4090（24 GB VRAM），用于端到端延迟基准测试。
- **其他硬件**：Apple M1（16 GB统一内存）用于部分延迟测量和调度验证。
- **训练资源**：XGBoost模型训练在CPU上完成，每个模型的训练时间未明确报告，但提及“在消费级CPU上重训练约500个样本通常<10秒”。训练使用Python 3.10+及XGBoost 2.0.3库。
- **推理开销**：ONNX模型推理0.015-0.029毫秒（CPU），无GPU加速要求。

## 5. 实验数量与充分性
- **实验范围**：
  - 1组数据集分布统计（7个数据集）。
  - 3个训练模型分布内排序准确率（表5）。
  - 1组跨分布排序准确率矩阵（表6，3×3=9个单元格）。
  - 1组基线对比（表7，4种方法×3数据集）。
  - 1组消融实验（表4，移除7种特征组×3模型）。
  - 2组端到端GPU延迟测试（表8，2个模型×2个条件×2类请求）。
  - 1组饥饿超时敏感性模拟（表9，5种τ值）。
  - 1组不同利用率下的延迟收益图（图3）。
- **充分性评价**：实验覆盖了多个自然对话数据集、不同分布泛化、与3种简单基线对比、消融分析、多种负载模式（突发/稳态/不同ρ）和超参数敏感性，整体较为充分。
- **客观性/公平性**：
  - 推荐了仓库提交版本号（39ad9a6）以确保可复现。
  - 使用了标准化指标（排序准确率、P50/P95/P99延迟）。
  - 未对比其他基于学习的方法（如S3、LTR），但作者在相关工作部分解释了差异化，认为互补而非竞争。

## 6. 主要结论与发现
- **数据集发现**：精选指令数据集（Alpaca、CodeAlpaca）因GPT简洁约束导致长响应样本极少（<0.02%），不适合长度预测训练；只有自然对话日志（ShareGPT、LMSYS、OASST1）提供足够的类别多样性。
- **预测性能**：19个词汇特征+XGBoost实现62-96%分布内排序准确率，推理延迟仅0.029毫秒；相比之下，嵌入预测方法P50=12.85毫秒，慢443-5125倍。
- **端到端延迟收益**：
  - 在RTX 4090上100并发突发下，短请求P50延迟降低70-76%（Gemma3:4b和Llama3.1:8b）。
  - 在稳态泊松到达（ρ=0.74）下短请求P50降低17%。
  - 在ρ<0.50时收益<3%，FCFS足够。
- **饥饿超时折中**：τ=3×μ_short为帕累托前沿肘点，短请求P50改进17%，长请求P95膨胀仅17%。
- **排名精度优于分类精度**：排序准确率比三段式分类准确率高21-29个百分点。

## 7. 优点
- **极低开销**：0.029毫秒预测延迟，比短生成时间低4个数量级，适合边缘部署。
- **无需修改后端**：作为透明代理，对Ollama、llama.cpp等零改动。
- **特征设计精巧**：仅使用字符串扫描和简单计数，无向量嵌入、无正则回溯，实现亚微秒级提取。
- **实用导向**：提供了饥饿超时校准建议（测μ_short → 设τ=3×μ_short → 在±20%内调优），降低了使用门槛。
- **开放与可复现**：开源代码、预训练模型、评估脚本，指定提交版本，便于验证和应用。
- **问题识别深刻**：明确指出指令数据集的“长类别饥饿”问题，对后续研究有警示意义。

## 8. 不足与局限
- **适用范围有限**：仅针对串行或低并发后端；已运行vLLM/Orca的部署无收益，甚至增加额外开销。
- **跨分布泛化较弱**：跨分布排序准确率仅52-66%，接近随机（50%），作者建议针对生产数据重训练。
- **英文/代码限制**：特征提取和动词映射仅支持英文；代码、多语言、符号密集型提示的近似词元长度（len//4）可能不准。
- **多租户/公平性未处理**：系统默认偏爱短请求，可能对长请求用户不公平；仅提出四种可能解决方案，未实现。
- **评估覆盖有限**：
  - 仅在一块RTX 4090上测试硬件；未验证llama.cpp、Jan等后端。
  - 离散事件模拟假设服务时间正态分布，未考虑真实LLM生成的提示相关方差。
  - 未单独测量首令牌时间（TTFT），虽评估了推理延迟小，但未实证证明对交互延迟的影响。
- **饥饿超时校准依赖经验**：τ=3×μ_short基于短期测试，未给出在生产环境自适应更新的机制。
- **未与最先进预测调度方法对比**：未与S3、LTR等在同一设置下对比，尽管作者理由充分。

（完）
