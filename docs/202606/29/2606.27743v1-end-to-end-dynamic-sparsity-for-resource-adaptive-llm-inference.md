---
title: End-to-End Dynamic Sparsity for Resource-Adaptive LLM Inference
title_zh: 端到端动态稀疏性用于资源自适应的大语言模型推理
authors: "Yuhang Chen, Jinhao Duan, Ruichen Zhang, Mingfu Liang, Xiaohan Wei, Yunchen Pu, Fei Tian, Chonglin Sun, Parish Aggarwal, Frank Shyu, Luke Simon, Sandeep Pandey, Tianlong Chen, Xi Liu"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27743v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 通过动态稀疏性实现资源自适应的LLM推理
tldr: "传统LLM推理假设静态资源，无法应对云环境的资源波动。提出L2A框架，通过轻量级预算和输入感知的门控网络，联合优化性能、逻辑一致性和资源成本，实现层跳过、头剪枝和推理令牌缩减。单模型覆盖整个计算-精度帕累托前沿，在34%层稀疏度下GSM8K性能损失仅0.6%，且零样本泛化。无需单独调参即可自适应资源动态。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27743v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 767, \"height\": 773, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1648, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1714, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27743v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1529, \"height\": 498, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27743v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1562, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1721, \"height\": 369, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1721, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1716, \"height\": 430, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1724, \"height\": 324, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27743v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1721, \"height\": 367, \"label\": \"Table\"}]"
motivation: 解决静态LLM推理无法适应云环境资源波动的问题，如实例抢占和QoS需求变化。
method: 端到端框架，通过预算条件和输入感知的门控网络，联合优化任务性能、逻辑一致性和资源成本，实现层跳过、头剪枝和推理令牌缩减。
result: "单模型在Llama-3-8B和Qwen-3-4B上实现34%动态稀疏度，GSM8K性能损失<0.6%，且零样本泛化。"
conclusion: L2A通过端到端学习资源自适应策略，无需单独调参即可达到完整计算-精度帕累托前沿。
---

## 摘要
大语言模型（LLM）的推理通常在静态资源假设下部署，模型执行固定计算图，而不考虑运行时环境。然而，现实世界的云基础设施本质上是动态的，其特点是资源可用性波动（例如，抢占式实例中断）和分层服务质量要求。在这种不稳定的环境中，静态模型缺乏灵活性：它们要么在资源约束下崩溃，要么在冗余操作上浪费计算资源。为弥合这一差距，我们提出学习分配（L2A），一个用于资源自适应推理的端到端框架。与仅基于输入难度的先前方法不同，我们将推理公式化为一个条件于输入和运行时资源预算本身的约束分配问题。我们引入轻量级的、预算条件化和输入感知的门控网络，集成到LLM中。这些门通过统一目标进行训练，该目标联合优化任务性能、逻辑一致性和资源成本，沿三个维度对应现实世界的动态表现：层跳过（用于内存和深度压力）、头剪枝（用于吞吐量竞争）和推理令牌减少（用于延迟紧缩）。这使得模型能够学习一个超越输入难度的预算感知策略：它根据实时资源动态自适应地配置其计算足迹，在资源允许时最大化推理深度，在预算紧张时严格执行节俭。单个L2A模型在Llama-3-8B和Qwen-3-4B上描绘了整个计算-准确率帕累托前沿：在高达34%的实现了的层稀疏度下，其在GSM8K上保持与密集基线相差不到0.6%，在分布外任务的零样本设置中相同差距，而每个静态或启发式基线需要分别调整的模型，并且在相当的推理时间下性能下降5-10%。

## Abstract
Large Language Models (LLMs) inference is typically deployed under a static resource assumption, where models execute a fixed computational graph regardless of the runtime environment. However, real-world cloud infrastructure is inherently dynamic, characterized by fluctuating availability (e.g., spot instance preemption) and tiered Quality-of-Service requirements. In such volatile settings, static models are inflexible: they either crash under resource constraints or waste compute on redundant operations. To bridge this gap, we propose Learning to Allocate (L2A), an end-to-end framework for resource-adaptive inference. Unlike prior methods that condition only on input difficulty, we formulate inference as a constrained allocation problem conditioned on both the input and the runtime resource budget itself. We introduce lightweight, budget-conditioned and input-aware gating networks integrated into the LLM. These gates are trained via a unified objective that jointly optimizes task performance, logical consistency, and resource costs along three axes matching how real-world dynamics manifest: layer skipping for memory and depth pressure, head pruning for throughput contention, and reasoning-token reduction for latency tightening. This lets the model learn a budget-aware policy beyond input difficulty alone: it adaptively configures its computational footprint with respect to real-time resource dynamics, maximizing reasoning depth when resources permit while enforcing strict frugality when budgets tighten. A single L2A model traces the entire compute-accuracy Pareto frontier on Llama-3-8B and Qwen-3-4B: at up to 34% realized layer sparsity, it stays within 0.6% of the dense baseline on GSM8K, with the same gap holding zero-shot on out-of-distribution tasks, while every static or heuristic baseline requires a separately tuned model and still drops by 5-10% at comparable inference time.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：大语言模型（LLM）推理部署通常假设静态资源（固定的计算图），但在真实云环境中，资源是动态波动的（例如抢占式实例中断、不同用户分层 QoS 要求）。静态模型要么因资源不足而崩溃（OOM），要么在充足资源下浪费计算。
- **动机**：现有方法分为静态压缩（剪枝/蒸馏）和动态推理（早期退出/启发式跳过），但静态方法无法根据输入难度调整，动态方法多依赖脆弱的启发式（如熵阈值）且未考虑外部实时资源预算。
- **总体含义**：需要一种能够同时基于输入难度和运行时资源预算进行自适应计算的推理框架，实现弹性资源利用。

## 2. 方法论

### 核心思想
论文提出 **L2A (Learning to Allocate)** 框架，将推理建模为一个受资源预算约束的分配问题。通过引入轻量级、**预算条件化（budget-conditioned）** 和 **输入感知** 的门控网络，联合优化任务性能、逻辑一致性和多维资源成本，学习一个端到端的动态计算策略。

### 关键技术细节

1. **预算信号设计**：外部标量预算 \( b \in [0,1] \)（越大表示允许更多计算），通过嵌入向量 \( e(b) \) 输入门控网络。训练时从分布 \( D \) 采样 \( b \)，推理时通过校准实时系统信号（截止时间、队列长度、内存余量）得到 \( b \)。

2. **三类动态门控机制**：
   - **层跳过门** \( \alpha_l(h_{l-1}, b) \)：由一个轻量 MLP 生成，控制是否跳过整个 Transformer 层（自注意力+MLP）。软门控训练，推理时二值化。
   - **头剪枝门** \( \beta_{l,h}(h_{l-1}, b) \)：类似结构，每个注意力头独立控制，输出为头的加权组合。
   - **推理-答案过渡门** \( \tau_t(h_t, b) \)：在生成过程中决定何时从 `<think>` 段切换到 `<answer>` 段，从而缩短不必要的推理链。

3. **参数高效微调**：冻结预训练模型，仅训练 LoRA 适配器（rank=16）和门控网络，防止灾难性遗忘并支持稀疏路径适应。

4. **统一优化目标**：
   \[
   \mathcal{L}(b) = \mathcal{L}_{ce} + \gamma \mathcal{L}_{kd} + \lambda |C(b) - B(b)| + \lambda_T R_{tok}
   \]
   - \( \mathcal{L}_{ce} \)：下一令牌交叉熵。
   - \( \mathcal{L}_{kd} \)：冻结密集教师与学生之间的 KL 散度蒸馏，保持逻辑一致性。
   - \( |C(b) - B(b)| \)：预算约束惩罚，其中 \( C(b) \) 是实际计算量（基于层/头门值加权），\( B(b) \) 是目标预算（单调递增函数）。
   - \( R_{tok} \)：推理段长度惩罚，鼓励早停止思考。

5. **训练与推理**：训练时联合优化 LoRA 和门控参数；推理时软门转为硬门，物理跳过层或头，以及触发思考段边界。

## 3. 实验设计

### 数据集与场景
- **分布内（ID）**：OpenWebText（语言建模，PPL）、GSM8K（数学推理，准确率）、MMLU（知识问答，准确率）、Alpaca-Eval（指令跟随，得分）。
- **分布外（OOD）**：HumanEval（代码生成，Pass@1）、Big-Bench Hard（符号推理，准确率）。

### 基准模型
- Llama-3-8B、Qwen-3-4B。

### 对比方法
- **Original**：完整密集模型。
- **Uniform Pruning**：每 k 层统一跳过。
- **Oracle Static Pruning**：基于离线校准数据的最佳固定剪枝配置。
- **AdaSkip**：基于熵启发式的早期退出。
- **FlexiDepth**：带辅助跳过损失的自适应方法。
- **FixedBudget**（消融中）：全局固定稀疏度，无法按输入动态调整。

### 指标
- 质量：准确率/得分/Pass@1、PPL。
- 效率：平均层/头稀疏度、峰值 GPU 内存、总推理时间。

## 4. 资源与算力

论文未明确说明使用的 GPU 型号、数量或训练时长。仅提及 LoRA rank=16，门控为 2 层 MLP。硬件和计算预算的具体信息缺失，这是报告中的一个不足。

## 5. 实验数量与充分性

- **主要对比**：表2（Llama-3-8B）和表3（Qwen-3-4B）展示了各方法在固定计算预算下的性能，L2A 在所有任务上处于或接近密集基线，优于所有对比方法。
- **固定预算评估**（表4）：区分了固定稀疏度与动态策略，L2A 在相同平均计算成本下性能显著优于固定预算方法。
- **跨域泛化**（表5）：探究单域训练的影响，证明混合训练使策略学会根据输入领域调节稀疏度（硬任务保守、简单任务高效），而单域训练导致过剪或欠剪。
- **消融研究**（表6）：移除蒸馏（KD）导致推理能力崩溃；移除预算损失导致完全密集无加速；仅用层跳过（无头剪枝）需过度剪枝才能匹配延迟，效果更差。
- **训练动态诊断**（图3、图4）：展示策略能够追踪变化的预算信号，稳定收敛，无门崩溃。

实验覆盖多模型、多任务、消融和泛化性验证，设计较全面、公平（基线均匹配到相同效率水平）。但缺乏对更大模型（如 70B）和大规模部署延迟的评估。

## 6. 主要结论与发现

- L2A 以单模型覆盖整个计算-精度帕累托前沿，**无需为每个预算点单独调参**。
- 在 Llama-3-8B 上，达 34% 层稀疏度时，GSM8K 准确率仅下降 0.6%（从 36.5% 到 35.9%），且零样本泛化到 HumanEval、BBH 等 OOD 任务，同样保持小差距。
- 动态策略比所有静态或启发式基线更优：静态模型在类似推理时间下性能下降 5-10%。
- 学习到的门控网络捕捉了输入难度特征，能够跨域迁移（如数学推理和代码生成均受益）。

## 7. 优点

1. **端到端统一优化**：突破静态剪枝与动态推理的割裂，联合训练 LoRA 和门控，使得稀疏路径直接适应模型。
2. **预算条件化**：不仅依据输入难度，还依据实时外部预算，这是关键创新，支持真正的资源自适应。
3. **多维度稀疏**：同时调节层深度、注意力头宽度和推理链长度，更细粒度地匹配不同资源压力（内存、吞吐、延迟）。
4. **泛化性强**：混合域训练后，模型能识别输入难度并在不同任务间共享注意力，跨域无显著性能衰减。
5. **实验设计公平**：对比方法均在相同效率水平下评估，而非简单按原始计算量比较。

## 8. 不足与局限

1. **头剪枝实际加速依赖实现**：论文指出其带来的加速是架构依赖的（如需要硬件支持稀疏注意力），实际收益可能不如层跳过稳定。
2. **预算校准依赖准确估计**：推理时需在线估计延迟、内存余量等，误差会导致预算偏离最优，增加保守或激进风险。
3. **结构化提示格式限制**：方法需用户采用 `<think>/<answer>` 格式，不适用于所有生成场景（如自由对话），且训练时需注入标签。
4. **模型规模有限**：仅验证了 4B/8B 级模型，未测试更大尺寸（如 70B+），较大模型的稀疏性行为可能不同。
5. **缺乏硬件效率报告**：没有给出实际部署中的 wall-clock 加速比（仅报告了“时间”指标但未解释测量细节），也没有分析额外门控网络的开销。
6. **可解释性不足**：虽然门值可观测，但未深入分析门控网络内部如何融合隐藏状态和预算信息来决策。

（完）
