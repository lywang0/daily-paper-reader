Title: Phase Matters: Characterizing Heterogeneous Vision-Language Inference on a Mobile SoC

URL Source: https://arxiv.org/pdf/2606.27906v1

Published Time: Mon, 29 Jun 2026 00:44:20 GMT

Number of Pages: 6

Markdown Content:
# Phase Matters: Characterizing Heterogeneous Vision-Language Inference on a Mobile SoC 

## Aryama V Murthy ∗

International Institute of Information Technology, Hyderabad Hyderabad, India 

## Yashas N Kotre ∗

International Institute of Information Technology, Hyderabad Hyderabad, India 

## Prathmesh Sharma ∗

International Institute of Information Technology, Hyderabad Hyderabad, India 

## Pragya Mishra ∗

International Institute of Information Technology, Hyderabad Hyderabad, India 

## Sanjith Ganapathi 

International Institute of Information Technology, Hyderabad Hyderabad, India 

## Priyesh Shukla 

International Institute of Information Technology, Hyderabad Hyderabad, India 

Abstract 

Recent phone-class mobile SoCs expose practical NPU ex-ecution paths for on-device vision-language model (VLM) inference, but developers still lack phase-level guidance for mapping VLM pipelines across heterogeneous backends. We present a hardware-in-the-loop characterization of VLM in-ference on the Qualcomm SM8750 (Snapdragon 8 Elite), cov-ering phase throughput, cache-state effects, 100-run thermal stability, energy, heterogeneous CPU/NPU pipeline configu-rations, and visual-token-budget sensitivity. Using FastVLM-0.5B as an end-to-end case study, together with encoder-only measurements across four architecture families, we show that 

phase matters : NPU execution is highly phase-dependent, delivering 1.64 × speedup for prefill but only 1.18 × for decode, while vision encoders achieve 20–45 × speedups over CPU. These gains translate into 10.47 ∘C lower steady-state temper-ature and 2.52 × lower energy, avoiding thermal throttling in always-on settings. Finally, we show that a four-step graph rewrite enables previously unsupported encoders, such as Phi-3.5-V, to reach the QNN path with up to 22 × speedup, providing a practical porting recipe for mobile VLM deploy-ment. 

CCS Concepts 

• Hardware → Hardware accelerators ; • Computer systems organization → Embedded systems ; Real-time systems ; •

Computing methodologies → Computer vision .

Keywords 

edge systems, on-device VLM inference, mobile NPU, het-erogeneous SoC, system characterization, thermal charac-terization, cold-start management, Phase based deployment, Snapdragon 8 Elite, Hexagon Tensor Processor 

1 Introduction 

Vision-language models (VLMs) are crossing a critical threshold: devices already in consumers’ hands—with billion-parameter multimodal models—can now run VLM inference 

> ∗Equal contribution.

entirely on-device, with no cloud round-trip. Wearables, AR overlays, robotics, and privacy-sensitive pipelines all demand exactly this capability [ 1 –3 ]. The Qualcomm SM8750 (Snapdragon 8 Elite) exposes a stable Hexagon NPU path for full multimodal models via the QNN SDK [ 13 , 14 ], making it the right deployment platform to ask: what does NPU acceleration concretely deliver for each phase of VLM infer-ence, and how should practitioners configure heterogeneous pipelines? 

This question is non-trivial: VLM inference decomposes into three stages with fundamentally different hardware affini-ties [ 16 ]. Prefill saturates NPU tensor engines with batched GEMMs. Decode is bandwidth-bound and shape-dynamic, favouring CPU cache locality [ 13 , 17 ]. Vision encoding is static-shape and compute-dense—the NPU’s clearest sweet spot, yet the least systematically studied. Treating these phases uniformly leads to measurable waste in latency, energy, and thermal budget. Prior systems work on heterogeneous mobile inference targets text-only LLMs [ 16 , 17 ]; the vision encoding phase, its thermal behavior, and practical porting paths for unsupported encoders are not covered. We address this gap with a VLM-specific, hardware-in-the-loop study on SM8750, contributing phase-aware deployment guidance and a practical QNN porting workflow for production phone NPUs. 

Contributions: 

∙ Phase map: 1.64 × NPU prefill, 1.18 × decode, 6.41 × cold-start overhead (Sec. 3). 

∙ Thermal suite: 10.47 ∘C cooler, 2.52 × lower energy, zero throttling on NPU over 100 runs (Sec. 4). 

∙ Pipeline map: Five CPU ↔NPU configurations; W4A8 hy-brid at 97.5 tok/s (Sec. 5). 

∙ Latency knob: Token-budget sweep 0.36–0.85 s TTFS, no recompilation (Sec. 5). 

∙ Encoder scaling: Four families × CPU/GPU/NPU, 20–45 ×

CPU-to-NPU speedups (Sec. 6). 

∙ Porting methodology: Four-step rewrite, 22 × on Phi-3.5-V (Sec. 7). 

> 1
> arXiv:2606.27906v1 [cs.AR] 26 Jun 2026

Table 1: VLM inference phase decomposition and backend affinity              

> Phase Operators Intensity Backend
> Vision encode Conv, ViT-attn Very high (static) NPU Prefill GEMM, attn High (compute) NPU Decode 1-tok attn + KV Low (BW-bound) CPU ††NPU gains 1.18 ×on decode (Table 2); CPU preferred for energy efficiency in heterogeneous pipelines.

2 Platform and Experimental Setup 2.1 Hardware: Qualcomm SM8750 Snapdragon 8 Elite 

Our target is the Qualcomm SM8750 [ 12 ], a 3 nm SoC with a 64-bit Oryon CPU (4.32 GHz), LPDDR5x at 5300 MHz, Adreno GPU, and a Hexagon NPU with scalar/vector/tensor engines and VTCM scratch [ 13 ]. W4A8 INT4-weight packing halves VTCM bandwidth demand vs. INT8 [ 20 , 21 ]. Static-shape NPU paths use QNN SDK [ 14 ] via ONNX; dynamic delegation uses LiteRT [15]. 

2.2 VLM Phase Decomposition 

Following the roofline analysis framework [ 28 ], VLM in-ference separates into three hardware-distinct phases (Ta-ble 1). Prefill has high operational intensity 𝐼 = FLOPsByte due to batched GEMM and ViT attention over full image and prompt—NPU systolic engines achieve peak utilization here [ 13 , 16 ]. Decode processes one token per step while reading a growing KV cache: per-layer traffic scales as 2𝐿𝐷𝑏 

bytes, making it bandwidth-bound. The NPU delivers 1.18 ×

decode throughput; heterogeneous pipelines assign decode to CPU to maximize energy efficiency and preserve NPU capacity for the compute-intensive prefill phase [ 17 ]. Vision encoding is entirely static-shape and compute-dense, yielding the highest NPU efficiency of any VLM stage [1, 10, 11]. 

2.3 Models, Runtimes, and Protocol 

We use FastVLM-0.5B [ 1 ] as the primary model: a 0.5B-parameter VLM built on Qwen-0.5B [ 8 ] with a FastViT encoder that applies aggressive token compression for mobile execution. For encoder scaling, we additionally benchmark ViT-B/16 [ 5 ], Phi-3.5-V [ 4 ], NanoVLM [ 6 ], and MobileNetV5 Gemma3n [ 7 ]. The CPU path uses LiteRT [ 15 ] with XN-NPACK FP16. The NPU path uses LiteRT with Qualcomm AI Engine delegate [ 14 ]. The W4A8 quantized path uses QAIRT [ 14 ] INT4 weight, INT8 activation containerization for the LLM decode stage. All measurements use a fixed 64-tok text prompt, 336 ×336 image, 128-tok output; 30 runs (100 for thermal); mean ± std reported. Device: AC power, 23 ∘C, CPU governor schedutil , NPU at Sustained Performance mode. Temperature: 1 s-sampled from NPU/CPU cluster thermal zones ( /sys/class/thermal/ ); energy: fuel-gauge I×V at 100 ms granularity, capturing total SoC inference draw. 

Cache states [18 , 19 ]— S1 (cold-start): process killed, 

drop_caches=3 , QNN context cache deleted; first call reloads 

Table 2: FastVLM-0.5B phase throughput: CPU vs. NPU, SM8750, warm-cache, 64-tok prompt + 336 ×336 image, 128-tok output; mean ± std, 30 runs. †Executor init (S2): first-token overhead with model loaded and QNN context cache intact; full pipeline latencies across all cache states in Table 4.                 

> Phase Metric CPU NPU Gain
> Prefill tok/s 120 .6±4.2197 .4±1.31.64 ×
> Decode tok/s 95 .5±0.2113 .1±0.41.18 ×
> Exec. init †ms 83 .6±1.1536 .0±15 .06.41 ×

model and recompiles QNN graph (Table 4 cold). S2 (cold-cache): process restarted; model hot in page cache; QNN context cache intact; executor init on first call. S3 (warm): process alive, QNN context in VTCM, dispatch only. Table 2 reports S2 executor-init overhead; Table 4 reports full S1 end-to-end pipeline latency—each measuring a distinct, well-defined deployment scenario. 

3 End-to-End NPU vs. CPU Throughput 3.1 Phase-Level Results 

Table 2 reports throughput under warm-cache condi-tions. NPU delivers 1.64 × prefill throughput (197.43 vs. 120.64 tok/s) and 1.18 × decode throughput (113.06 vs. 95.49 tok/s). The asymmetry reflects fundamentally different operational intensity: prefill’s large batched GEMMs sat-urate the Hexagon tensor engine, while decode’s per-step KV-cache reads are memory-bandwidth-limited, with VTCM saturation increasing at longer contexts [13]. 

Prefill Decode 

0

0.5

1 

> Norm. latency (CPU = 1.0)
> CPU NPU

Figure 1: Normalized latency (CPU = 1.0) for FastVLM-0.5B. NPU wins 1.64 × on prefill and 1.18 × on decode. 

3.2 Cold-Start as a First-Class Deployment Dimension 

The NPU executor-init overhead (536 ms vs. 84 ms CPU, S2 state) is a design dimension steady-state benchmarks conceal. Persistent worker processes and model-cache pinning [ 18 , 19 ]keep the NPU executor warm, amortizing this cost and recov-ering a net 1.4–1.6 × throughput advantage [ 15 ]. Warm-state management is therefore a primary deployment objective, not an optimization detail. 3.3 Qualitative Output Consistency 

Table 3 confirms that the NPU backend produces seman-tically equivalent captions to CPU. Both backends recover identical semantic anchors (autumn park, fluffy cat, wooden bench); differences are stylistic only—NPU includes eye-color and posture details, CPU emphasizes fur texture—confirming functional multimodal equivalence under accelerator execu-tion. 

Table 3: Qualitative multimodal consistency check: shared image input, NPU vs. CPU output (FastVLM-0.5B, warm state).      

> Item Output
> Input
> NPU “The image depicts a serene park scene during what appears to be autumn. The focal point is a large, fluffy cat on a wooden park bench. . . In summary, the image captures a tranquil autumn day in a park. . . ” CPU “The image depicts a serene park scene during what appears to be autumn. The focal point is a large, fluffy cat on a wooden park bench. . . In summary, the image captures a tranquil moment in a park during autumn. . . ”

4 Thermal Characterization 4.1 100-Run Stability Suite 

Fig. 2 shows SoC temperature during sustained back-to-back inference across a 100-run stability suite. The NPU path sta-bilizes at a mean of 40.78 ±3.03 ∘C (peak 44.20 ∘C), while the CPU path reaches 51.25 ±3.62 ∘C (peak 55.30 ∘C)—a 10.47 ∘C

average gap with 5 × lower variance. Under deliberate thermal stress (15 minutes of continuous VLM inference), CPU paths reach 82 ∘C and trigger governor-driven clock reduction, while NPU paths stabilize at 66 ∘C and remain below the thermal throttle trip point (85 ∘C skin per Qualcomm thermal HAL configuration). 

0 20 40 60 80 100 

30 

40 

50 

60 

> CPU mean 51.3 ∘C
> NPU mean 40.8 ∘C

Inference run index SoC Temp. ( ∘C)  

> CPU path NPU path

Figure 2: SoC temperature across 100 back-to-back inference runs. NPU path is 10.47 ∘C cooler on average with 5 × lower variance. 

4.2 Implications for Always-On Edge Deployment 

Mobile SoC governors reduce clock non-linearly past the thermal design point, inflating CPU latency by 1.3–2.1 ×

under sustained load—invisible in short-burst benchmarks [ 2 ,3 ]. CPU paths show three latency outliers ( 𝑍 > 2.0, at runs 20, 53, 76) from governor scaling; NPU paths show zero. The NPU’s 2.52 × lower energy per request (fuel-gauge measured) directly extends always-on session duration [13]. 

5 Heterogeneous Pipeline Configurations 5.1 Five-Configuration Study 

A VLM pipeline on a heterogeneous SoC is not binary—it can assign encoder, prefill, and decode to different backends independently. Table 4 and Fig. 3 report end-to-end latency for five configurations under cold- and warm-cache conditions. 

Table 4: FastVLM-0.5B end-to-end pipeline latency (ms). E/P/D = encoder/prefill/decode backends. Cold = S1 (pro-cess fresh, drop_caches, QNN recompiled); warm = S3 (ex-ecutor+VTCM resident). Same workload as Table 2.                         

> Config EP–D Cold (ms) Warm (ms)
> CPU-only CPU CPU–CPU 4 180 3 412 NPU-only NPU NPU–NPU 26 820 2 080 Hybrid-PD CPU NPU–CPU 6 240 2 640 Hybrid-full NPU NPU–CPU 27 100 2 190 Hybrid-enc NPU CPU–CPU 5 100 3 290
> CPU-only
> NPU-only
> Hybrid-PD
> Hybrid-full
> Hybrid-enc

10 3

10 4 

> Latency (ms)
> Cold cache Warm cache

Figure 3: Pipeline latency (log scale); cold=S1, warm=S3. Recommendation: Hybrid-PD gives near-NPU warm latency (2,640 ms) with manageable cold (6,240 ms)—unlike NPU-only (26,820 ms cold). 

Under warm-cache conditions, NPU-only (2,080 ms) and Hybrid-full (2,190 ms) deliver the lowest latency. Under cold-cache, CPU-only (4,180 ms) is the fastest because warm-state transitions on the NPU amortize over subsequent requests. Hybrid-PD—offloading only the prefill stage to the NPU while the encoder and decode remain on CPU—achieves 2,640 ms warm with a manageable 6,240 ms cold, offering the best cold/warm balance for latency-sensitive deployments [18]. 

5.2 Visual Token Budget as a Latency Knob 

In the Hybrid configuration (vision encoder on NPU, lan-guage model on CPU), the image_tokens_after parameter provides a continuous latency control knob without any model 50 100 150 200 

0.4

0.6

0.8

Visual token count ( image_tokens_after )

> TTFS (s)

Figure 4: TTFS vs. token budget, W4A8 hybrid, warm-cache (S3). Sweet spot: 64 tokens = 0.497 s TTFS, 97.5 tok/s decode— half the latency of 224 tokens, no recompilation needed. 

re-export. Sweeping from 24 to 224 tokens, TTFS scales lin-early from 0.361 s to 0.850 s while decode throughput remains stable at 95–100 tok/s (Fig. 4). The W4A8-quantized decode path [ 20 ] achieves 97.5 tok/s at a TTFS of 0.497 s—a practi-cal operating point for streaming VQA workloads. This knob enables adaptive quality-latency tradeoffs at runtime without model recompilation. 

6 Vision Encoder Scaling Across Backends 6.1 Three-Backend Benchmark 

Table 5 and Fig. 5 report standalone encoder latency for four families across CPU/GPU/NPU on SM8750. FastVLM-0.5B full-pipeline characterization is the subject of Sections 3–5; the standalone encoder benchmark here isolates module-level scaling across four additional families. CPU-to-NPU speedups: 20 × (MobileNetV5) to 45 × (ViT-B/16, NanoVLM); GPU-to-NPU: 1.7–6.9 ×.

Table 5: Vision encoder module latency (ms) on SM8750, 336 ×336 input, 30-run mean; CPU=FP16/XNNPACK, GPU=FP16/Adreno-LiteRT, NPU=INT8/QNN. Isolates encoder-module scaling; full-pipeline results in Tables 2–4.                       

> Encoder CPU GPU NPU CPU/NPU (FP16) (FP16) (INT8)
> ViT-B/16 [5] 663.0 98.9 14.9 44.5 ×
> Phi-3.5-V CLIP [4] 2286.1 436.0 103.5 22.1 ×
> NanoVLM-222M [6] 638.3 98.8 14.3 44.6 ×
> MobileNetV5 Gemma3n [7] 1629.5 134.6 80.1 20.3 ×

6.2 Architectural and Deployment Insights 

Three patterns emerge across the encoder benchmark. 

Encoder choice is the dominant per-query cost lever. NPU encoder latency spans 14.3 ms (NanoVLM) to 103.5 ms (Phi-3.5-V CLIP) across the four benchmarked families—a 7× range within the same backend, larger than any single backend-switch gain. Encoder architecture selection is therefore a higher-leverage design decision than backend selection for latency-constrained mobile VLMs [2, 3]. 

GPU is not a competitive alternative to NPU for vision encoding. Across all four benchmarked families, GPU trails NPU by 1.7–6.9 ×. MLC-LLM [ 26 ] and GPU-first frameworks therefore impose a significant encoder latency penalty on Snapdragon relative to QNN-compiled NPU paths. 

> ViT-B/16
> Phi-3.5-V
> NanoVLM
> MobileNetV5

10 1

10 2

10 3  

> Latency (ms)
> CPU (FP16) GPU (FP16) NPU (INT8)

Figure 5: Vision encoder latency across three backends (log scale) for four non-FastVLM families. NPU dominates CPU by 20–45 × and GPU by 1.7–6.9 ×. FastVLM-0.5B full-pipeline timings appear in Tables 2–3. INT8 NPU quantization preserves output fidelity. We evalu-ate FastVLM-0.5B FP16 (CPU) vs. INT8 (NPU) using greedy decoding on 500 COCO val2017 images (CIDEr via pycoco-evalcap) and 200 VQAv2 open-ended questions (exact-match accuracy after lowercase normalization). CIDEr drops by 0.4 points and VQAv2 accuracy by 0.3%—within the variance of image-quality variation at mobile resolution, confirming deployment-grade equivalence. 

7 Inference Code Rewrite for Operator Support 7.1 The Porting Challenge 

Porting modern VLM encoders to QNN’s Hexagon backend requires resolving three structural adaptations between en-coder architectures and the static, integer-typed execution model of mobile NPU compilers [14, 25, 27]. 

Dynamic shapes. ViT-based encoders use symbolic batch dimensions and resolution-dependent reshapes during tok-enization. NPU compilers require fully static tensor shapes at AOT compile time; exporting at a fixed resolution (e.g., 

336 ×336 , patch size 14, yielding 576 visual tokens) and re-placing symbolic dimensions with concrete constants resolves this class of shapes for full NPU dispatch. 

Non-standard attention. Flash Attention [ 22 ] and SDPA variants use memory-layout fusions outside Hexagon’s micro-kernel set. Rewriting to explicit Q/K/V matmul sequences before ONNX export yields graphs the QNN compiler re-fuses into Hexagon-native kernels. 

Precision boundaries. Projection heads that accumulate in FP32 while the encoder body operates in INT8 require explicit Cast nodes inserted at each precision boundary via 

onnx-rewriter before conversion. Once made explicit, these boundaries become well-typed compiler barriers rather than implementation assumptions. 

7.2 Methodology and Outcomes on Phi-3.5-V 

We applied this methodology to Phi-3.5-V’s [4] CLIP-based encoder. Residual unsupported operators (custom rotary posi-tional embedding and GatherElements nodes) were handled via QNN’s partitioned-model API, which executes them on CPU while keeping 96.8% of FLOPs on Hexagon. Table 6 summarizes the progression. The adapted encoder runs at 

103.5 ms versus 2,286.1 ms CPU-only: a 22.1 × speedup that closes the gap between architecturally complex encoders and natively efficient ones like ViT-B/16. 

Table 6: Phi-3.5-V encoder porting progression on SM8750. Each row adds one rewrite step; NPU FLOP share grows mono-tonically until the model is runnable. †Intermediate shares estimated by op-count; 96.8% measured by profiler.               

> Step Action NPU FLOP share
> 0Baseline (CPU-only) 0% †
> 1Static shape export 41% †
> 2Attention decomposition 78% †
> 3Explicit cast insertion 93% †
> 4CPU fallback partition 96.8% Final latency (step 4) 103.5 ms
> Speedup vs. baseline 22.1 ×

This methodology generalizes: the same four steps apply to any encoder whose operators lie within the ViT family with standard transformer backbones [ 5, 10 , 11 ]. For newer architectures with GroupQueryAttention [ 23 ] or RoPE [ 24 ]variants, the attention decomposition step requires additional specialization, but the partitioned fallback mechanism pro-vides a practical safety net that keeps the bulk of compute on the NPU. 

8 Discussion 

Phase disaggregation is essential. The 1.64 × prefill vs. 1.18 ×

decode gap is fundamental [ 16 ]: single end-to-end numbers hide backend placement opportunities [17]. 

Encoder architecture dominates backend selection. NPU encoder latency spans 14.3–103.5 ms across four families—a 7× range larger than any backend switch gain. Treat the vision backbone as an independently profiled, swappable component [2, 3]. 

Thermal and cold-start are deployment-class effects. Both determine battery life and perceived latency in always-on scenarios [ 18 , 19 ]. The 2.52 × NPU energy advantage directly extends inference sessions. 

Token budget is a zero-cost latency knob. The 0.36–0.85 s TTFS sweep needs no recompilation—a practical quality-latency handle under thermal pressure, demonstrated on FastVLM-0.5B on SM8750. 

9 Related Work 

Mobile VLM efficiency. FastVLM [ 1 ] proposes efficient im-age tokenization via FastViT for mobile deployment. Mo-bileVLM [ 2 ] and LiteVLM [ 3 ] similarly reduce VLM cost for resource-constrained inference. Qwen2-VL [ 9 ] and Phi-3.5-V [ 4] target capable edge deployment with efficient ar-chitectures. These model-level optimizations are orthogonal to—and complementary with—our deployment characteriza-tion. 

Heterogeneous on-device inference. LLM.npu [ 16 ] and Het-eroInfer [ 17 ] characterize heterogeneous LLM inference on mobile SoCs, showing per-phase backend specialization out-performs uniform placement. Our work extends this in two directions absent from LLM-only studies: (1) we add the vision encoding phase as a distinct hardware domain, quan-tifying 20–45 × CPU-to-NPU speedups across four encoder families; (2) we provide a concrete four-step porting method-ology for encoders that fail QNN compilation out of the box—a practical gap not addressed by prior work targeting text-only LLM pipelines. 

Cold-start and quantization. NNV12 [ 18 ] and PASK [ 19 ]motivate our three-state cache taxonomy. W4A8 [ 20 ] and AWQ [ 21 ] reduce VTCM pressure, enabling 97.5 tok/s decode. FlashAttention [ 22 ] and GQA [ 23 ] are resolved in the operator adaptation step. 

Compilation and runtime toolchains. ExecuTorch [25] and MLC-LLM [ 26 ] target edge AOT export; our encoder porting methodology complements these by resolving QNN-specific operator adaptations required for Hexagon deployment. RoPE [ 24 ] embeddings require additional decomposition in our attention rewrite step. 

10 Conclusion 

We presented a VLM-specific phase characterization and de-ployment study on a production mobile NPU (Qualcomm SM8750), yielding six actionable insights: phase-dependent NPU gain (1.64 × prefill, 1.18 × decode); 10.47 ∘C thermal advantage with 2.52 × lower energy eliminating throttling; Hybrid-PD as the best cold/warm pipeline balance; a zero-recompilation TTFS knob spanning 2.4 ×; encoder archi-tecture as the dominant latency lever (20–45 × CPU-to-NPU speedups across four families); and a four-step porting methodology at 22 × on Phi-3.5-V. Together these results constitute a deployment map for heterogeneous VLM infer-ence across the latency, energy, and thermal axes that define always-on edge intelligence. 

References                                

> [1] P. Bhatt et al. , “FastVLM: Efficient vision encoding for vision language models,” CVPR , 2025. [2] X. Chu et al. , “MobileVLM: A fast, reproducible and strong vision language assistant for mobile devices,” arXiv:2312.16886 , 2023. [3] H. Yuan et al. , “LiteVLM: Lightweight vision-language models for edge deployment,” arXiv:2406.07496 , 2024. [4] Microsoft Research, “Phi-3 technical report: A highly capable language model locally on your phone,” arXiv:2404.14219 , 2024. [5] A. Dosovitskiy et al. , “An image is worth 16x16 words: Trans-formers for image recognition at scale,” ICLR , 2021. [6] H. Laurençon et al. , “NanoVLM: Efficient vision-language models you can train and deploy on the edge,” arXiv:2504.05299 , 2025. [7] Google DeepMind, “Gemma 3n: An efficient multimodal model for edge devices,” Technical Report, 2025. [8] J. Bai et al. , “Qwen technical report,” arXiv:2309.16609 , 2023. [9] P. Wang et al. , “Qwen2-VL: Enhancing vision-language model’s perception of the world at any resolution,” arXiv:2409.12191 ,2024. [10] A. Radford et al. , “Learning transferable visual models from natural language supervision,” ICML , 2021. [11] X. Zhai et al. , “Sigmoid loss for language image pre-training,”
> ICCV , 2023. [12] Qualcomm Technologies, Inc., “Snapdragon 8 Elite mobile plat-form product brief,” 2024. [13] Qualcomm Technologies, Inc., “Unlocking on-device generative AI with an NPU and heterogeneous computing,” 2024.

[14] Qualcomm Technologies, Inc., “Qualcomm AI Engine direct: QNN SDK documentation,” 2024. [15] Google, “LiteRT: Lightweight runtime for on-device inference,” https://ai.google.dev/edge/litert, 2024. [16] D. Xu et al. , “Fast on-device LLM inference with NPUs,” ASP-LOS , 2025. [17] L. Chen et al. , “Characterizing mobile SoC for accelerating het-erogeneous LLM inference,” SOSP , 2025. [18] Z. Cao et al. , “NNV12: An efficient on-device inference framework for mobile vision tasks,” MobiSys , 2024. [19] H. Liu et al. , “PASK: Preloaded-and-shared kernel for fast on-device DNN inference,” MobiCom , 2024. [20] T. Dettmers et al. , “LLM.int8(): 8-bit matrix multiplication for transformers at scale,” NeurIPS , 2022. [21] J. Lin et al. , “AWQ: Activation-aware weight quantization for LLM compression and acceleration,” MLSys , 2024. [22] T. Dao et al. , “FlashAttention: Fast and memory-efficient exact attention with IO-awareness,” NeurIPS , 2022. [23] J. Ainslie et al. , “GQA: Training generalized multi-query trans-former models from multi-head checkpoints,” EMNLP , 2023. [24] J. Su et al. , “RoFormer: Enhanced transformer with rotary posi-tion embedding,” Neurocomputing , 2024. [25] Meta AI, “ExecuTorch: On-device AI across mobile, embedded and edge for PyTorch,” https://pytorch.org/executorch/, 2024. [26] T. Chen et al. , “MLC-LLM: Universal LLM deployment engine with ML compilation,” arXiv:2404.09542 , 2024. [27] Google, “TensorFlow Lite delegates,” https://www.tensorflow. org/lite/performance/delegates, 2024. [28] S. Williams et al. , “Roofline: An insightful visual performance model for multicore architectures,” CACM , 52(4):65–76, 2009.