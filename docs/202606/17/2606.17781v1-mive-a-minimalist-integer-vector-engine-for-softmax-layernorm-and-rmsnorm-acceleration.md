---
title: "MIVE: A Minimalist Integer Vector Engine for Softmax LayerNorm and RMSNorm Acceleration"
title_zh: MIVE：用于Softmax、LayerNorm和RMSNorm加速的最小化整数向量引擎
authors: "Kosmas Alexandridis, Giorgos Dimitrakopoulos"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17781v1"
tags: ["query:edge-llm"]
score: 9.0
evidence: 面向LLM向量运算的专用硬件加速器
tldr: 大语言模型推理中，Softmax、LayerNorm和RMSNorm等非线性向量运算成为硬件瓶颈。现有加速器为各操作配备专用计算单元，导致硅面积浪费和资源重复。本文提出MIVE，一种可编程整数向量引擎，通过挖掘三者共有的计算模式，在统一数据路径中实现高效执行。ASIC实现结果显示，MIVE在支持多种操作的同时，其面积效率和硬件效率均优于现有主流专用加速器。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17781v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1880, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17781v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 874, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17781v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 379, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17781v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 924, \"height\": 538, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17781v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1344, \"height\": 464, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17781v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 927, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有LLM加速器为Softmax、LayerNorm、RMSNorm分别设计专用硬件，导致资源重复和硅利用率低下。
method: 提出MIVE，一种可编程整数向量引擎，利用三者共同计算模式，在统一数据路径中执行所有操作。
result: 物理ASIC实现表明，MIVE在提供多操作支持的同时，面积和硬件效率优于多数现有独立加速器。
conclusion: MIVE通过硬件共享降低实现开销，为LLM推理提供了高效的多功能向量加速方案。
---

## 摘要
大规模语言模型（LLMs）的快速增长加剧了对专用硬件加速器的需求，这些加速器必须满足严格的推理延迟和功耗限制。尽管矩阵乘法主导了整体计算工作负载，但非线性向量归一化操作（如LayerNorm、RMSNorm和Softmax）可能成为关键的硬件瓶颈。现有的加速器通常使用专用的硬件模块来实现这些功能，导致资源重复和硅利用率低下。为解决这一问题，我们提出了一种最小化整数向量引擎（MIVE），这是一种可编程架构，能够在统一的数据通路中执行所有三种操作。通过利用LayerNorm、RMSNorm和Softmax之间的共同计算模式，所提出的向量引擎最大化了硬件共享，同时减少了实现开销。物理ASIC实现结果表明，MIVE提供了全面的多函数支持，同时在面积和硬件效率方面优于大多数最先进的独立加速器。

## Abstract
The rapid growth of Large Language Models (LLMs) has intensified the need for specialized hardware accelerators that can satisfy stringent inference latency and power constraints. Although matrix multiplications dominate the overall computational workload, non-linear vector normalization operations, such as LayerNorm, RMSNorm and Softmax can become critical hardware bottlenecks. Existing accelerators typically implement these functions using dedicated hardware blocks, leading to duplicated resources and inefficient silicon utilization. To address this limitation, we propose a Minimalist Integer Vector Engine (MIVE), a programmable architecture capable of executing all three operations within a unified datapath. By exploiting common computational patterns across LayerNorm, RMSNorm and Softmax the proposed vector engine maximizes hardware sharing while reducing implementation overhead. Physical ASIC implementation results show that MIVE provides comprehensive multi-function support while achieving higher area and hardware efficiency than most state-of-the-art standalone accelerators.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：大语言模型（LLM）推理中，非线性向量归一化操作（Softmax、LayerNorm、RMSNorm）虽计算量远小于矩阵乘法，却因依赖指数、平方、开方、累加等昂贵操作而成为性能和能效瓶颈。现有硬件加速器通常为每种函数设计专用模块，导致硅面积重复、资源利用率低下。
- **整体含义**：本文旨在通过挖掘三种操作的计算共性，设计统一可编程整数向量引擎，在共享数据通路上高效执行Softmax、LayerNorm和RMSNorm，以降低硬件开销并提升面积效率，为LLM推理提供更紧凑的加速方案。

## 2. 方法论：核心思想、关键技术细节、算法流程
- **核心思想**：将LayerNorm、RMSNorm、Softmax分解为一组通用的**原始操作**，包括：
  - **元素级操作**：加减、平方、乘加（映射到统一 `muladd` 单元）
  - **向量级归约**：求和、求最大值、求均值（映射到统一 `vecsum` 单元）
- **关键技术细节**：
  - 采用**INT8整数运算**，配合定点表示和分段线性（PWL）近似实现指数、倒数、倒数平方根等非线性函数。
  - 设计**ISA级接口**，指令直接驱动数据通路信号，避免复杂译码逻辑。
  - 向量元素级计算由并行`muladd`阵列完成；标量校正由独立`muladd`单元执行；归约由二叉树`vecsum`单元完成。
  - 通过**迭代子向量处理**和**校正算法**（如LayerNorm的并行方差更新、Softmax的指数和重缩放）支持任意长度输入向量。
- **算法流程（文字说明）**：
  - LayerNorm：两次遍历——第一次累加均值与平方和，同时更新运行均值和方差；第二次用全局校正后的均值和归一化因子缩放并添加学习参数。
  - RMSNorm：一次归约平方和，随后PWL求倒数平方根，最后缩放加参数。
  - Softmax：一次归约求全局最大值并累加指数和（含重缩放校正），第二次遍历用全局最大值偏移、PWL指数、归一化。
  - 校正算法（LNC、SMC）均利用`muladd`单元完成标量仿射运算。

## 3. 实验设计：数据集、场景、Benchmark、对比方法
- **数据集/场景**：
  - **硬件评估**：28nm标准单元工艺，2GHz时钟频率，向量长度L=8。
  - **LLM精度评估**：使用两个模型及数据集：
    - OPT-30B（含Softmax+LayerNorm）在LAMBADA数据集上测量准确率；
    - Llama2-7B（含RMSNorm）在Wikitext数据集上测量困惑度。
- **Benchmark**：不与基线对比精度退化，而是独立测试FP16准确模型 vs. INT8 + MIVE近似量化模型。
- **对比方法**：硬件复杂度对比：
  - Spagnolo et al. [13] (Softmax-only, 28nm, INT16)
  - Kim et al. [14] (Softmax-only, 28nm, INT16)
  - Softmax Shao et al. [4] (Softmax-only, 45nm, INT16)
  - SOLE [9] (LayerNorm-only, 28nm, INT12)
  - SOLE [8] (Softmax+LayerNorm, 28nm, INT8)
  - 对比指标：面积(μm²)、功耗(mW)、吞吐量(GOPS)、面积效率(GOPS/mm²)、功率效率(GOPS/mW)、支持功能。

## 4. 资源与算力
- 论文**未明确说明**使用GPU/TPU训练或推理算力（如GPU型号、数量、训练时长），仅针对硬件加速器实现和端到端LLM推理测试。
- 硬件实现使用**28nm CMOS工艺**，目标频率**2GHz**，向量长度L=8。
- 功耗测量基于**lm-evaluation-harness框架**，在多种benchmark上运行取平均值。

## 5. 实验数量与充分性
- **硬件综合实验**：一组完整ASIC物理实现（布局如Fig.3），对比5个现有工作，表格列出面积、功耗、吞吐量、效率。
- **精度实验**：两组LLM模型各一个数据集，分别展示准确率（OPT-30B/LAMBADA）和困惑度（Llama2-7B/Wikitext）的退化情况（FP16 vs INT8+MIVE）。
- **充分性评价**：
  - 硬件对比较充分，涵盖了主流独立加速器，但缺乏与同样支持三种操作的统一加速器对比（可能是因为当前领域尚无此类工作）。
  - 精度评估仅两个模型、两个数据集，样本偏少，未消融不同量化设置或PWL分段数量影响。
  - 未讨论不同向量长度L或工艺节点对结果的影响。
- 整体实验设计**基本客观**，但**验证充分性有限**，可进一步扩展更多模型和任务（如翻译、摘要）。

## 6. 主要结论与发现
- MIVE在**支持全部三种归一化操作**的同时，实现了**332.6 GOPS/mm²的面积效率**，优于SOLE [8]（2.1倍）、Kim [14]（4.1倍）等，并在功率效率（1.7 GOPS/mW）上领先。
- 相比仅支持部分函数的加速器，MIVE通过硬件共享节省了面积和功耗。
- 对LLM模型精度的影响**极小**：OPT-30B准确率仅下降1%（81%→80%），Llama2-7B困惑度上升0.2（5.8→6.0），证明PWL近似在INT8量化下可忽略模型退化。

## 7. 优点
- **创新性**：首次系统地分解Softmax、LayerNorm、RMSNorm的共性，设计统一可编程整数向量引擎，避免功能冗余。
- **硬件高效性**：仅使用`muladd`和`vecsum`两类基本单元，控制逻辑简单，可扩展性强。
- **实用性**：面向INT8量化LLM推理，与现代加速器范式兼容，实际精度损失可接受。
- **物理实现**：基于28nm工艺完成完整ASIC布局，数据真实可信。

## 8. 不足与局限
- **实验覆盖不足**：仅测试两个LLM模型（OPT-30B、Llama2-7B），未在更多模型（如GPT系列、BERT）或更复杂任务（如对话、翻译）上验证。
- **消融分析缺失**：未讨论PWL分段数、整数位宽、校正精度对性能和准确率的影响。
- **对比公平性**：对比方法采用不同工艺节点（45nm vs 28nm）和数据精度（INT12 vs INT8），直接比较面积效率可能存在偏差；且未考虑计入片上内存、控制逻辑等开销的公平归一化。
- **应用限制**：仅支持INT8定点精度，若需更高精度需额外扩展；向量长度L固定为8，扩展至更大L需重新评估树形归约延迟。
- **未讨论功耗分解**：未报告各单元（muladd阵列、ROM、控制）功耗占比，难以定位瓶颈。

（完）
