---
title: "dMX: Differentiable Mixed-Precision Assignment for Low-Precision Floating-Point Formats"
title_zh: dMX：面向低精度浮点格式的可微分混合精度分配
authors: "Giuseppe Franco, Ian Colbert, Pablo Monteagudo-Lago, Felix Marty, Nicholas Fraser"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.04115v1"
tags: ["query:edge-llm"]
score: 8.0
evidence: 面向LLM的可微混合精度量化框架，支持可学习的比特宽度分配，契合软硬协同设计
tldr: 大语言模型量化中统一位宽分配次优，本文提出dMX可微分混合精度框架，将每层浮点格式参数化为连续标量，通过温度退火实现离散化，并用目标正则化控制平均位宽。在Llama、Qwen3等模型上，dMX持续产出帕累托主导模型，在困惑度和零样本推理上优于KL启发式方法，有效平衡了模型质量与部署效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04115v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1439, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04115v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04115v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1476, \"height\": 949, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04115v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 975, \"height\": 444, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04115v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1475, \"height\": 946, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04115v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1407, \"height\": 1003, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04115v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1472, \"height\": 952, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04115v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1478, \"height\": 950, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04115v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 961, \"height\": 869, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04115v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 961, \"height\": 778, \"label\": \"Table\"}]"
motivation: 统一位宽量化LLM效果欠佳，需自动学习每层最优浮点格式以提升性能与效率的权衡。
method: 将每层浮点位宽参数化为连续标量，用温度退火渐进离散化，并引入目标正则化约束平均位宽。
result: 在Llama、Qwen3等模型上，dMX的困惑度和零样本推理准确度均达到帕累托最优，优于KL启发式基线。
conclusion: dMX能高效搜索硬件兼容的混合精度配置，在模型质量与位宽间取得优异权衡。
---

## 摘要
将大型语言模型（LLM）量化到低精度浮点表示是高效部署的关键，然而在所有的层上统一应用单一比特宽度在性能和精度上都不是最优的。本文提出了dMX，一个可学习的浮点位宽分配的可微分混合精度量化框架。我们研究了其在开放计算项目（OCP）标准定义的微缩放浮点（MXFP）数据类型族上的应用。逐层的位宽分配被形式化为一个连续优化问题，其中每一层的浮点格式通过一个标量参数进行参数化，将多变量设计空间折叠为一个可学习的偏移量。在训练过程中，该偏移量取连续值，避免了离散量化格式之间的突然振荡。基于温度退火调度逐渐离散化学习到的偏移量，确保最终配置映射到硬件兼容的MXFP格式，训练和推理行为之间不会出现突变。目标感知正则化项将平均位宽导向用户指定的预算，作为推理成本的粗粒度代理，平衡模型质量与部署效率。我们在不同的LLM家族上进行了实验，如Llama、Qwen3和SmolLM2，在WikiText-2上评估困惑度，并在四个零样本推理基准上评估准确率。在这些设置下，dMX一致地产生帕累托主导模型，并优于基于KL散度的层选择启发式方法，有效地导航模型质量与平均位宽之间的权衡。

## Abstract
Quantizing large language models (LLMs) to low-precision floating-point representations is central to efficient deployment, yet applying a single bit-width uniformly across all layers is sub-optimal in terms of both performance and accuracy. This work introduces dMX, a differentiable mixed-precision quantization framework for learnable floating-point bit-width assignment. We study its application for the microscaling floating-point (MXFP) family of data types defined by the Open Compute Project (OCP) standard. The per-layer bit-width assignment is formulated as a continuous optimization problem in which each layer's floating-point format format is parameterized by a scalar parameter, folding the multi-variate design space into a single learnable offset. During training this offset takes continuous values, avoiding sudden oscillations between discrete quantization formats. A temperature-based annealing schedule progressively discretizes the learned offsets, ensuring that the final configuration maps to hardware-compatible MXFP formats without abrupt transitions between training and inference behavior. A target-aware regularization term steers the average bit-width toward a user-specified budget, serving as a coarse-grained proxy for inference cost and balancing model quality against deployment efficiency. We performed experiments on different families of LLM, such as Llama, Qwen3, and SmolLM2, evaluating perplexity on WikiText-2 and accuracy on four zero-shot reasoning benchmarks. Across these settings, dMX consistently yields Pareto-dominating models and improves over Kullback-Leibler (KL) divergence-based layer-selection heuristics, efficiently navigating trade-offs between model quality and average bit-width.

---

## 论文详细总结（自动生成）

# 论文总结：dMX: Differentiable Mixed-Precision Assignment for Low-Precision Floating-Point Formats

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：将大型语言模型（LLM）部署到边缘或云端需要大幅降低内存和计算开销，量化是主要手段。但当前业界广泛使用的浮点格式（如MXFP8、MXFP6、MXFP4）若在全网所有层上使用统一位宽，会导致显著的精度损失或效率低下。逐层分配不同位宽（混合精度量化）是一个组合优化难题，传统启发式方法（如基于KL散度的贪婪选择）难以捕捉跨层误差累积，且仅针对整数格式。
- **整体含义**：本文提出dMX——首个针对浮点格式（特别是OCP MX标准下的MXFP格式族）的**可微分混合精度量化框架**，将每层浮点格式的位宽参数化为一个连续标量，通过梯度下降端到端优化，最终收敛到硬件兼容的离散配置，实现模型质量与推理效率的最佳权衡。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将每层的浮点格式（MXFP8、MXFP6、MXFP4）用一个共享偏移量 β 参数化，定义格式为 `E(2+β)M(1+β)`。β 在 [0,2] 区间内连续取值，训练时使用连续值避免离散跳变，通过温度退火逐渐将 β 硬化到两个离散端点（β=0 → MXFP4，β=2 → MXFP8或MXFP6）。
- **关键技术细节**：
  - **连续浮点位宽参数化**：量化公式（缩放、取整、截断）在连续 e,m 下保持可微，梯度通过直通估计器（STE）传播。
  - **温度退火离散化**：使用 `β = F(β̂, T) = ½ + e^{-T·(β̂-0.5)}` 的 sigmoid 映射，T 从低到高指数增长，初始为近似线性，后期变为阶跃函数，确保最终映射到硬件支持的离散格式。
  - **正则化项**：两个选项——简单缩放惩罚（`R⨛ = λ·avg_width`）和目标感知惩罚（`R⨛ = max(0, λ·(avg_width - target_width))`）。后者可精确控制平均位宽。
  - **模型级平均位宽**：支持简单平均和按张量元素数加权的加权平均，后者更符合实际内存成本。
- **算法流程**（见论文图2）：预训练LLM每层对应一个可学习偏移 β_i；前向时通过温度退火函数 F 将连续 β 映射到离散格式；反向传播同时更新 β 和权重；温度随训练步数增加，逐步硬化；最终所有 β 收敛到离散值，得到混合精度MXFP模型。

## 3. 实验设计

- **模型**：Llama 3.2 1B、Qwen3 1.7B、SmolLM2 1.7B。
- **数据集**：
  - 校准数据：FineWeb 数据集中的 3200 个样本（batch size = 8，共 400 步优化）。
  - 评估：WikiText-2 困惑度；四个零样本推理基准（ARC-Challenge, ARC-Easy, HellaSwag, WinoGrande），使用 LightEval 框架。
- **基准（Baseline）**：
  - 全精度 BF16 模型。
  - 均匀量化配置：全 MXFP4、全 MXFP6、全 MXFP8。
  - 对比方法：KL散度预选层精度（贪婪启发式）、离散化版本（Round + STE）。
- **实验设置**：
  - 格式对：MXFP8/MXFP4 和 MXFP6/MXFP4 混合精度配置。
  - 初始所有层为 MXFP4。
  - 超参数：温度从 8 到 400 指数增长，预热比例 Tratio=60%；正则化 λ 固定为 5（目标感知），目标位宽从 4.1 到 8 取 17 个点；位宽优化使用 SGD 学习率 1.1。

## 4. 资源与算力

- 论文中未明确说明所使用的 GPU 型号、数量及训练总时长。仅在实验设置中提到训练步数（400步）、校准样本数（3200）、以及使用了 SpinQuant 类似的训练基础设施（但没给出具体硬件规格）。

## 5. 实验数量与充分性

- **实验组数**：相当充分。主实验覆盖了 3 个模型 × 2 种格式对 × 多个目标位宽（约4~6个点） → 至少 30+ 组对比点；消融实验包括：
  - 正则化函数对比（目标感知 vs 简单缩放）
  - 平均位宽计算方式（简单平均 vs 加权平均）
  - 连续表示 vs 离散（Round+STE）对比
  - 学习分配 vs KL预选对比
- **充分性**：实验覆盖了不同模型族、不同格式对、不同位宽区间，且提供了完整的趋势图，Pareto 曲线清晰；消融实验针对每个设计选择都给出了定量比较。整体设计客观、公平。

## 6. 主要结论与发现

- dMX 在所有三个模型上均持续产生**帕累托主导模型**：即相同平均位宽下困惑度更低、零样本准确率更高，优于均匀量化和 KL 启发式方法。
- 在中位平均位宽（5.0~7.0）区间，dMX 优势最明显，因为该区间需要精细分配预算，而 KL 启发式在该区间性能平坦。
- MXFP6/MXFP4 配置比 MXFP8/MXFP4 在相同位宽下表现更好（更位宽高效）。
- 连续表示相比 Round+STE 离散方式更稳定，能更好匹配目标位宽，取得更低困惑度。
- 目标感知正则化比简单缩放提供更可控、分布均匀的位宽点。
- 简单平均与加权平均正则化的最终性能重叠，但简单平均倾向于达到更低困惑度。

## 7. 优点

- **首次针对浮点格式的梯度式位宽学习**，填补了 MX 格式族下可微分混合精度的空白。
- **参数化巧妙**：用一个连续偏移量 β 参数化整个格式，压缩了搜索空间，且量化的数学保持可微。
- **温度退火机制**：从连续到离散的平滑过渡，避免了训练时频繁的格式切换带来的不稳定。
- **正则化设计**：目标感知惩罚允许用户直接指定目标平均位宽，实用性强。
- **兼容性好**：可与现有 PTQ 方法（如 SpinQuant 的旋转优化）结合，提供端到端框架。
- **实验覆盖面广**：包括多种模型、格式对、消融、对比方法，图表完整，结论可靠。

## 8. 不足与局限

- **模型规模**：实验限于 1–2B 参数模型，未验证更大模型（如 7B、13B 及以上）或混合专家模型（MoE），其中每层敏感度可能差异更大。
- **超参数调谐**：温度调度、预热比例、学习率等未做系统搜索，论文提到“相对稳健但更仔细的搜索可能带来进一步提升”。
- **未考虑激活量化**：权重和激活被绑定为相同位宽，但实际中激活可能需要不同位宽或动态量化。
- **计算资源不明**：未给出具体 GPU 型号、数量、训练耗时，可复现性信息不足。
- **只支持 OCP MX 格式族**：虽针对 MX 设计，但方法可能可推广到其他浮点格式（如 FP8 E4M3/E5M2），论文未讨论扩展性。
- **评估仅限推理精度**：未报告实际的推理速度、内存占用或延迟等硬件效率指标。

（完）
