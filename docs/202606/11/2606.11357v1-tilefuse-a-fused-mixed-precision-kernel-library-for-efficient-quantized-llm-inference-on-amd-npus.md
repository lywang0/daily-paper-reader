---
title: "TileFuse: A Fused Mixed-Precision Kernel Library for Efficient Quantized LLM Inference on AMD NPUs"
title_zh: "TileFuse: 一种用于AMD NPU上高效量化LLM推理的融合混合精度内核库"
authors: "Wesley Pang, Gregory Hyegang Jun, Feiyang Liu, Deming Chen"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11357v1"
tags: ["query:edge-llm"]
score: 10.0
evidence: 面向NPU的混合精度内核库，LLM推理的异构计算运行时
tldr: "当前边缘设备上的LLM推理面临量化格式与NPU软件栈不匹配的问题，导致AWQ等高效量化方案难以直接部署。针对AMD XDNA2 NPU，本文提出TileFuse混合精度内核库，通过协同设计权重布局、元数据放置和融合解包、反量化与GEMM/GEMV执行的微内核，实现AWQ风格的W4A16和W8A16量化推理。在Ryzen AI笔记本上，TileFuse相较于全精度基线实现最高121.6%的GEMM性能提升和281%的GEMV性能提升，端到端预填充延迟降低2.0倍，能耗降低64.6%。该工作证明了XDNA2是实用的边缘LLM量化推理平台，使NPU对主流量化格式的原生支持更具可行性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11357v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 588, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 854, \"height\": 1545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1473, \"height\": 594, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 868, \"height\": 744, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1805, \"height\": 428, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 850, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 893, \"height\": 991, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 880, \"height\": 290, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 861, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1816, \"height\": 357, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11357v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1816, \"height\": 376, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11357v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11357v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1267, \"height\": 1684, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11357v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 886, \"height\": 198, \"label\": \"Table\"}]"
motivation: 现有NPU软件栈对AWQ等通用量化格式支持不足，迫使模型适配专有方案，限制了NPU在边缘LLM推理中的实用性和能效。
method: TileFuse融合解包、反量化与GEMM/GEMV执行，采用交错的预分块布局和密集GEMV数据流，利用全4x8 AIE阵列实现混合精度计算。
result: "GEMM性能提升达121.6%，GEMV性能提升达281%，端到端预填充延迟降低2.0倍，能耗降低64.6%，超过强iGPU基线2倍以上。"
conclusion: TileFuse表明XDNA2可以高效支持AWQ风格量化推理，使NPU更易于在真实客户端部署中发挥能效优势。
---

## 摘要
随着设备端LLM推理需求的增长，边缘SoC越来越多地集成NPU，以在严格的功耗和热预算下提升性能和能效。然而，当前客户端NPU上的实际LLM部署仍然困难：广泛使用的量化格式（如AWQ）无法干净地映射到许多现有的NPU软件栈上，这些软件栈通常是专有的，并且暴露的底层控制有限。在本工作中，我们提出了TileFuse，这是一个面向AMD XDNA2 NPU的近硬件混合精度内核库，针对量化LLM推理中的Transformer线性层。TileFuse将实用的低位格式（如AWQ风格的W4A16和W8A16）直接引入XDNA2，而不是迫使模型围绕NPU特定的量化方案进行重塑。TileFuse协同设计了权重布局、元数据放置、混合精度微内核和阵列级数据流。具体来说，它将解包、反量化和GEMM/GEMV执行融合到单个内核流中，引入了一种支持高达32K GEMM维度的交错预分块布局，并重新设计了GEMV数据流以充分利用4x8 AIE阵列。在内核级评估中，与全精度基线相比，TileFuse对GEMM的性能提升高达121.6%，对GEMV的性能提升高达281%，同时在GEMM上比强iGPU基线获得了超过2倍的性能和能效提升。在Ryzen AI笔记本电脑的端到端LLM实验中，TileFuse实现了高达2.0倍的预填充延迟降低，能耗降低超过64.6%。这些结果共同表明，XDNA2是AWQ风格边缘LLM推理的实用目标，对现成量化的原生NPU支持可以使NPU在实际客户端部署中更加可用。

## Abstract
With the growing demand for on-device LLM inference, edge SoCs increasingly integrate NPUs to improve performance and energy efficiency under tight power and thermal budgets. However, practical LLM deployment on current client NPUs remains difficult: widely used quantization formats such as AWQ do not map cleanly onto many existing NPU software stacks, which are often proprietary and expose limited low-level control. In this work, we present \textit{TileFuse}, a close-to-metal mixed-precision kernel library for AMD XDNA2 NPUs that targets transformer linear layers in quantized LLM inference. TileFuse brings practical low-bit formats such as AWQ-style W4A16 and W8A16 directly onto XDNA2, rather than forcing the model to be reshaped around an NPU-specific quantization scheme.   TileFuse co-designs weight layout, metadata placement, mixed-precision microkernels, and array-level dataflow. Specifically, it fuses unpacking, dequantization, and GEMM/GEMV execution into a single kernel flow, introduces an interleaved pre-tiling layout that supports GEMM dimensions up to 32K, and redesigns GEMV dataflow to utilize the full 4x8 AIE array. Across kernel-level evaluations, TileFuse improves performance by up to 121.6% for GEMM and 281% for GEMV over full-precision baselines, while delivering more than 2x performance and energy-efficiency gains over strong iGPU baselines on GEMM. In end-to-end LLM experiments on Ryzen AI laptops, TileFuse achieves up to 2.0x lower prefilling latency with more than 64.6% lower energy consumption. Together, these results show that XDNA2 is a practical target for AWQ-style edge LLM inference and that native NPU support for off-the-shelf quantization can make NPUs substantially more usable in real client deployments.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：随着大语言模型（LLM）在边缘设备上的需求激增，系统级芯片（SoC）厂家越来越多地集成神经网络处理单元（NPU）以提高性能和能效。然而，主流NPU软件栈（如AMD的ONNX Runtime、Apple的Core ML、Intel的OpenVINO、Qualcomm的QNN）通常是专有的，且仅暴露高层部署接口，缺乏底层编程控制。这导致广泛使用的量化格式（如AWQ风格的W4A16）无法直接映射到NPU上，用户被迫让模型适配NPU专有量化方案，增加了部署难度，也限制了NPU的实用性和能效优势。

- **整体含义**：本文旨在解决NPU对现成量化格式（特别是AWQ）的“原生支持缺失”问题，通过为AMD XDNA2 NPU设计一套近硬件（close-to-metal）的混合精度内核库TileFuse，将解包、反量化、GEMM/GEMV执行融合为单一内核流，并协同优化权重布局、元数据放置和数据流，从而实现高效、可移植的量化LLM推理。该工作表明，NPU的可编程性（如XDNA2）足以支持主流量化格式，使NPU成为边缘设备上更具吸引力的LLM加速器。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：TileFuse的核心是将低位权重（INT4/INT8）直接从压缩格式消费，在计算核内完成“解包→反量化→矩阵乘法”的融合流程，避免中间BF16权重的全局内存往返。同时，通过离线预分块（pre-tiling）将量化元数据（scale/zero-point）与权重打包，并通过交错列主序布局解决大数据流中DMA步幅受限的问题；针对GEMV阶段，重新设计数据流以利用全4×8 AIE阵列。

- **关键技术细节**：
  - **融合微内核**：在AIE计算核内，对INT4权重先进行位掩码和移位解包为INT8，再转换为BF16并应用scale/zero-point反量化（W4A16为不对称，W8A16为对称），然后使用BF16 8×8矩阵乘原语执行GEMM/GEMV。反量化后的BF16权重存储在本地缓冲中，以便在后续激活切片中重用，避免重复反量化。
  - **权衡**：W4A16带宽更小但解包反量化更复杂；W8A16带宽更大但反量化简单（仅scale）。性能取决于算术强度与内存行为。
  - **交错预分块布局**：为解决LLM MLP层中大权重矩阵（如K,N>8K）因DMA步幅限制而需要多次分块执行的问题，提出离线交错列主序排列：将分配给同一AIE列的权重块在内存中连续放置，从而支持最大32K的GEMM维度。
  - **GEMV数据流重设计**：基线仅使用一行计算核（8个核），利用率低。优化后利用内存核作为中间分发层，将每个shim核传来的四块权重分发给同一列的四个计算核，激活向量广播至所有核，从而让全部32个计算核并行工作，提升计算吞吐。

## 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法

- **数据集/场景**：
  - 内核级评估：使用合成GEMM和GEMV操作，扫查M维度（256~8192），固定K/N为4K×14K或14K×4K，模拟LLM常见线性层形状。
  - 端到端评估：使用三个主流LLM——Llama-3-8B、Gemma-2B、Qwen2.5-3B，均采用AWQ W4A16量化（组大小128）和W8A16（每通道对称量化）。
  - 测试平台：两台Ryzen AI笔记本（Ryzen AI 7 350 [Krackan Point] 与 Ryzen AI 9 HX 370 [Strix Point]），分别集成8核与16核iGPU，NPU同为XDNA2。

- **基准对比**：
  - 基线：llama.cpp在iGPU上的Q4_K和Q8_0融合量化内核（2026年1月更新版，性能有所提升）；CPU版本也做了对比。
  - 未使用专有NPU推理框架（如ONNX Runtime），因为其不支持AWQ格式。
  - 与其他NPU工作（如FastFlowLM、ScalingLLM）的吞吐量对比（1024 tokens预填充）。

- **测量指标**：
  - 内核级：TOPS（万亿次操作/秒），GOPs（GEMV）。
  - 端到端：预填充延迟（TTFT）、生成吞吐量（tok/s）、能耗（J）、电流迹线。

## 4. 资源与算力

- **算力描述**：论文未明确给出GPU型号、数量或训练时长等传统算力指标。实验在商用Ryzen AI笔记本上运行，利用XDNA2 NPU（32个AIE核，4×8布局）和集成RDNA iGPU（8或16 CU）。没有使用独立GPU或大规模集群。
- **运行时**：内核执行在CPU+NPU+iGPU混合环境中，无训练过程，仅推理。文中提到NPU内核调度和重构开销在毫秒级，但对于小算子（GEMV）显著影响性能。

## 5. 实验数量与充分性

- **实验数量**：
  - 内核GEMM：两个平台×两种量化格式（W4A16/W8A16）×两种矩阵形状（4K×14K / 14K×4K）×M从1024到8192共约8个点 = 约64组性能曲线。
  - 内核GEMV：表1列出四个平台×两种精度（4/8 bit）= 8组数字。
  - 端到端预填充：表2覆盖3个模型×2种精度×2个平台×6种prompt长度（256-8192）= 72组延迟数据；图7为图形化展示。
  - 端到端生成：图8仅显示三个模型×两个平台的W4A16生成吞吐（共6组柱状图）。
  - 能耗：图9仅展示Llama3-8B 4096-token预填充场景下的电流迹线（4条曲线）。
  - 消融实验：图10、图11展示不同GEMM/GEMV尺寸下，逐步添加预分块、混合精度、微内核/数据流优化对性能的影响，横跨多种M/N值。
  - 跨工作对比：表3对比了FastFlowLM和ScalingLLM的生成吞吐（2组）。

- **充分性与公平性**：
  - **优点**：实验覆盖了内核级和端到端，包含性能、能耗、消融，对比了不同精度、不同平台、不同基线，且对比的iGPU基线（llama.cpp）是当前AMD iGPU上最高性能的量化内核之一，因此对比公平。
  - **不足**：
    - 生成阶段性能仅用吞吐量柱状图展示，缺乏prompt长度变化；且未与iGPU基线做详细的逐token延迟对比。
    - 能耗测量仅在一个具体场景（预填充4096 tokens）下进行，未覆盖生成阶段或其他模型。
    - 消融实验只在一个平台上（Krackan Point）展示了全面的GEMV消融，另一个平台（Strix Point）仅提供GEMM消融（图10b缺少GEMV微内核优化？实际上图11b是Strix Point的GEMV消融，但只显示baseline+pre-tiling+mixed，缺少微内核/数据流优化线？检查图11b确实缺少后两根线，因为平台不支持？文中提到Strix Point上GEMV优化效果有限，但未做详细解释。这是一个不足）。
  - **潜在偏差**：实验中NPU端只使用TileFuse实现的混合精度内核，而iGPU端使用llama.cpp的Q4_K/Q8_0，两者量化格式不同（虽然是同等级4-bit/8-bit），可能引入细微差异。此外，NPU端仅执行线性层，其余操作（FlashAttention等）仍在iGPU上运行，因此端到端性能部分受到iGPU影响，不单纯是NPU的效果。

## 6. 论文的主要结论与发现

- **内核性能**：
  - GEMM：TileFuse混合精度内核相比全精度BF16基线，性能提升最高121.6%；在Ryzen AI 7 350上达到12 TOPS峰值，超过iGPU（5-7 TOPS）2倍以上；并且支持高达32K的GEMM维度，而基线限制在8K。
  - GEMV：通过微内核扩宽（64×8权重块）和全数组数据流，混合精度+优化后相比全精度基线提升281%；但在绝对吞吐上仍低于CPU和iGPU（因为NPU更擅长计算而非低延迟流式处理）。

- **端到端性能**：
  - 预填充（W4A16）：在Ryzen AI 7 350上，NPU路径在中等以上prompt长度（512+）均优于iGPU，最大加速比2.0×（Llama3-8B 2048 tokens）；在Ryzen AI 9 HX 370上加速比略低（最大1.22×），因为该平台NPU内核性能较弱而iGPU更强。
  - 生成吞吐：NPU路径的生成吞吐普遍低于iGPU（~7-19 tok/s vs. ~30-38 tok/s），因为GEMV的延迟对NPU调度开销敏感，且MLP维度非齐整时padding导致过计算。

- **能耗**：预填充4096 tokens时，NPU路径在两个平台上分别降低能量64.6%（AI 7 350）和52.8%（AI 9 HX 370），同时瞬时电流更低。

- **可重构性的双面性**：XDNA2的可重构性允许定制化内核和大矩阵流，但引入的非平凡调度/重构开销在短算子（GEMV）中难以摊销，这解释了生成阶段NPU劣势。

- **实用建议**：提出混合部署策略——预填充用NPU、生成用iGPU，可结合两者优势（由于统一内存，数据移动开销低）。

## 7. 优点

- **方法原创性**：首次在AMD XDNA2 NPU上实现AWQ风格W4A16/W8A16的融合推理内核，不是简单移植，而是结合了硬件特性（有限流接口、DMA步幅约束、AIE本地缓冲）进行协同设计。
- **技术细节扎实**：公开了完整的微内核设计（解包、反量化、本地重用）、交错预分块布局、GEMV数据流优化，并定量分析了可重构性的收益与代价。
- **实验设计较全面**：包含内核级、端到端、能耗、消融、跨平台对比，且基线选择强（iGPU采用llama.cpp优化版）。
- **实用性**：开源代码（GitHub），可直接用于Ryzen AI设备，推动NPU在边缘LLM中的可用性。
- **明确分析了局限性**：坦白指出了NPU在GEMV阶段的开销问题，并提出混合部署方向，有学术诚实度。

## 8. 不足与局限

- **生成阶段表现不佳**：NPU的生成吞吐显著低于iGPU（甚至低于CPU），论文归因于调度开销和维度padding，但未尝试其他优化（如批处理、流水线或与iGPU协同执行），也未跨更多模型或量化格式验证。
- **能耗测试覆盖面窄**：仅一个模型（Llama3-8B）和一个prompt长度（4096）下测量能耗，缺少生成阶段、更短/更长上下文、不同模型的对比，结论的普适性受限。
- **缺少与已有NPU框架的公平对比**：虽然对比了FastFlowLM和ScalingLLM，但平台不同（SSD? Qualcomm NPU?），且只有1024 token预填充的粗略对比，缺乏统一环境下的横向对比。
- **消融实验不完整**：Strix Point上的GEMV消融仅显示混合精度+微内核优化的效果，缺失Ours（全优化）的结果，可能是因为该平台微内核/数据流增益不大（图11b暗示？）。但应说明原因并补充数据。
- **实验统计缺失**：未报告多次运行的标准差或置信区间，难以评估结果稳定性。
- **数据依赖风险**：仅测试了三个相对较小的模型（2B-8B），未扩展至更大模型（如Llama-13B/70B或Mistral/Mixtral系列），也未测试视觉模型或其他Transformer变体，通用性待验证。
- **量化格式有限**：主要关注AWQ（W4A16）和对称INT8，未测试GPTQ、GGML（Q4_K）等流行格式；且仅使用对称INT8，未探索非对称INT8的更多情况。
- **硬件依赖性**：方法高度依赖XDNA2的微架构特性（AIE引擎、有限流接口、本地缓冲容量），移植到其他NPU架构（如Apple ANE、Qualcomm Hexagon）需要大量调整。

（完）
