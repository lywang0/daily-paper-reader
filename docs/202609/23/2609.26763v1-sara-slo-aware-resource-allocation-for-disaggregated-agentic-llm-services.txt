Title: SARA: SLO-Aware Resource Allocation for Disaggregated Agentic LLM Services

URL Source: https://arxiv.org/pdf/2609.26763v1

Published Time: Wed, 23 Sep 2026 01:45:00 GMT

Number of Pages: 17

Markdown Content:
> 1

# SARA: SLO-Aware Resource Allocation for Disaggregated Agentic LLM Services 

Shicong Liu, Graduate Student Member, IEEE , Xianghao Yu, Senior Member, IEEE , Zhen Gao, Member, IEEE ,and Jun Zhang, Fellow, IEEE 

Abstract —Recent advances in large language models (LLMs) are driving the emergence of multi-modal and agentic services for mobile users through cloud and edge infrastructures, where long-context workloads pose daunting challenges for inference latency. Existing disaggregated LLM serving systems largely rely on hardware profiling, configuration enumeration, or heuristic scheduling, offering limited analytical guidance for cost-efficient resource allocation. In this paper, we propose SARA, a Service level objectives (SLOs)-Aware Resource Allocation framework for disaggregated agentic LLM serving systems, which maximizes goodput under a deployment cost constraint and a series of quantile-based SLO constraints. By capitalizing on queuing theory, we first model the prefill, KV cache transfer, and decode stages as an M/G/ kp queue, an M/G/ 1 queue, and a generalized birth-death process, respectively. The analysis reveals that the prefill and decode stages are dominantly limited by computational capacity and high-bandwidth memory (HBM) re-sources, respectively. With these mathematical models, we further derive tractable tail behaviors of different stage-wise service level metrics for both light- and heavy-tailed workloads. These characterizations explicitly map workload, model architecture, and hardware parameters to stage-wise SLO constraints and minimum resource requirements. We also reveal that the required numbers of prefill and decode devices scale quadratically with the input and output lengths, respectively, and that stringent SLOs and heavy-tailed workloads substantially amplify the resource requirements beyond the stability condition. Finally, we develop an effective resource allocation framework to maximize system goodput under limited cost budgets. Simulation and hardware results demonstrate that the proposed framework accurately predicts the stage-wise SLO with mean errors below 5%, and improves system goodput by 26 .6% on average over state-of-the-art baseline methods under the same deployment cost. 

Index Terms —Agentic artificial intelligence, disaggregated in-ference, large language model, queuing theory, resource alloca-tion, service level objectives. 

I. I NTRODUCTION 

Large language models (LLMs) are rapidly evolving from general-purpose conversational tools into foundational ser-vices for intelligent web search, code generation, multi-modal understanding, complex reasoning, and autonomous agentic workflows [1]–[3]. As model capabilities and context windows continue to expand, LLM applications are moving beyond 

(Corresponding Author: Xianghao Yu. )Shicong Liu and Xianghao Yu are with the Department of Electri-cal Engineering, City University of Hong Kong, Hong Kong (email: sc.liu@my.cityu.edu.hk, alex.yu@cityu.edu.hk). Zhen Gao is with the Advanced Research Institute of Multidisciplinary Science (ARIMS), Beijing Institute of Technology. Jun Zhang is with the Department of Electronic and Computer Engineering, The Hong Kong University of Science and Technology, Hong Kong (email: eejzhang@ust.hk) 

single-turn dialogue interactions toward multiple-turn agen-tic tasks involving high-effort reasoning [4] and decision making [5]. Unlike model training, online LLM inference must continuously serve large and fluctuating workloads under stringent service level objectives (SLOs), including quantile-based latency metrics such as time to first token (TTFT) and time per output token (TPOT). Consequently, inference is becoming a persistent and growing infrastructure workload, and its resource efficiency will largely determine whether LLM services can be deployed at scale with sustainable cost [6]. The resource demands of LLM inference vary substantially across its execution process [7]–[11]. Upon receiving a user request, which typically contains textual or multi-modal input data, the model first tokenizes and embeds the entire input into large-scale matrices for subsequent intensive matrix multipli-cation. This stage is commonly referred to as the prefill stage, which requires substantial computational resources. After the prefill stage, the model starts the autoregressive token gener-ation process, i.e., the decode stage. This stage contains less parallel computation but frequent access to the intermediate results, which are stored in the key and value (KV) cache 

computed in the prefill stage. Therefore, the decode stage requires excessively high memory bandwidth. The heterogeneous resource requirements of the prefill and decode stages pose significant challenges for the design of LLM inference at data centers. When the two stages are executed within the same resource pool, they will interplay with each other, significantly elongate inference time [7], and reduce inference efficiency. Besides, colocated deployment reduces resource utilization and increases the cost of data center expansion as the number of user requests increases. To mitigate interference between the two stages and better match resources to their respective demands, a disaggregated prefill and decode (PD) deployment strategy has become an important design direction for large-scale LLM serving systems [7]–[10]. Moreover, such disaggregation paves the way for edge-assisted LLM services for mobile users. The compute-intensive prefill is isolated in data centers, while the memory-intensive decode stage can be pushed toward edge nodes in proximity to users. However, the disaggregated LLM architecture also intro-duces several novel problems. First, the decode stage relies on the KV cache generated during the prefill stage. When the two stages are deployed separately, the KV cache must be transferred from the prefill to decode resource pool, making the transfer latency an important factor in inference efficiency. Second, it further introduces the problem of how resources should be provisioned and allocated across the disaggregated arXiv:2609.26763v1 [cs.PF] 22 Sep 2026 2

resource pools and communication link of the KV cache transfer. Finally, the heavy-tailed user request distributions typically encountered in agentic and multi-modal workloads further complicate performance analysis and resource plan-ning. These challenges call for an analytical framework and resource allocation scheme for disaggregated LLM serving systems. 

A. Related Work 

To meet increasingly stringent SLO requirements in LLM serving systems, research efforts have been devoted to improv-ing LLM inference efficiency. From the perspective of model architecture, multi-query attention (MQA) and grouped-query attention (GQA) were proposed to reduce the computational complexity and memory access overhead by allowing multiple query matrices to share KV matrices [12], [13]. Besides, mixture-of-experts (MoE) models integrate multiple smaller domain-specific models into a unified architecture, employing dynamic gating based on the input user requests [14]. Since only a sparse subset of these experts is activated during inference, the MoE architecture significantly enhances the inference efficiency. Furthermore, to accommodate the het-erogeneous resource demands of Transformer-based LLMs, Sarathi-Serve partitions long user requests into smaller chunks and flexibly schedules their execution to improve resource uti-lization [15]. Beyond fine-grained execution scheduling, sev-eral recent works have improved inference efficiency through workload-aware resource configuration and request routing. Specifically, DynamoLLM configures resource pools with different hardware compositions and routes requests accord-ing to their sequence characteristics to improve energy effi-ciency [11]. ModServe dispatches modality-specific workloads to specialized resource pools to enhance resource utilization and inference throughput [8]. In addition, Taiji dynamically adapts resource allocation and request scheduling according to the current SLO bottleneck to increase SLO achieving throughput [16]. However, these approaches still colocate the prefill and decode stages on shared resource pools and thus cannot fundamentally eliminate their mutual interference. Moreover, even when scheduling mitigates such interference to some extent, the shared deployment couples the prefill and decode resources, limiting the independent scaling of the two stages according to their distinct workload characteristics and SLO bottlenecks. To eliminate the mutual interference between prefill and decode stages and further facilitate resource planning, recent studies have focused on the disaggregated LLM inference ar-chitecture, which executes the two stages on separate resource pools [7], [9], [10], [17]. In particular, Splitwise is one of the pioneer works that implements an LLM serving system onto separate prefill and decode resource pools, while still preserv-ing a mixed resource pool for elastic workload balance [10]. Moreover, DistServe then proposed to maximize the goodput, i.e., the throughput achieving designated SLO requirements, of the inference system, where the parallelism strategy and re-source allocation are achieved via enumeration over a reduced search space [7]. Furthermore, Mooncake develops a KV cache-centric disaggregated architecture and improves serving performance through carefully designed cache scheduling and transfer [17]. In addition, PaaS leverages linear-complexity attention to reduce KV cache transfer overhead and introduces scheduling policies that jointly exploit cache availability and communication bandwidth [9]. Despite these advances, existing disaggregated systems still rely primarily on hardware performance profiling [10], con-figuration enumeration [7], or heuristic scheduling [9], [17]. Although effective for particular platforms and workloads, these approaches offer limited theoretical guidance for offline data-center capacity planning or low-overhead online recon-figuration. In particular, they lack a tractable model that maps workload statistics and heterogeneous resource allocations to stage-wise SLO-attainment probabilities and minimum re-quired resources. These gaps motivate our SLO-aware system modeling and characterization, as well as cost-constrained resource allocation framework for disaggregated LLM serving. 

B. Contributions 

In this paper, we investigate SLO-aware resource allocation for disaggregated LLM serving systems to maximize the system goodput under cost constraints. The main contributions are summarized as follows.  

> •

To capture the heterogeneous resource demands of dis-aggregated LLM serving systems, we develop stochastic mathematical models including an M/G/ k prefill queue, an M/G/ 1 KV cache transfer queue, and a continuous batching decode process model. The models explicitly incorporate key system parameters, and provide math-ematical insights on the resource demands. Based on these models, we further derive the transition conditions between the compute- and memory-intensive regimes, validating that prefill and decode stages are predomi-nantly constrained by computational capacity and mem-ory bandwidth, respectively.  

> •

We derive tractable and analytical characterizations of the stage-wise latency quantiles, including TTFT, KV cache transfer latency, and TPOT to move beyond conventional empirical SLO assessments. Specifically, under limited cost constraint, heavy-traffic approximation is employed to characterize the waiting and sojourn latency for both the prefill and KV cache transfer queuing models, while the moment matching method is leveraged to characterize the latency quantile in the decode stage. The resulting an-alytical expressions explicitly connect system parameters and hardware resources to SLOs under both light- and heavy-tailed workloads.  

> •

We further propose a resource allocation framework to solve the goodput maximization problem for disaggre-gated LLM serving systems. In particular, stage-wise minimum resource requirements can be obtained directly from the derived closed-form expressions connecting SLOs and hardware demands, and the maximum goodput is obtained via a bisection search. 3 

> •

We finally conduct practical hardware measurements and simulations to validate the proposed analytical frame-work. The results verify the accuracy of the derived stage-wise latency quantiles with representative model architectures and both light- and heavy-tailed workloads, demonstrating the applicability of the proposed SLO-aware resource allocation framework. 

Notations : We use lowercase and uppercase boldface letters to denote vectors and matrices, respectively. The element at the k-th row and the m-th column of matrix H is denoted as H[k, m ], and the n-th element in the vector h is denoted by h[n]. {Hn}Nn=1 represents a matrix set with the cardinality of N . The superscripts (·)T and (·)H represent the transpose and conjugate transpose operators, respectively , and det( ·)

denotes the determinant operator. R and Z denote the sets of real numbers and integers, respectively. We use B[n, p ] and 

LN [μ, σ 2] to denote binomial distribution with parameters 

{n, p } and log-normal distribution with parameters {μ, σ 2},respectively. We use E[X] and Var( X) to denote the mean and variance of random variable X.II. P RELIMINARIES 

In this section, we introduce basic concepts of a typical LLM inference system. 

A. Generative Pre-Trained Transformer 

The generative pre-trained Transformer (GPT) has emerged as the dominant architecture for modern LLMs [18], [19]. It is first pre-trained on massive text-based or multi-modal corpora [20], [21] to predict the next token given preced-ing tokens, and can then be adapted or aligned for various downstream applications. Most LLMs adopt a decoder-only, multi-layer Transformer architecture [22]–[24], where each layer comprises a self-attention module and a feed-forward network [25], as illustrated in Fig. 1. During a typical LLM inference process, the core computational operations are as follows:  

> •

Tokenization & Embedding : A user request typically involves textual or other multi-modal input data. They are first tokenized into a sequence with Li input tokens, which are then mapped to matrices X ∈ RLi×Nmod via an embedding lookup table.  

> •

Attention : The input matrix X experiences large-scale multiplications. Take classical multi-head attention (MHA) 1 [25] as an example, the input matrix is first mapped to Nhead query and KV matrices in shape 

Li × Nattn by 

Qh = XW Qh , Kh = XW Kh , Vh = XW Vh , (1) respectively, where 1 ≤ h ≤ Nhead denotes the index of attention heads, and WQh , WKh , and WVh in shape 

Nmod × Nattn are the pre-trained weight matrices. Each 

> 1In modern LLMs, the attention module consists of hybrid structures, i.e., full attention [12], [13], [25] and linear-complexity attention modules [26]– [28], which reduce the computational complexity and memory consumption.

head then performs scaled dot-product attention in paral-lel as 

A(h)H = softmax 

 QhKTh

√Nattn 

+M



Vh ∈ RLi×Nattn , (2) where M in shape Li × Li denotes the causal mask matrix, whose upper-triangular entries are set to −∞ ,while the diagonal and lower-triangular entries are set to zero. This mask prevents each token from attending to future tokens during self-attention, and reduces the computational complexity by two-fold in ideal cases. The outputs of all heads are concatenated and multiplied by a pre-trained output projection matrix WO as 

AH =

h

A(1) H , · · · , A(Nhead )H

i

WO ∈ RLi×Nmod . (3)  

> •

Feed-Forward : The matrix AH is further processed by large-scale matrix operations involving two matrix multiplications and a non-linear activation function as 

O = σ (AHW1) W2 ∈ RLi×Nmod , (4) where W1 ∈ RNmod ×Nhid and W2 ∈ RNhid ×Nmod are the feed-forward projection matrices. Output matrix O

acts as the input matrix X of the next Transformer layer. After the last Transformer layer, the last row vector of O

is projected and normalized into a probability distribution, where the first output token is generated via sampling or greedy search [29].  

> •

Normalization : Normalization is applied to each row of 

O (post-norm) or X (pre-norm) independently to ensure numerical stability among layers. The computational complexity of Transformers is mainly determined by general matrix multiplications (GEMMs) [7]. The number of multiplications in one Transformer layer is characterized as follows. The projection operations in (1), attention operation in (2), the aggregation operation in (3), and the feed-forward in (4) require 3LiN 2mod ,  L2i +Li

 Nmod ,

LiN 2mod , and 2LiNmod Nhid multiplications in the ideal case, respectively. Therefore, the overall number of multiplications for Nlayer layers is given by 

Nmul (Li)= Nlayer 

 4LiN 2mod +( L2i +Li)Nmod +2 LiNmod Nhid 

 . (5) Note that for advanced attention variants such as multi-query attention (MQA) [12] and grouped-query attention (GQA) [13], NKV ≤ Nhead key-value heads are shared among Nhead query heads in the attention operation (2). Let 

g = NKV /N head ∈ (0 , 1] be the compressive factor and then the only difference in Nmul occurs in the projection operations of (1) as (1 + 2 g)LiN 2mod .

B. Disaggregated LLM Inference Architecture 

As introduced in Sec. I, resource requirements vary sig-nificantly across the execution stages of LLM inference. Specifically, generating the first token requires a full execution of all Transformer layers, resulting in excessively high compu-tational demand. During the subsequent autoregressive token generation, the intermediate KV matrices can be reused, which 4GPU HBM      

> (c)
> KV Cache Transfer Input KV Cache
> Feed-Forward Attention
> ···
> Write
> Model Weights
> Read
> Transformer Layer 1 HBM
> Input
> Transformer Layer Nlayer
> Autoregressive Decoding
> ···
> Token Output Stream
> ···
> Output Output Token User 1 Input Output Output
> (a)
> Prefill Instance 1 Prefill Instance
> ···
> GPU HBM
> GPU HBM
> Prefill Instance 2 GPU HBM
> ···
> KV Cache Transformer Layer Nlayer
> Feed-Forward Attention
> ···
> Write
> Model Weights
> Read
> HBM
> (b)
> Tokenizer User Request Embedding Token #1 First Output Token Transformer Layer 1 Decode Resource Pool User Prefill Resource Pool User Requests Input User 2 Input Output
> ··· ···
> KV Cache Pool
> Fig. 1. (a) Overall disaggregated LLM inference architecture. Key computational operations of one user request in the (b) prefill stage and (c) decode stage [30].

substantially reduces computational operations. However, fre-quent access to the KV cache makes memory bandwidth the major demand. Consequently, an inference process can be divided into three disaggregated and sequential stages according to their resource demands as follows.  

> •

Prefill : The prefill stage spans from user prompt input to the generation of the first output token. It includes intensive attention and feed-forward operations across all transformer layers, as shown in Fig. 1(b) and introduced in Sec. II-A. Note that the KV matrices {Kh}NKV  

> h=1

and 

{Vh}NKV  

> h=1

associated with attention operations are stored as the KV cache to support subsequent stages, while 

{Qh}Nhead  

> h=1

are discarded after the first token is generated. Owing to the intensive large-scale matrix multiplications involved, the resource bottleneck of this stage is the computational capacity [7].  

> •

KV Cache Transfer : The KV matrices {Kh}NKV  

> h=1

and 

{Vh}NKV  

> h=1

computed during the prefill stage remain fixed and are reused throughout the inference. Therefore, in the disaggregated LLM inference framework, these matrices are stored as KV cache and must be further transferred to the decode resource pool to accelerate further compu-tations.  

> •

Decode : In this stage, the LLM autoregressively generates a token stream based on the entire KV cache 2. Specifi-cally, as illustrated in Fig. 1(c), in each iteration of the decode stage, the previous token acts as the input and yields incremental query and KV vectors, which together with the KV cache finish the following attention oper-ations to generate the next token. The decode iteration ends when an end-of-sequence (EOS) token is generated or the maximum output length is reached. Because of frequent memory accesses of the KV cache, this stage requires extremely high memory bandwidth for efficient 

> 2The decode stage also produces incremental KV cache when generating new output tokens, which is appended to the total KV cache for subsequent inference, as illustrated in Fig. 1(c). Yet, the additional computational overhead introduced here is usually negligible compared with the entire prefill stage.

inference. A typical disaggregated LLM inference process is illustrated in Fig. 1. Specifically, each prefill resource pool employs kp

prefill instances, which process incoming user requests in par-allel. Upon completion, KV caches and the first output tokens of user requests are transferred to the decode resource pool, where output token streams are autoregressively generated in parallel via continuous batching [31]. 

C. KV Cache 

As we have introduced in Sec. II-B, KV cache is the intermediate output mainly generated in the prefill stage and reused during the following LLM inference procedure. In the decode stage, it stores KV matrices computed by each Transformer layer using all previously input and output tokens, so that the subsequent autoregressive iterations can directly reuse them without repeated computations. However, for LLMs possessing several hundred billion of parameters, the resulting KV cache can also be extremely memory-consuming. For example, in an MHA-dominated ar-chitecture such as GPT-3 175B [24], the typical KV cache memory consumption with Li = 2048 input tokens and 

Nbits = 16 bits quantization in the prefill stage is given by 

ΩMHA KV (Li) = 2LiNlayer Nmod Nbits 

8 × 1024 3 = γL i = 9 (GiB) , (6) where GPT-3 possesses Nlayer = 96 layers of Transform-ers with model dimension Nmod = 12 , 288 , and γ =

Nlayer Nmod Nbits /4/1024 3 is the KV cache scaling factor. Similarly, for GQA and MQA attention variants, the memory footprint is ΩKV (Li) = gγL i.With the emergence of hybrid-attention architecture, where the MHA is replaced by the mixture of a small number of full-attention layers and a larger number of linear-complexity or bounded-state layers [9], [12], [13], [25]–[28], the memory consumption of the KV cache is substantially reduced, making the KV cache transfer across data centers possible. 5

III. S YSTEM MODEL 

In this section, we introduce the mathematical model devel-oped for the disaggregated LLM service architecture. 

A. M/G/ kp Queuing Prefill Model 

Consider a prefill resource pool that is composed of kp iden-tical prefill instances with replications of the same LLMs. Each instance is equipped with GPU devices and high bandwidth memory (HBM) as shown in Fig. 1(a), and serves one user request at a time. User requests waiting for available instances form a shared queue. Based on the behavior of prefill operations, we model the prefill stage as a queuing system with kp parallel servers. Since the LLM service requests typically come from a large number of independent users, the arrival process can be treated as a Poisson process with arrival rate λ [7], [15]. For a given input sequence with Li tokens at a prefill instance, according to (5), the computational latency of the prefill stage is calculated by 

Tp,1(Li) = Nmul (Li)

Fp

, (7) where Fp denotes the compute capacity of each prefill instance in the resource pool. Besides computational operations, the prefill stage also introduces HBM traffic latency, which mainly consists of model weight loading and KV cache writing. Specifically, the memory footprints of the model weight and KV cache are given by ΩLLM and ΩKV (Li) in (6), respectively, which result in the memory prefill latency 

Tp,2 (Li) = ΩLLM + Ω KV (Li)

BHBM 

, (8) where BHBM is the bandwidth of HBM. Hence, the overall service time Tp is then modeled as 

Tp (Li) = max ( Tp,1 (Li) , T p,2 (Li)) . (9) In summary, we model the prefill stage as an M/G/ kp

queue [32]. Prefill utilization is given by ρp = λE[Tp(Li)] /k p,which must satisfy ρp < 1 to ensure the stability of the queue. 

B. KV Cache Transfer 

After the prefill stage, the generated KV cache must be transferred to the decode resource pool. The departure traffic from the kp prefill instances is forwarded to the KV cache transfer stage. When kp is large and the departure traffics from kp instances are mutually independent, the superposed departure process of the KV cache can also be approximated as a Poisson process [33]. For a stable prefill queue without request loss or abandonment, the KV cache transfer arrival rate 

λKV equals the user request arrival rate of the prefill stage as 

λKV = λ. Let B denote the link bandwidth for KV cache transfer, the service time is given by 

TTR (Li) = ΩKV (Li)

B = gγL i

B . (10) Therefore, the KV transfer stage can also be modeled as an M/G/ 1 queue, and the utilization is 

ρKV = λE[TTR (Li)] = λE[Ω KV (Li)] 

B < 1. (11) 

C. Generalized Birth-Death Decode Model 

As shown in Fig. 1(c), in the decode stage, the next output token is generated based on the input and all previous output tokens. The reuse of KV cache significantly alleviates the computational overhead of token generation. To fully exploit the computational capability of the decode resource pool, requests containing the first output tokens generated by the prefill stage are executed via continuous batching [31]. This enables the sharing of computation and HBM resources across 

kd GPU devices. In each decoding iteration, Nbat requests in a batch are decoded in parallel, each producing one output token at a time. As new requests arrive or existing requests terminate after different numbers of iterations, requests are dynamically admitted to or removed from the batch, so the value of Nbat 

varies over time. Let Lio denote the total number of tokens within the batch of user requests as 

Lio =

> Nbat

X

> n=1



L(n)i + L(n)o



, (12) where L(n)i and L(n)o denote the number of input tokens and the number of output tokens already generated for the n-th user request, respectively. The required number of multiplications is then given by 

Nmul ,d(Lio ) (13) 

= Nlayer 

 Nbat 

 2(1 + g)N 2mod +2 Nmod Nhid 

+2 Nmod Lio 

 .

Therefore, the decode computational latency is modeled as 

Td,1(Lio ) = Nmul ,d(Lio )

kdFd

, (14) where Fd denotes the computational capacity of each decoding GPU. In each decoding iteration, all KV caches of the requests in a batch must be loaded into the computing units. Because of the large memory footprint of KV cache gγL io within the batch, the resulting latency 

Td,2(Lio ) = ΩLLM + gγL io 

kdBHBM 

(15) is non-negligible. Assuming perfect parallelism between com-putation and HBM read/write operations, the overall decode service time can be written as 

Td(Lio ) = max ( Td,1, T d,2) . (16) 

D. Stage-Wise LLM Service Metrics 

To evaluate the SLO of LLM services, the following metrics are commonly considered.  

> •

Time to First Token (TTFT) : A critical latency metric in the prefill stage. TTFT is defined as the elapsed time from the arrival of a user request to the generation of the very first output token. It can be expressed by the sojourn time of the prefill queuing model as 

TTTFT = T Wp + Tp (Li) , (17) 6

where T Wp is the waiting time of the user request in the prefill stage. TTFT is affected by the number of input tokens Li, model footprint ΩLLM , and hardware compute capacity Fp. In real-time chatting or agentic workflows, minimizing TTFT is of great importance, as it directly reflects the user’s waiting time.  

> •

KV-Cache Transfer Latency : After the prefill stage, the generated KV cache and the first token ID are transferred to the decode resource pool. The transfer latency consists of the waiting time T WTR in the transfer queue and the actual transmission time TTR given by 

TKV = T WTR + TTR (Li). (18) Although it is not commonly treated as a conventional SLO metric, it connects the prefill and decode stages and influences the overall service quality. We hence treat it as an independent stage-wise SLO metric.  

> •

Time per Output Token (TPOT) : A fundamental la-tency metric in the decode stage. TPOT is defined by the time required to generate each output token as 

TTPOT = Td(Lio ), (19) which is also a random variable affected by the context length Lio , batch size Nbat , and other system parameters. Lower TPOT produces smoother and more responsive text generation, and a suitable TPOT should not exceed the time interval in which users can comfortably read. 

E. Problem Formulation 

In this subsection, we formulate the SLO-aware resource allocation problem for the disaggregated LLM serving system described in the previous subsections. An SLO specifies a latency threshold that the related met-ric must remain below with a prescribed probability pSLO .Goodput denotes the user request throughput λ adhering to the latency SLO targets, i.e., ensuring that pSLO percent of the requests satisfy the designated latency requirements [7], [16]. In this work, we aim to maximize the system goodput by jointly allocating the overall cost. For the aforementioned three inference stages, let r = {kp, B, k d, N bat } be the set of resources to be allocated, the goodput maximization problem is then defined by 

P1 : max  

> r

λ (20a) 

s.t. kpCp + BC KV + kdCd ≤ Cmax , (20b) 

Pr ( TTTFT ≤ τpre ) ≥ pSLO , (20c) 

Pr ( TKV ≤ τKV ) ≥ pSLO , (20d) 

Pr ( TTPOT ≤ τdec ) ≥ pSLO , (20e) 

ρp < 1, ρ KV < 1, k p, k d, N bat ∈ Z. (20f) In problem P1, (20b) denotes the cost constraint, where 

Cp, CKV , and Cd are the unit costs of prefill instance, KV cache transfer bandwidth, and decode device, respectively, and 

Cmax is the maximum cost to be allocated. (20c)-(20e) are the SLO targets for prefill, KV cache transfer, and decode stages, respectively, where τpre , τKV , and τdec denote the latency thresholds for TTTFT , TKV , and TTPOT , respectively. The probabilities in (20c)-(20e) typically do not admit closed-form expressions due to the complicated behaviors and heterogeneous stochastic mechanisms. Therefore, in the following sections, we will analyze the stage-wise latency distributions, derive tractable approximations for the SLO-attainment probabilities, and establish the corresponding re-source requirements. IV. A NALYSIS 

In this section, we analyze the stage-wise pSLO -quantile latency tail of SLO constraints in P1, and derive the corre-sponding resource requirements with respect to corresponding decision variables. Under practical setups 3, the prefill and decode stages are bottlenecked by computational capacity and memory bandwidth under practical setups, respectively (See Appendix A for the proof). Therefore, we only consider latency arising from compute capacity in the prefill stage and memory bandwidth in the decode stage. 

A. Prefill Stage 

In this subsection, we derive the closed-form approximation of sojourn latency TTTFT = T Wp + Tp (Li) in the prefill stage. We first derive the behavior of the prefill waiting time 

T Wp . With a limited overall cost Cmax , the prefill queue can operate at very high utilization, i.e., ρp → 1. In this case, the Kingman’s approximation [33], [35] can be applied to the waiting time T Wp of M/G/ kp queue to analyze the tail behavior as 

Pr  T Wp ≥ τ  ≈ Ckp(A) e− τw , (21) where 

Ckp(A) = 

Akp

kp!11 − ρp

> kp−1

X

> n=0

An

n! + Akp

kp!11 − ρp

(22) is the Erlang-C waiting probability with traffic intensity A =

ρpkp,

w = 1 + c2

2

ρp

λ (1 − ρp) (23) is the approximation factor, and c = pVar[ Li]/E[Li] is the coefficient of variation (CV) of random variable Li.Next, we derive effective approximations for the service time in the prefill stage Tp (Li). According to Appendix A, we have Tp ≈ Tp,1 = Nmul (Li)/F p. Under practical parameters 3,the quadratic term in Nmul (Li) shows a limited contribution as Nlayer Nmod L2i

Nmul (Li) = 0 .0256 , (24) which indicates that the quadratic term in Nmul (Li) has a limited effect (below 2.6% ) on the overall computational                     

> 3Unless otherwise stated, examples of practical system parameters in this paper refer to NVIDIA A100 GPU [34] with Fp= 156 ×10 12 multiplications per second and BHBM = 2 TBps, LLaMA 3.1 8B LLM [22] with Nlayer =32 ,Nmod = 4 ,096 ,Nhid = 14 ,336 ,g= 0 .25 , and Li= 1 ,024 .7

complexity. Therefore, the prefill service time can be further approximated as 

Tp ≈ αpLi, (25) where αp = Nlayer (2(1 + g)N 2mod +(2 Nhid + 1) Nmod )/F p.Since the waiting time T Wp and the service time Tp(Li) are independent, the CDF FTTFT (τpre ) of sojourn time TTTFT is obtained by the convolution as 

Pr ( TTTFT ≤ τpre )=

Z τpre 

> 0

Pr  T Wp ≤ τpre − s dFTp (s)

≈

Z τpre 

> 0



1−Ckp(A) e− τpre −sw



dFTp (s)= FTp (τpre ) − Ckp(A)

Z τpre 

> 0

e− τpre −sw dFTp (s),

(26) where FTp (·) is the cumulative distribution function (CDF) of prefill service time Tp. The latency SLO constraint (20c) is then reformulated as 

FTp (τpre ) − Ckp (A)

Z τpre 

> 0

e− τpre −sw dFTp (s) ≥ pSLO . (27) In particular, (27) provides a unified characterization for any given distribution of Li. Based on application scenarios, the distribution 4 of Li can be categorized into two types [36]:  

> •

Light-tailed : Short interactive workloads, such as con-versation and code completion, are commonly dominated by relatively short prompts and can be modeled by an exponential distribution [10], [11], i.e., Li ∼ Exp[1 /ℓ i],where ℓi = E[Li]. In this case, (21) is an exact equality instead of an approximation.  

> •

Heavy-tailed : Long context workloads, such as multi-modal and agentic tasks with multi-turn dialogues, usu-ally exhibit a heavy-tailed length distribution that can be modeled by a log-normal distribution [8], i.e., Li ∼LN [μi, σ 2i ].For light-tailed case with Li ∼ Exp[1 /ℓ i], the CDF of Tp(Li)

is given by FTp (t) = 1 − e− tαpℓi . The integral form in (27) for constraint (20c) is then reduced to 

1 − e− τpre  

> αpℓi

− wC kp (A)

αpℓi − w



e− τpre  

> αpℓi

− e− τpre 

> w



≥ pSLO . (28) However, for the heavy-tailed case with Li ∼ LN [μi, σ 2i ], the convolution integral (27) does not admit a finite closed-form expression and should therefore be evaluated numerically. Since an SLO constraint physically corresponds to a minimal resource requirement, the unique optimal k⋆ 

> p

can be obtained via enumeration. The approximations in (21) and (25) are validated in Fig. 2. In Fig. 2(a), the only approximation stems from (25) since (21) is exact in the light-tailed case. In other words, we show that (25) is an accurate approximation. In Fig. 2(b), the average deviation over the entire range of pSLO is within 5% , which validates the accuracy of the closed-form approximations                            

> 4While Liis a discrete random variable in practice, to facilitate analysis, we model its distribution in the continuous domain. Similar treatment applies to other random variables such as Loand Lio in the remainder of this paper. 0.9 0.91 0.92 0.93 0.94 0.95 0.96 0.97 0.98 0.99
> pSLO
> 00.2 0.4 0.6 0.8 1
> TTTFT  (s) (a)
> LLaMA 8B Qwen2 :5 32B GPT !3 175B Theoretical Queue Simulation Hardware Test
> 0.9 0.91 0.92 0.93 0.94 0.95 0.96 0.97 0.98 0.99
> pSLO
> 00.2 0.4 0.6 0.8 1
> TTTFT  (s) (b)
> Fig. 2. TTFT metric with varying pSLO thresholds for (a) light-tailed and (b) heavy-tailed workloads, respectively.

in (21) and (25) for the heavy-tailed case. In addition, the hard-ware measurements closely follow the theoretical predictions. The average relative error remains below 5% for pSLO < 0.98 ,and the discrepancy becomes more visible only at the extreme tail percentiles. 

B. KV Cache Transfer Stage 

In this subsection, we analyze the sojourn latency TKV in the KV cache transfer stage, and derive approximate closed-form solutions for the light-tailed and heavy-tailed cases, respectively. In the KV cache transfer stage, the communication link with bandwidth B serves one KV cache transfer request. Therefore, the KV cache transfer stage is modeled as an M/G/ 1 queue. For user requests with light- and heavy-tailed characteristics, the sojourn latencies are analyzed as follows. 

1) Light-Tailed: For the number of tokens following a light-tailed exponential distribution as Li ∼ Exp[1 /ℓ i], the service time TTR = gγL i/B is also exponentially distributed. Therefore, the queuing model of the KV cache transfer stage degenerates to an M/M/ 1 queue. In this case, the waiting time 

T WTR also follows an exponential distribution with probability mass 1 − ρKV at t = 0 . Therefore, the CDF of the waiting time is given by [37] 

Pr  T WTR ≤ t = 1 − ρKV e− twKV , (29) where 

wKV = ρKV 

λ (1 − ρKV ) (30) is the tail decay factor. Since the waiting time and the service time of an arriving request are independent, the CDF of the sojourn latency TKV can be obtained by the convolution as 

Pr ( TKV ≤ τKV ) = 

Z τKV 

> 0

Pr  T WTR ≤ τKV − t d Pr ( TTR ≤ t)= 1 − e− τKV  

> wKV

. (31) 80.9 0.91 0.92 0.93 0.94 0.95 0.96 0.97 0.98 0.99            

> pSLO
> 0123
> TKV  (s) (a)
> LLaMA 8B Qwen2 :5 32B GPT !3 175B Theoretical Queue Simulation Hardware Test
> 0.9 0.91 0.92 0.93 0.94 0.95 0.96 0.97 0.98 0.99
> pSLO
> 01234
> TKV  (s) (b)
> Fig. 3. KV cache transfer latency with varying pSLO thresholds for (a) light-tailed and (b) heavy-tailed workloads, respectively.

Therefore, the SLO constraint (20d) is exactly characterized as 

1 − e− τKV  

> wKV

≥ pSLO , (32) which yields 

B ≥ λgγℓ i + gγℓ i

τKV 

log 11 − pSLO 

. (33) 

2) Heavy-Tailed: For multi-modal and agentic requests, the input length Li is modeled by a log-normal distribution as Li ∼ LN [μi, σ 2i ], where μi and σi are the corresponding distribution parameters. According to (10), the transfer service time also follows a log-normal distribution as 

TTR (Li) = gγL i

B ∼ LN 

h

μi + log 

 gγ B



, σ 2i

i

. (34) Unfortunately, with heavy-tailed service time behavior, the waiting time distribution of the M/G/ 1 queue does not admit the tractable exact expression available in the light-tailed case. Hence, we resort to the Kingman-type heavy traffic approximation [33], [35] similar to the prefill stage and obtain 

B ≥ λgγℓ i + eσ2i

2

gγℓ i

τKV 

log ρKV 

1 − pSLO 

. (35) For detailed derivation, please refer to Appendix B. Hence, we can obtain optimal B⋆ via one-dimensional line search. Figs. 3(a) and 3(b) validate the accuracy of the closed-form expressions (33) and (35) for the light- and heavy-tailed cases, respectively. In particular, the theoretical predictions match the queue simulations and hardware measurement results with negligible errors. The deviations increase only at the extreme high tails, where system-level overheads that are not consid-ered in the queuing model may have a stronger impact. 

Remark 1. It is worth noting that for both light- and heavy-tailed workloads, the bandwidth B requirements on the right-hand-side of (33) and (35) can be divided into a λ-related term and an SLO-related term, while the SLO-related bandwidth requirement is independent of the request arrival rate λ. This indicates that when λ changes, we only need to scale the λ-related bandwidth linearly with the factor gγℓ i to meet the SLO target. C. Decode Stage 

In this subsection, we analyze the TPOT constraint (20e) in 

P1. Different from the prefill and KV cache transfer stages, the resource allocation in the decode stage is determined by two variables, i.e., the number of decode GPU devices kd

and the maximum batch size Nbat . Increasing Nbat improves the admission capability of user requests, but also increases the requirements of HBM capacity kdΩHBM and total band-width kdBHBM . In the following, we derive the relationships between kd and Nbat to determine the feasible region for resource allocation. According to Appendix A, the decode service time is mainly determined by the HBM bandwidth under practical configurations as 

TTPOT ≈ Td,2 = ΩLLM + gγL io 

kdBHBM 

. (36) where the Lio is the aggregated number of tokens within the batch defined by (12). Different from the prefill stage, where 

Li follows either a light-tailed exponential or heavy-tailed log-normal distribution, Lo statistically follows an exponential distribution as Lo ∼ Exp[1 /ℓ o], and is typically independent with Li [8], [11], [36]. According to (36), the pSLO -quantile of TPOT is closely related to that of Lio . Therefore, in the following, we analyze the TPOT SLO constraint under light- and heavy-tailed cases using moment matching [38], [39], respectively.  

> •

Light-tailed : In this case, Lio is the sum of Nbat ex-ponential random variables Li ∼ Exp[1 /ℓ i] and Nbat 

exponential random variables Lo ∼ Exp[1 /ℓ o]. There-fore, we can approximate the distribution of Lio by using a Gamma distribution random variable with a constant bias [39]. Specifically, let L′ 

> io

= ℓLT + ˜Lio be the approximated random variable of Lio , where ˜Lio ∼ Gamma[ αLT , θ LT ]

is a Gamma-distributed random variable, and ℓLT is a constant bias. The parameters {ℓLT , α LT , θ LT } can be obtained by matching the mean, variance, and skewness of Lio as 



ℓLT = Nbat 

ℓiℓo (ℓi − ℓo)2

ℓ3i + ℓ3o

αLT = Nbat 

 ℓ2i + ℓ2o

3

(ℓ3i + ℓ3o)2

θLT = ℓ3i + ℓ3o

ℓ2i + ℓ2o

. (37) Therefore, the pSLO -quantile of L′ 

> io

is given by 

ℓ (pSLO ) = ℓLT + θLT P −1 

> αLT

(pSLO ), (38) where P −1 

> αLT

(·) is the inverse function of the regularized lower incomplete gamma function with parameter αLT .9 

> •

Heavy-tailed : In this case, Lio is the sum of Nbat log-normal random variables Li ∼ LN [μi, σ 2i ] and Nbat ex-ponential random variables Lo ∼ Exp[1 /ℓ o]. Therefore, we can model Lio using a log-normal random variable with a constant bias [38]. Specifically, let L′ 

> io

= ℓHT + ˜Lio be the approximated random variable of Lio , where ˜Lio ∼ LN [μℓ, σ 2 

> ℓ

] is a log-normal random variable, and ℓHT is a constant bias. The parameters {ℓHT , μ ℓ, σ ℓ} can be obtained by matching the mean, variance, and skewness of Lio as 



ℓHT = m −√vxμℓ = 12 log v

1 + x2 − log xσ2 

> ℓ

= log  1 + x2

, (39) where 



m = Nbat 



ℓo + eμi+σ2i /2

v = Nbat 



ℓ2o + ( eσ2i − 1) e2μi+σ2i



x = 2 sinh 

 13 arcsinh 

 s

2



s =

e3μi+ 32 σ2i



eσ2i + 2 

  

eσ2i − 1

2

+ 2 ℓ3o

q

Nbat 

 e2μi+σ2i

 eσ2i − 1 + ℓ2o

3

. (40) Therefore, the pSLO -quantile of L′ 

> io

is given by 

ℓ(pSLO ) = ℓHT + eμℓ+σℓΦ−1(pSLO ), (41) where Φ( ·) is the CDF of the standard normal distribu-tion. Therefore, the probability on the left-hand-side of (20e) in P1

can be represented by L′ 

> io

as 

Pr( TTPOT ≤ τdec ) ≈ Pr 



L′ 

> io

≤ τdec kdBHBM − ΩLLM 

gγ 



. (42) Based on the workload characteristics, the SLO con-straint (20e) is equivalent to 

C1 (L) : kd ≥ gγ  ℓLT + θLT P −1 

> αLT

(pSLO ) + Ω LLM 

τdec BHBM 

(43) for the light-tailed workload, or 

C1 (H) : kd ≥

gγ 



ℓHT + eμℓ+σℓΦ−1(pSLO )



+ Ω LLM 

τdec BHBM 

(44) for the heavy-tailed workload. These conditions ensure that 

pSLO percent of the requests satisfy the SLO (20e). They pro-vide one of the boundaries for decision variables {kd, N bat }.Another boundary is given by the HBM capacity. Other than the model weights, each active user request needs to maintain the KV cache associated with its input tokens and currently generated output tokens. To ensure that the HBM capacity is not exhausted with probability pmem , the following inequality must hold as 

ΩLLM + gγℓ (pmem ) ≤ kdΩHBM , (45) which yields 

C2 (L) : kd ≥ gγ  ℓLT + θLT P −1 

> αLT

(pmem ) + Ω LLM 

ΩHBM 

(46) or 

C2 (H) : kd ≥

gγ 



ℓHT + eμℓ+σℓΦ−1(pmem )



+ Ω LLM 

ΩHBM 

. (47) Note that C1 and C2 both implicitly limit the batch size 

Nbat for a given kd, i.e., C1 limits Nbat to satisfy the TPOT SLO constraint, while C2 limits Nbat to prevent HBM capacity exhaustion. Nevertheless, the batch size Nbat cannot be arbitrarily small. There are also two major constraints that contribute to the other two boundaries. The first one is the stability condition given by 

C3 : Nbat > λΩLLM 

kdBHBM p0 − λgγ (ℓi + ℓo) , (48) where p0 denotes the probability of reaching the EOS token for each request. Since the output length L(n)o follows an expo-nential distribution as L(n)o ∼ Exp[1 /ℓ o], and is independent of the input length L(n)i [8], [11], [36], p0 is further given by 

p0 = Pr ( Lo ≤ n + 1 | Lo ≥ n) = 1 − e− 1 

> ℓo

. (49) Please refer to Appendix C for more derivation details. The second one is the direct admission condition given by 

C4 : Nbat ≥ μbat + σbat Φ−1(pjoin ), (50) where μbat and σbat are the mean and standard deviation of the number of requests in a batch, and pjoin is the probability that a request can directly join the decoding batch without waiting. Please refer to Appendix D for more details. 

C4 is typically a tighter condition than C3 , since it intrinsi-cally implies the stability of the decoding process, and requires a larger batch size Nbat to reduce the waiting time. Fig. 4 depicts the feasible regions delineated by boundaries 

C1 − C4 for LLaMA 3.1 and GPT-3 models under varying system parameters and heavy-tail user request distributions. For a fixed kd, C1 and C2 act as upper constraints of batch size Nbat , while C3 and C4 serve as lower constraints. Fig. 5 validates the proposed TPOT quantile characterization under both light- and heavy-tailed workloads. The analytical predictions closely match the simulation results. Meanwhile, the deviation between the analytical results and hardware measurements falls below 5% over most of the considered 

pSLO range. These results demonstrate that the proposed model accurately captures the decoding latency behavior under practical hardware execution. V. SLO-A WARE RESOURCE ALLOCATION 

In this section, we develop an SLO-aware resource alloca-tion scheme for solving the goodput maximization problem 

P1.According to the analysis in Sec. IV, the minimum required resources r = {kp, B, k d, N bat } can be obtained given the arrival rate λ as well as SLOs, which yield the minimum deployment cost Cmin . Since Cmin increases monotonically 10 1 2 3 4                                     

> Number of Decode Devices kd
> 050 100 150 200 250 300 350 400
> Batch Size  Nbat
> (a) LLaMA 3 8B, 6= 20, =dec = 10 ms
> Feasible Region C1: TPOT Constraint C2: HBM Capacity Constraint C3: Stability Condition C4: Waiting Condition
> 1234
> Number of Decode Devices kd
> 050 100 150 200 250 300 350 400
> Batch Size  Nbat
> (b) LLaMA 3 8B, 6= 40, =dec = 5 ms
> 5678910 11 12
> Number of Decode Devices kd
> 0510 15 20 25 30 35 40
> Batch Size  Nbat
> (c) GPT-3 175B, 6= 1, =dec = 30 ms
> 78910 11 12 13 14
> Number of Decode Devices kd
> 0510 15 20 25 30 35 40
> Batch Size  Nbat
> (d) GPT-3 175B, 6= 3, =dec = 15 ms

Fig. 4. The heavy-tailed decision boundaries C1 − C4 and the corresponding feasible regions for (a)-(b) LLaMA 3.1 8B and (c)-(d) GPT-3 175B models. 0.9 0.91 0.92 0.93 0.94 0.95 0.96 0.97 0.98 0.99          

> pSLO
> 25 30 35 40 45 50
> TTPOT  (ms) (a)
> LLaMA 3 :1 8B Qwen2 :5 32B GPT !3 175B Theoretical GBD Simulation Hardware Test
> 0.9 0.91 0.92 0.93 0.94 0.95 0.96 0.97 0.98 0.99
> pSLO
> 25 30 35 40 45 50
> TTPOT  (ms) (b)

Fig. 5. TPOT with varying pSLO thresholds for (a) light-tailed and (b) heavy-tailed workloads, respectively. 

with arrival rate λ, we can then obtain the maximum goodput via a bisection search. Therefore, we can solve the following cost minimization problem as 

P2 : max  

> r

λ (51a) 

s.t. kpCp + BC KV + kdCd ≤ Cmax , (51b) (27) , (51c) (33) or (35) (51d) 

C1 − C4 , , (51e) 

ρp < 1, ρ KV < 1, k p, k d, N bat ∈ Z. (51f) where (51d) is determined by the light-tailed or heavy-tailed workload type. The procedure of resource allocation is sum-marized in Algorithm 1. VI. I NSIGHTS 

In this section, we revisit the analytical mappings estab-lished above and summarize the insights that are most rele-vant to the resource planning of disaggregated LLM serving systems. 

A. Token Throughput 

For LLM serving systems, the token generation rate, i.e., the token throughput, is another key performance metric that 

Algorithm 1 Proposed Resource Allocation Algorithm 

Input: The cost budget Cmax , the prescribed SLO thresholds 

{τpre , τ KV , τ dec } and pSLO , the decode reliability param-eters pmem and pjoin for C2 and C4 , the unit costs Cp,

CKV , and Cd, and the parameters of model and hardware 

Output: The maximum goodput λ⋆

1: Initialize trial solution λ(0) and iteration index i = 0 

2: repeat {i ← i + 1 }

3: Compute the minimum prefill instance requirement 

kp(λ(i)) via (27) 

4: Compute the minimum KV transfer bandwidth require-ment B(λ(i)) via (33) or (35) 

5: Compute the minimum decode device requirement 

kd(λ(i), N bat ) via C1 − C4 

6: Compute the overall cost 

C(i) = kp(λ(i))Cp + B(λ(i))CKV + kd(λ(i), N bat )Cd

7: Bisectionally update λ(i) by comparing C(i) and Cmax 

8: until |λi − λi−1| ≤ ε

9: return Maximum goodput λ⋆ = λi

measures the total serving capacity of the system. Under the proposed analytical framework, each batched request generates exactly one token per decoding iteration, whose duration is dominated by the HBM access overhead in (36). Therefore, the token throughput is given by 

Γ = Nbat 

E [TTPOT ] = kdBHBM Nbat 

ΩLLM + gγN bat (ℓi + ℓo) , (52) where Nbat denotes the number of concurrent requests within the decoding batch. Since ∂Γ/∂N bat > 0 always holds, the token throughput always increases with the number of con-current requests Nbat . However, the fact that ∂2Γ/∂N 2bat < 0

indicates that the throughput gain diminishes as the concur-rency Nbat grows. As Nbat continues to increase, the token throughput Γ saturates at 

Γmax = lim  

> Nbat →∞

Γ = kdBHBM 

gγ (ℓi + ℓo) , (53) which depends only on the overall HBM bandwidth kdBHBM 

of the decode pool and the KV cache footprint ΩKV =

gγ (ℓi + ℓo). As shown in Fig. 6(a), under identical hardware configurations, LLaMA 3.1 8B and Qwen2.5 32B achieve 11 10 0 10 1 10 2 10 3 10 4 10 5             

> Nbat
> 10 2
> 10 3
> 10 4
> 10 5
> 10 6
> ! (tokens/s) (a)
> LLaMA 3.1 8B Qwen2.5 32B GPT-3 175B Feasible Infeasible
> !max
> Simulation C1 C2 C3 C4
> (b)
> 10 210 310 410 510 6
> Li(tokens)
> 10 0
> 10 2
> 10 4
> 10 6
> Mean TTFT  E[Tp] (ms)
> Const Linear Quad Theoretical LLaMA 3.1 8B Qwen2.5 32B GPT-3 175B Simulation
> (c)
> 10 210 310 410 5
> Li(tokens)
> 10 0
> 10 2
> 10 4
> 10 6
> Mean TPOT  E[Td] (ms) (d)
> 10 210 310 410 5
> Lo(tokens)
> 10 0
> 10 2
> 10 4
> 10 6
> Decode Latency  E[LoTd] (ms)

Fig. 6. (a) Token throughput over varying batch size. (b)-(d) The inference latency metrics over varying lengths of input user requests Li and output sequences 

Lo.10 !3      

> 10 !2
> 10 !1
> 1!pSLO
> 3456
> kp
> (a) Pre -ll
> SLO tax CV = 1 CV = 1 :5CV = 2 CV = 2 (Stability) 10 !3
> 10 !2
> 10 !1
> 1!pSLO
> 100 200 300 400
> B (GiB/s) (b) KV Cache Transfer
> SLO tax 10 !3
> 10 !2
> 10 !1
> 1!pSLO
> 16 20 24 28 32
> kd
> (c) Decode
> SLO tax

Fig. 7. The resource requirements under varying SLO violation probability 1 − pSLO and stability condition. 

higher saturation throughputs Γmax than the GPT-3 175B, owing to their smaller per-token KV cache footprints and the use of GQA, characterized by γ and g, respectively. It is also worth noting that in the feasible region delineated by 

C1 − C4 , all models can achieve over 90% of the maximum token throughput. 

B. Quadratic Impact of Input and Output Lengths 

With the popularization of agentic LLM workflows, scenar-ios with extremely long input user requests from multi-turn LLM conversations are becoming increasingly common. The expanding context lengths for both input Li and output Lo

impose distinct SLO pressures across different stages. First, as shown in Fig. 6(b), the TTFT latency versus 

Li across different LLM models falls into three regions. For small Li, i.e., Li < ˜Li, TTFT is dominated by HBM bandwidth and thus remains largely constant. As Li grows, TTFT scales approximately linearly. For sufficiently large Li,TTFT will increase quadratically with Li. This is because the increase of Li amplifies the impact of the quadratic term L2i

in (5) during the prefill stage. Under the stability condition 

ρp = λE[Tp(Li)] /k p < 1, we have kp > λ E[Tp(Li)] , which suggests that, in multi-modal or agentic multi-turn dialogue scenarios, the requirement of prefill devices kp also scales quadratically with the input length Li.Besides, both the input length Li and output length Lo show a significant impact on the decode stage. Specifically, when the input (or output) sequence length Li (or Lo) is small, the LLM weight term ΩLLM outweighs KV cache term gγL io in (15). However, as either Li or Lo increases, TPOT will eventually exhibit linear growth with Li or Lo, as illustrated in Fig. 6(c). Furthermore, long output lengths Lo in complex tasks lead to an approximately quadratic effect on total decode time, as shown in Fig. 6(d). When ℓo is sufficiently large, the probability of reaching EOS token is approximated by 

p0 = 1 − e− 1 

> ℓo

≈ 1/ℓ o. According to the stability condition 

C3 , the requirement of decoding devices is given by 

kd ≳ λ  ℓoΩLLM + gγN bat 

 ℓiℓo + ℓ2o

 

Nbat BHBM 

∼ O (ℓ2o), (54) indicating that kd scales quadratically with the output length when ℓo is large. It is worth noting that these hardware requirements are derived from the stability condition, which are conservative estimates of the numbers of the prefill and decode devices required for stable operation. Satisfying more stringent SLOs typically requires additional hardware capacity and conse-quently a substantially larger device allocation. 

C. The SLO Tax 

Compared to the stability conditions, i.e., ρp < 1, ρKV < 1,and C3 , which only guarantee the functionality of the LLM serving system, enforcing the SLO constraints (20c)-(20e) requires additional hardware resources. This extra cost is the 

SLO tax . Fig. 7 illustrates the SLO tax by showing the mini-mum requirements derived from both the stability conditions and SLO constraints as pSLO tightens. The evaluation adopts the GPT-3 175B model with A100 devices, λ = 20 req/s, and 12 10 !2 10 0                      

> =pre ,=KV ,=dec (s)
> 10 !1
> 10 0
> 10 1
> 10 2
> !@C=@ =
> (a)
> =pre =0 :222 s =KV =0 :036 s `o=dec =0 :742 s
> Pre -ll KV Cache Transfer Decode
> =0=1 :00 s
> (b)
> 0.5 0.75 11.25 1.5 1.75 2
> =0(s)
> 00.2 0.4 0.6 0.8 1
> Latency Budget Share
> Pre -ll =pre
> KV =KV
> Decode `o=dec
> 0.5 0.75 11.25 1.5 1.75 2
> =0(s)
> 050 100 150 200
> 6 (req/s) (c)
> Infeasible
> Uniform Empirical Optimal
> Fig. 8. Latency budget τ0allocation across the prefill, KV cache transfer, and decode stages. (a) Marginal benefits curves with varying τ, (b) optimal budget shares versus budget τ0, and (c) goodput versus budget τ0compared with empirical and uniform baselines.

(τpre , τ KV , τ dec ) = (1 .5, 0.2, 0.02) s. Both exponential light-tailed ( CV = 1 ) and log-normal heavy-tailed ( CV ∈ { 1.5, 2})cases are considered. First, Fig. 7(a) shows the minimum kp under the stability condition ρp < 1 and the SLO constraint (20c) characterized by (27). It shows that kp diverges rapidly as pSLO → 1, and the heavy-tailed effect of input lengths significantly aggravates the hardware demand. Besides, Fig. 7(b) shows the minimum bandwidth B against 

1 − pSLO , characterized by (33) and (35) for light- and heavy-tailed workloads, respectively. According to (33) and (35), the SLO tax beyond the stability floor B > λgγℓ i grows linearly with log 11−pSLO , with a slope proportional to eσ2i /2 = (1 + c2TR )/2. In other words, the required bandwidth increases exponentially with pSLO , and the heavy-tailed effect further amplifies the exponential factor. Furthermore, Fig. 7(c) shows the minimum kd against pSLO ,where the SLO constraint follows from C1 and the stability floor follows from C3 . As pSLO increases, the quantile term 

θLT P −1 

> αLT

(pSLO ) or eμℓ+σℓΦ−1(pSLO ) in (43) or (44) inflates the context length within a batch that must be served within TPOT threshold τdec , and the device count increases accordingly. As 

pSLO → 1, the additional demand incurred by the SLO tax can reach about twice the stability condition, revealing the necessity of SLO-aware resource allocation. 

D. Latency Budget Allocation 

We further investigate the latency thresholds in the SLO constraints (20c)-(20e). In agentic workflows, a single in-ference task may contain tens of user requests, and the completion time of every request directly affects the overall service latency. Therefore, in addition to TTFT and TPOT, the aggregate latency τ0 is also an important metric for LLM services. For each request, the overall budget of the aggregate latency τ0 can be expressed as 

τ0 ≥ τpre + τKV + ℓoτdec . (55) To achieve goodput maximization, we need to allocate the latency budget τ0 to three stages. Since the deployment cost 

Cmax increases monotonically with the achievable arrival rate 

λ, maximizing goodput is essentially equivalent to minimizing the deployment cost at a fixed λ. Hence, the allocation of τ0

is equivalent to minimizing the deployment cost 

C(τ ) = kp(τpre )Cp + B(τKV )CKV + kd(τdec )Cd, (56) subject to (55), where τ = {τpre , τ KV , τ dec }. The Lagrangian of the cost function (56) is given by 

L(τ ) = kp(τpre )Cp + B(τKV )CKV + kd(τdec )Cd

+ ν (τpre + τKV + ℓoτdec − τ0) , (57) whose first-order optimal conditions yield 

Cp

∂k p

∂τ pre 

= CKV 

∂B ∂τ KV 

= Cd

ℓo

∂k d

∂τ dec 

= −ν. (58) Since the minimum cost of each stage is a non-increasing function of its own latency budget, the solution to (58) is the unique global optimum. Therefore, the optimal latency budget allocation can be obtained by solving (58) numerically. The physical interpretation of (58) is that the marginal benefit of latency budget allocation is identical across three stages. Fig. 8(a) shows the marginal benefit curves −∂C/∂τ of three stages, where the optimal allocation at τ0 = 1 s is marked by the dashed line. It is validated that the marginal benefits −∂C/∂τ are decreasing over τ , so the optimal budget allocation can be obtained via numerical methods. Fig. 8(b) illustrates the latency budget allocation share among three stages. When the total budget τ0 is small, a larger fraction of the budget is allocated to the prefill stage. This is because a tight TTFT threshold τpre substantially increases the prefill queuing delay and the required number of prefill instances, resulting in a larger marginal cost reduction when τpre is relaxed. As τ0 increases, the prefill resource requirement gradually approaches its stability floor, while the KV cache transfer and decode requirements remain sensitive to their latency thresholds, so the latency shares these stages keep increasing. Fig. 8(c) shows the goodput achieved by the optimal al-location, where an empirical latency budget allocation with 

25% : 25% : 50% share split and a uniform share split baselines are considered. The optimal allocation consistently attains the highest goodput, and the gain is most pronounced under tight budgets. As τ0 increases, the goodput of all schemes increases, while the proposed allocation scheme still achieves the highest goodput performance. 13 

TABLE I BASIC PARAMETERS OF THE EVALUATED LLM S.

Parameter LLaMA 3.1 8B Qwen2.5 32B GPT-3 175B 

Attention Mechanism GQA GQA MHA 

Nmod 4,096 5,120 12 ,288 

Nhead 32 40 96 

NKV 8 8 96 

Nattn 128 128 128 

Nhid 14 ,336 27 ,648 49 ,152 

Nlayer 32 64 96 

g = NKV /N head 0.25 0.2 1

γ (GiB/token) 4.883 ×10 −4 1.221 ×10 −3 4.395 ×10 −3

ΩLLM (GiB) 14 .90 59 .60 325 .96 

VII. M AIN RESULTS 

In this section, we validate the modeling accuracy of the proposed closed-form approximations and evaluate the perfor-mance of the proposed resource allocation scheme. 

A. Parameter Setup 

Unless otherwise stated, in the following evaluation part, we conduct LLM inference experiments using the high-performance SGLang framework [40] on multiple NVIDIA A100 GPU devices. In the simulation, the cost for each device is set to Cp = Cd = 5 USD per hour, and the cost for bandwidth is set to CKV = 0.1 USD per GiB per hour. The SLO constraint probability is pSLO = 0.95 , and the typical latency thresholds τpre , τKV , and τdec are set to 0.5,

0.15 , and 0.04 seconds, respectively. pmem and pjoin are both set to 0.99 . We mainly consider user requests with heavy-tailed workload, where Li follows a log-normal distribution as 

Li ∼ LN [μi, σ 2i ] with mean ℓi = E[Li] = eμi+σ2i /2 = 1 , 024 

and CV σi/μ i = 1 .25 [36]. The output length follows an exponential distribution Lo ∼ Exp[1 /ℓ o] with ℓo = 256 .The baseline models cover a wide range of memory foot-prints, including LLaMA 3.1 8B [22], Qwen2.5 32B [41], and GPT-3 175B [24]. The parameters of these baseline models are illustrated in Table I. 

B. Resource Allocation 

We first evaluate the effectiveness of the proposed SLO-aware resource allocation, i.e., SARA, scheme in maximizing goodput under a fixed deployment cost. The baseline methods are as follows.  

> •

Exhaustive Search : Enumerates all feasible configura-tions of {kp, B, k d, N bat } and selects the configuration that provides the best goodput performance under the given cost Cmax and SLO constraints (20c)-(20e).  

> •

Empirical : The overall cost Cmax is allocated with fixed proportions among the three stages. In this work, the proportion is set as 45% : 10% : 45% . 

> •

Splitwise : Apart from the prefill and decode resource pools, this scheme considers a mixed resource pool where the short user requests are routed to. Prefill and decode stages are operated together within the mixed resource pool. 40 50 60 70 80 90 100                

> Cmax (USD/h)
> 100 150 200
> Goodput (req/s)  (a) LLaMA 3 8B, Light-Tailed
> Exhaustive Search SARA Splitwise Colocated Empirical
> 60 80 100 120 140 160 180 200
> Cmax (USD/h)
> 050 100
> Goodput (req/s)  (b) Qwen 2.5 32B, Light-Tailed
> 150 200 250 300 350 400
> Cmax (USD/h)
> 010 20
> Goodput (req/s)  (c) GPT-3 175B, Light-Tailed

Fig. 9. Goodput performance under light-tailed workload and varying cost budget Cmax for (a) LLaMA 3 8B, (b) Qwen2.5 32B, and (c) GPT-3 175B models. 40 50 60 70 80 90 100                

> Cmax (USD/h)
> 100 150 200
> Goodput (req/s)  (a) LLaMA 3 8B, Heavy-Tailed
> Exhaustive Search SARA Splitwise Colocated Empirical
> 60 80 100 120 140 160 180 200
> Cmax (USD/h)
> 020 40 60
> Goodput (req/s)  (b) Qwen 2.5 32B, Heavy-Tailed
> 150 200 250 300 350 400
> Cmax (USD/h)
> 0510 15
> Goodput (req/s)  (c) GPT-3 175B, Heavy-Tailed

Fig. 10. Goodput performance under heavy-tailed workload and varying cost budget Cmax for (a) LLaMA 3 8B, (b) Qwen2.5 32B, and (c) GPT-3 175B models.  

> •

Colocated : Only one mixed pool is considered in this scheme, where the prefill and decode stages operate con-currently. The latency introduced by KV cache transfer is eliminated, while the interference between the two stages persists. Figs. 9 and 10 show the achievable goodput under light-tailed and heavy-tailed workloads, respectively. The goodput increases with the deployment cost for all methods because additional deployment costs lead to higher hardware capacity to support a higher request rate. For most of the cases, 14                                                         

> TABLE II EXECUTION TIME OF RESOURCE ALLOCATION SCHEMES (SECONDS ).
> Light-Tailed Heavy-Tailed Method LLaMA 3.1 8B Qwen2.5 32B GPT-3 175B LLaMA 3.1 8B Qwen2.5 32B GPT-3 175B
> Exhaustive Search 1.44 ×10 32.44 ×10 36.19 ×10 21.44 ×10 32.44 ×10 36.19 ×10 2
> SARA 0.70 1.60 0.61 0.44 1.11 1.02
> Splitwise 60 .978 .62.02 60 .878 .52.01
> Colocated 13 .07.71 2.46 13 .07.73 2.47 20 40 60 80 100 120 140 160
> Time (h)
> 020 40 60
> Goodput (req/s)
> Exhaustive Search SARA Splitwise Colocated Empirical O,ered Load
> Fig. 11. Goodput performance of the replayed seven-day trace [11] with the LLaMA 3.1 8B model on A100 GPUs.

the proposed SARA method achieves the highest goodput performance, preceded only by the exhaustive search scheme, which reveals the superiority of the SARA scheme. For extremely large models, e.g., GPT-3 175B, with a limited cost budget Cmax , the colocated scheme achieves slightly higher goodput performance while all the disaggregated schemes fail to operate. Note that the goodput improvement becomes more significant for heavy-tailed workloads and higher cost budgets, showing the effectiveness of the proposed scheme in agentic workflows and large-scale deployments. We then replay the seven-day public Microsoft Azure LLM inference trace [11] to evaluate the allocation schemes under a time-varying production workload. We employ the LLaMA 3.1 8B model with overall cost Cmax = 35 USD/h on A100 GPUs. The resource allocation algorithms are executed once per hour, and the goodput results are shown in Fig. 11. During off-peak hours, the goodput difference across the algorithms is marginal. However, pronounced differences can be observed during peak hours. The exhaustive search achieves the highest goodput performance and is tightly tracked by the proposed SARA scheme. The maximum performance gap between the two schemes is 3.8% under extreme workloads. On the con-trary, the splitwise, colocated, and empirical schemes fail to utilize resources efficiently, and the maximum performance degradations reach 21 .5% , 45 .6% , and 57 .2% during peak periods, respectively. These results demonstrate that the pro-posed scheme achieves superior resource utilization over the baselines. 

C. Computational Complexity 

We also investigate the computational complexity of the proposed resource allocation algorithm, and the results are shown in Table II. Exhaustive search requires extremely high execution time as it enumerates millions of hardware com-binations to find the optimal configuration. Both Splitwise and colocated schemes estimate the required resources and prune the search space, so the allocation search overheads are substantially reduced. Empirical allocation follows a resource allocation preset, and therefore exhibits no execution time at the cost of lower goodput performance. The proposed SARA completes the allocation with no more than 1.60 s, achieving an acceleration of several orders of magnitude over exhaustive search, and is faster than all baseline methods, especially when the resource requirement is sufficiently large. Combined with the goodput results in Figs. 9 and 10, these results demonstrate that SARA is suitable for practical resource planning and system reconfiguration. It is worth noting that the execution time in Table II drops significantly for the GPT-3 175B model. This is because its large memory footprint constrains the feasible solution space under limited resources. Meanwhile, the proposed SARA scheme consistently executes in around 1 s, as it reduces the solution space via closed-form expressions, eliminating the need for exhaustive enumeration. VIII. C ONCLUSION 

In this paper, we developed an SLO-aware resource alloca-tion framework for disaggregated LLM serving systems. We modeled the prefill, KV cache transfer, and decode stages using an M/G/ kp queue, an M/G/ 1 queue, and a generalized birth-death process, respectively. Based on these models, we derived tractable latency quantile characterizations and re-source requirements under both light-tailed and heavy-tailed workloads. Based on the proposed analytical framework, we found and validated several insights on data center planning and resource allocation. First, the saturated token throughput of the decode stage depends on the HBM bandwidth and the LLM parameters, and enlarging the batch size without violating the TPOT SLO helps improve the overall throughput. Besides, the required numbers of prefill and decode devices scale quadratically with the input and output lengths, revealing the fundamental resource pressure imposed by long-context agentic workloads. Furthermore, both the SLO constraints and the heavy-tailed workload significantly amplify the resource requirements, imposing hardware demands well beyond stabil-ity conditions. Finally, both the proposed latency budget allo-cation and the SARA schemes effectively improve the system goodput with low complexity, demonstrating the practicality of the proposed analytical framework. 15 

REFERENCES [1] M. A. Ferrag, N. Tihanyi, and M. Debbah, “From LLM reasoning to autonomous ai agents: A comprehensive review,” IEEE Access , vol. 14, pp. 84 237–84 285, Jun. 2026. [2] L. Qiao, M. B. Mashhadi, Z. Gao, R. Tafazolli, M. Bennis, and D. Niyato, “Token communications: A large model-driven framework for cross-modal context-aware semantic communications,” IEEE Wireless Commun. , vol. 32, no. 5, pp. 80–88, Sept. 2025. [3] G. Shi, N. Lan, X. Xie, and J. Wu, “Rethinking intelligence in the era of large language models,” J. Commun. Inf. Netw. , vol. 11, no. 1, pp. 39–50, 2026. [4] J. Wei et al. , “Chain-of-thought prompting elicits reasoning in large language models,” in Proc. Adv. Neural Inf. Process. Syst. (NeurIPS) .Red Hook, NY, USA: Curran Associates Inc., 2022. [5] C. Sun, S. Huang, and D. Pompili, “LLM-based multi-agent decision-making: Challenges and future directions,” IEEE Robot. Autom. Lett. ,vol. 10, no. 6, pp. 5681–5688, Apr. 2025. [6] L. Cozzi et al. , “Key Questions on Energy and AI,” International Energy Agency, Paris, France, World Energy Outlook Special Report, Apr. 2026. [7] Y. Zhong et al. , “Distserve: Disaggregating prefill and decoding for goodput-optimized large language model serving,” in Proc. USENIX Symp. Operating Syst. Design and Implement. (OSDI’24) . Santa Clara, CA, USA: USENIX Association, 2024. [8] H. Qiu et al. , “Modserve: Modality- and stage-aware resource disag-gregation for scalable multimodal model serving,” in Proc. ACM Symp. on Cloud Comput. (SOCC’25) . Online: Association for Computing Machinery, 2026, p. 817–830. [9] R. Qin et al. , “Prefill-as-a-service: KVCache of next-generation models could go cross-datacenter,” 2026. [Online]. Available: https: //arxiv.org/abs/2604.15039 [10] P. Patel et al. , “Splitwise: Efficient generative LLM inference using phase splitting,” in Proc. Int. Symp. Comput. Archit. (ISCA’24) .Buenos Aires, Argentina: IEEE Press, July 2025, pp. 118–132. [Online]. Available: https://doi.org/10.1109/ISCA59077.2024.00019 [11] J. Stojkovic, C. Zhang, Goiri, Íñigo, J. Torrellas, and E. Choukse, “DynamoLLM: Designing LLM inference clusters for performance and energy efficiency,” in Proc. IEEE Int. Symp. High-Perform. Comput. Archit. (HPCA’25) , Las Vegas, NV, USA, 2025, pp. 1348–1362. [12] N. Shazeer, “Fast transformer decoding: One write-head is all you need,” 2019. [Online]. Available: https://arxiv.org/abs/1911.02150 [13] J. Ainslie, J. Lee-Thorp, M. de Jong, Y. Zemlyanskiy, F. Lebron, and S. Sanghai, “GQA: Training generalized multi-query transformer models from multi-head checkpoints,” in Proc. Conf. Empirical Methods Nat. Lang. Process. (EMNLP) . Singapore: Association for Computational Linguistics, Dec. 2023, pp. 4895–4901. [14] N. Shazeer et al. , “Outrageously large neural networks: The sparsely-gated mixture-of-experts layer,” in Proc. Int. Conf. Learn. Represent. (ICLR) , Toulon, France, Apr. 2017. [15] A. Agrawal et al. , “Taming Throughput-Latency tradeoff in LLM inference with Sarathi-Serve,” in Proc. USENIX Symp. Operating Syst. Design Implement. (OSDI’24) . Santa Clara, CA: USENIX Association, July 2024, pp. 117–134. [16] C. Wang, P. Zuo, Z. Chen, Y. Liang, Z. Yu, and M.-C. Yang, “TaiJi: Unifying prefill-decode disaggregation and aggregation for goodput-optimized LLM serving,” in Proc. Int. Conf. High Perform. Comput., Netw., Storage, Anal. (SC) , Chicago, IL, USA, 2026. [17] R. Qin et al. , “Mooncake: A KVCache-centric disaggregated architecture for LLM serving,” ACM Trans. Storage , Nov. 2025. [18] A. Radford, K. Narasimhan, T. Salimans, and I. Sutskever, “Improving language understanding by generative pre-training,” OpenAI Technical Report, 2018. [19] G. Yenduri et al. , “GPT (generative pre-trained transformer)— a compre-hensive review on enabling technologies, potential applications, emerg-ing challenges, and future directions,” IEEE Access , vol. 12, pp. 54 608– 54 649, 2024. [20] H. Liu, C. Li, Q. Wu, and Y. J. Lee, “Visual instruction tuning,” in Proc. Adv. Neural Inf. Process. Syst. (NeurIPS) , vol. 36. New Orleans, LA, USA: Curran Associates, Inc., 2023, pp. 34 892–34 916. [21] G. Team et al. , “Gemini 1.5: Unlocking multimodal understanding across millions of tokens of context,” 2024. [Online]. Available: https://arxiv.org/abs/2403.05530 [22] A. Grattafiori et al. , “The llama 3 herd of models,” 2024. [Online]. Available: https://arxiv.org/abs/2407.21783 [23] A. Yang et al. , “Qwen3 technical report,” 2025. [Online]. Available: https://arxiv.org/abs/2505.09388 [24] T. Brown et al. , “Language models are few-shot learners,” in Proc. Adv. Neural Inf. Process. Syst. (NeurIPS) , vol. 33. Curran Associates, Inc., 2020, pp. 1877–1901. [25] A. Vaswani et al. , “Attention is all you need,” in Proc. Adv. Neural Inf. Process. Syst. (NeurIPS) , vol. 30. Long Beach, CA, USA: Curran Associates, Inc., 2017. [26] K. Team et al. , “Kimi linear: An expressive, efficient attention architecture,” 2025. [Online]. Available: https://arxiv.org/abs/2510.26692 [27] I. Beltagy, M. E. Peters, and A. Cohan, “Longformer: The long-document transformer,” 2020. [Online]. Available: https://arxiv.org/abs/ 2004.05150 [28] OpenAI et al. , “GPT-OSS-120b & GPT-OSS-20b model card,” 2025. [Online]. Available: https://arxiv.org/abs/2508.10925 [29] Y. Leviathan, M. Kalman, and Y. Matias, “Fast inference from transform-ers via speculative decoding,” in Proc. Mach. Learn. Res. , A. Krause, E. Brunskill, K. Cho, B. Engelhardt, S. Sabato, and J. Scarlett, Eds., vol. 202. PMLR, July 2023, pp. 19 274–19 286. [30] C. Shi, Y. Chen, Y. Chen, X. Miao, and B. Cui, “Multi-segment attention: Enabling efficient KV-cache management for faster large language model serving,” 2026. [Online]. Available: https://arxiv.org/abs/2606.02964 [31] G.-I. Yu, J. S. Jeong, G.-W. Kim, S. Kim, and B.-G. Chun, “Orca: A distributed serving system for Transformer-Based generative models,” in 

Proc. USENIX Symp. Operating Syst. Design and Implement. (OSDI’22) .Carlsbad, CA: USENIX Association, July 2022, pp. 521–538. [32] J. F. C. Kingman, “The first erlang century—and the next,” 

Queueing Syst. , vol. 63, no. 1, p. 3, Nov. 2009. [Online]. Available: https://doi.org/10.1007/s11134-009-9147-4 [33] J. Kingman, Poisson Processes , ser. Oxford Studies in Probability. Clarendon Press, 1992. [34] NVIDIA Corporation, “NVIDIA A100 Tensor Core GPU,” May 2022. [35] W. Whitt, Stochastic-Process Limits: An Introduction to Stochastic-Process Limits and Their Application to Queues , 1st ed., ser. Springer Series in Operations Research and Financial Engineering. Springer New York, NY, 2002. [36] Y. Xiang et al. , “ServeGen: Workload characterization and generation of large language model serving in production,” in Proc. USENIX Symp. Netw. Syst. Design Implement. (NSDI’26) . Renton, WA, USA: USENIX Association, May 2026, pp. 1845–1859. [37] A. O. Allen, Probability, statistics, and queueing theory with computer science applications . USA: Academic Press Professional, Inc., Sept. 1990. [38] L. Fenton, “The sum of log-normal probability distributions in scatter transmission systems,” IRE Trans. Commun. Syst. , vol. 8, no. 1, pp. 57–67, Mar. 1960. [39] Y. Liu, M. Derakhshani, and S. Lambotharan, “Outage analysis and power allocation in uplink non-orthogonal multiple access systems,” 

IEEE Commun. Lett. , vol. 22, no. 2, pp. 336–339, Feb. 2018. [40] L. Zheng et al. , “SGLang: efficient execution of structured language model programs,” in Proc. 38th Int. Conf. Neural Inf. Process. Syst. (NIPS’24) . Red Hook, NY, USA: Curran Associates Inc., 2024. [41] Qwen et al. , “Qwen2.5 technical report,” 2025. [Online]. Available: https://arxiv.org/abs/2412.15115 [42] F. Pollaczek, “Über eine Aufgabe der Wahrscheinlichkeitstheorie. I,” 

Mathematische Zeitschrift , vol. 32, no. 1, pp. 64–100, 1930. [43] J. Abate, G. L. Choudhury, and W. Whitt, “Waiting-time tail probabilities in queues with long-tail service-time distributions,” Queueing Syst. ,vol. 16, no. 3, pp. 311–338, Sept. 1994. 16 

APPENDIX ALATENCY DOMINANT FACTORS 

According to (9) and (16), the service time in the prefill and decode stage is determined by multiple system parameters. To facilitate efficient analysis of P1, in this subsection, we first identify the dominant performance parameters under practical configurations. 

A. Compute-Bound Prefill Service Time 

In the prefill stage, according to (7) and (8), the prefill service time Tp is determined by the compute capacity Fp and HBM bandwidth BHBM . To further investigate the dominant factors of service time Tp, we first solve Tp,1(Li) > T p,2(Li),which yields a quadratic form of Li as 

aL 2i + bL i + c > 0 (59) with b = BHBM Nlayer Nmod (1 + 2 Nhid + 2(1 + g)Nmod ) −

Fpgγ , a = BHBM Nlayer Nmod , and c = −FpΩLLM . For practical LLM serving systems 3, the quadratic term aL 2i is negligible compared with the linear term bL i as 

aL 2i

bL i

= Li

BHBM Nlayer Nmod (1+2 Nhid +2(1+ g)Nmod )−Fpgγ 

≈ 0.004 ≪ 1. (60) Hence, (59) can be approximately reduced to a linear inequal-ity bL i + c > 0, which yields the length threshold 

˜Li = FpΩLLM 

BHBM Nlayer Nmod (1+2 Nhid +2(1+ g)Nmod )−Fpgγ ,

above which (59) holds. Based on practical parameter setups 3,the length threshold is around ˜Li ≈ 156 tokens, as shown in Fig. 12(a). Due to prompt prefixes and accumulated input and output context in multi-turn conversations of LLM user requests, the input token length Li in realistic scenarios is typically around thousands of tokens, which is much longer than ˜Li [10], [11], [36]. Consequently, the prefill service time is mainly determined by compute capacity, i.e., Tp ≈ Tp,1.

B. HBM Bandwidth-Bound Decode Service Time 

Similarly, we first calculate Td,1(Lio ) < T d,2(Lio ) accord-ing to (14) and (15), which yields 

(gγF d − 2BHBM Nlayer Nmod ) Lio (61) 

> 2BHBM Nlayer Nbat 

 (1+ g)N 2mod +Nmod Nhid 

−FdΩLLM .

Since gγF d − 2BHBM Nlayer Nmod > 0 typically holds true under practical parameters 3, the threshold of Lio is given by 

˜Lio (62) 

= 2BHBM Nlayer Nbat 

 (1+ g)N 2mod +Nmod Nhid 

−FdΩLLM 

Fdgγ − 2BHBM Nlayer Nmod 

.

The resource bottleneck is HBM bandwidth BHBM for Lio ≥

˜Lio , and is compute capacity Fd otherwise. With practical batch size such as Nbat = 128 , the token threshold ˜Lio is even negative as shown in Fig. 12(b), so the decode service time is mainly determined by the HBM bandwidth BHBM , i.e., 

Td ≈ Td,2.0 50 100 150 200 250 300 350 

Input Tokens Li

> 0510
> Tp (ms)

(a)           

> ~Li:156 Tokens
> Tp;1(Li) (Compute)
> Tp;2(Li) (Memory) -3 -2 -1 012345

Overall Tokens Lio #10 4

> 468
> Td (ms)

(b)  

> ~Lio : ! 14336 Tokens
> Td;1(Lio ) (Compute)
> Td;2(Lio ) (Memory)
> Fig. 12. Token thresholds for (a) prefill and (b) decode stages under practical parameters stated in Footnote 3.

APPENDIX BHEAVY -T AILED ANALYSIS FOR KV C ACHE TRANSFER 

According to Kingman-type heavy traffic approxima-tion [33], [35], we resort to analyze the waiting time as 

Pr  T WTR ≥ t ≈ ρKV e− twTR , (63) where 

wTR = 1 + c2TR 

2

ρKV 

λ (1 − ρKV ) = eσ2i

2

gγℓ i

B − λgγℓ i

is the modified tail decay factor, ℓi = E[Li] = eμi+σ2i /2, and 

cTR =

p

eσ2i − 1 is the CV of TTR . Under high utilization 

ρKV → 1, the waiting time T WTR asymptotically dominates the sojourn time as [42], [43] 

lim  

> ρKV →1

E[T WTR ]

E[TTR ] = lim  

> ρKV →1

ρKV 

 1 + c2TR 



2(1 − ρKV ) ≫ 1, (64) and 

lim  

> ρKV →1

Var( T WTR )Var( TTR ) = lim     

> ρKV →1
> ρKV e3σ2i
> 3(1 −ρKV )

+ ρ2KV e2σ2i 

> 4(1 −ρKV )2

eσ2i − 1 ≫ 1. (65) Eqs. (64) and (65) indicate that TTR is a random variable with much smaller expectation value and variance than T WTR .Hence, we can safely approximate the tail behavior of sojourn latency using the waiting time T WTR as 

TKV ≈ T WTR , (66) which simplifies the analytical framework without incurring noticeable error. Hence, the SLO constraint (20d) is charac-terized as 

τKV ≥ wTR log ρKV 

1 − pSLO 

, (67) which yields 

B ≥ λgγℓ i + eσ2i

2

gγℓ i

τKV 

log ρKV 

1 − pSLO 

. (68) 17 

APPENDIX CDERIVATION OF CONDITION C3 

Statistically, the output length L(n)o follows an exponential distribution as L(n)o ∼ Exp[1 /ℓ o], and is independent with the input length L(n)i [8], [11], [36]. Therefore, in each decoding iteration, the probability of reaching the EOS token for each request is 

p0 = Pr ( Lo ≤ n + 1 | Lo ≥ n) = 1 − e− 1 

> ℓo

. (69) The number of departing requests Dk then follows a binomial distribution Dk ∼ B [N (k)bat , p 0]. Let the number of arrivals in the k-th decoding iteration be Ak ∼ Poisson[ λT d], so the number of requests to be processed in the (k + 1) -th iteration is 

N (k+1) bat = N (k)bat − Dk + Ak. (70) At the steady state of N (k)bat , i.e., k → ∞ , we should have 

lim 

> k→∞



E

h

N (k+1) bat 

i

− E

h

N (k)bat 

i 

= 0 (71) to ensure the stability of the decoding system. In this case, taking the conditional expectation on both sides of (70), we have 

E

h

N (k+1) bat | N (k)bat 

i

= E

h

N (k)bat − Dk | N (k)bat 

i

+ E

h

Ak | N (k)bat 

i

= N (k)bat e− 1 

> ℓo

+

λ



ΩLLM + gγN (k)bat (ℓi + ℓo)



kdBHBM 

,

(72) where ℓi = E[Li]. Taking the conditional mean over N (k)bat on both sides of (72) yields 

E

h

N (k+1) bat 

i

= E

h

N (k)bat 

i

e− 1 

> ℓo

+

λ



ΩLLM +gγ E

h

N (k)bat 

i

(ℓi +ℓo)



kdBHBM 

.

(73) Substituting (71) into (73), we obtain the steady state mean 

lim  

> k→∞

E

h

N (k)bat 

i

= λΩLLM 

kdBHBM p0 − λgγ (ℓi + ℓo) (74) subject to kdBHBM p0 > λgγ (ℓi + ℓo), which further yields 

C3 : Nbat > λΩLLM 

kdBHBM p0 − λgγ (ℓi + ℓo) . (75) APPENDIX DDERIVATION OF CONDITION C4 

Following the notation in Appendix C, the conditional variance of N (k+1) bat is given by 

Var 



N (k+1) bat | N (k)bat 



= Var 



N (k)bat − Dk | N (k)bat 



+ Var 



Ak | N (k)bat 



= N (k)bat p0(1 − p0) + 

λ



ΩLLM + gγN (k)bat (ℓi + ℓo)



kdBHBM 

.

(76) According to the law of total variance, we have 

Var 



N (k+1) bat 



= E

h

Var 



N (k+1) bat | N (k)bat 

i 

+ Var 



E

h

N (k+1) bat | N (k)bat 

i 

,

(77) and the variance of steady state N (k)bat is given by 

Var 



N (∞)bat 



=

λΩLLM 

kdBHBM 

+



p0(1 − p0) + λgγ (ℓi + ℓo)

kdBHBM 



E

h

N (∞)bat 

i

1 −



1 − p0 + λgγ (ℓi + ℓo)

kdBHBM 

2

(78) Note that given a practically large number of parallel requests within the batch, the steady state distribution of 

Nbat is the sum of i.i.d. Bernoulli Dk and Poisson Ak

components. Hence, if there is no limit on the maximum batch size, the distribution of requests within the batch is approximately Gaussian according to the central limit theorem (CLT), i.e., Nbat ∼ N (μbat , σ 2bat ), where μbat = E[N (∞)bat ]

and σ2bat = Var( N (∞)bat ). Therefore, we can empirically assign a large batch size to avoid excessive waiting time in the decode stage by 

C4 : Nbat ≥ μbat + σbat Φ−1(pjoin ), (79) where pjoin is the probability of directly joining the decoding batch without waiting.