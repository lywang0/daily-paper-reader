---
title: "The Other Half of the Memory Wall: Serving 35B MoEs from SSD with Trained Routing Prediction"
title_zh: 内存墙的另一半：用训练好的路由预测从 SSD 服务 35B MoE 模型
authors: "Yu Lin, Yiming Wang, Runyuan Cai, Hanze Liu, Xiaodong Zeng"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18063v2"
tags: ["query:edge-llm"]
score: 9.0
evidence: 面向消费级硬件的SSD流式MoE推理引擎
tldr: MoE 推理在消费级硬件上受限于权重内存：35B 模型 4-bit 下仍需 19.5GB，稀疏性只减少每 token 计算量、不减少常驻字节。朴素 SSD 卸载无效，因为第 N+1 层专家必须在第 N 层输出前选定，读取无法提前隐藏。Edge0 用预路由头提前一个 token 预测下一层路由，并直接以预测作为真实路由，使预取专家集与命中集一致、不丢弃专家。配合在 student 路径上训练的未合并恢复 LoRA，补偿 int4 量化与路由替换的精度损失。单台 24GB 机器上以 20tok/s 服务 35B MoE，峰值活跃内存低于 3GiB，五个公开基准平均仅落后 fp16 教师几个点，框架与权重开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.18063v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1684, \"height\": 783, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.18063v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1667, \"height\": 730, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.18063v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1670, \"height\": 659, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.18063v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1490, \"height\": 644, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.18063v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1577, \"height\": 387, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.18063v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 648, \"height\": 233, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.18063v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1203, \"height\": 402, \"label\": \"Table\"}]"
motivation: 消费级硬件上 MoE 权重内存是硬瓶颈，而层间路由依赖导致 SSD 预取无法提前启动，朴素卸载失效。
method: 提出 Edge0 流式引擎：每层预路由头提前一个 token 预测下一层路由并直接用作路由，再用 student 路径训练的恢复 LoRA 补偿量化损失。
result: 单台 24GB 机器上 35B MoE 达 20tok/s，峰值活跃内存低于 3GiB，五个基准平均仅落后 fp16 教师几个点。
conclusion: 证明训练式路由预测可把内存墙另一半转化为可流式服务，并开源框架、检查点与适配器。
---

## 摘要
在消费级硬件上进行混合专家（MoE）推理受限于权重内存：一个 35B 级模型在 4 位量化下为 19.5GB，而稀疏性减少的是每个 token 的计算量，而不是必须持有的字节数。朴素地卸载到 SSD 本身无济于事，因为第 N+1 层的专家必须在第 N 层输出存在之前就被选定，所以读取无法足够早地开始以隐藏在计算之后。我们提出 Edge0，一个流式 MoE 推理引擎，它通过预路由器（prerouter）弥合这一差距：每层的一个头提前一个 token 预测下一层的路由，并且该预测被当作路由本身来消费，因此暂存的专家集合等于被路由的专家集合，不会丢弃任何内容。一个在 student 路径上训练的未合并恢复 LoRA，补偿了 int4 量化和路由替换带来的质量损失。在单台 24GB 机器上，Edge0 在 3GiB 峰值活跃内存内以 20tok/s 服务一个 35B MoE，在五个公开基准上平均与其 fp16 教师模型相差几个点。一个 8B 层级在同一框架上运行，且框架、检查点和适配器均为开源。

## Abstract
Mixture-of-experts (MoE) inference on consumer hardware is bounded by weight memory: a 35B-class model is 19.5GB at 4-bit, and sparsity shrinks the compute per token, not the bytes that must be held. Naive offloading to SSD does not help on its own, because layer N+1's experts must be chosen before layer N's output exists, so the reads cannot start early enough to hide behind compute. We present Edge0, a streaming MoE inference engine that closes the gap with a prerouter: a per-layer head predicts the next layer's routing one token ahead, and the prediction is consumed as the routing itself, so the staged expert set equals the routed set and nothing is dropped. An unmerged recovery LoRA, trained on the student path, pays back the quality lost to int4 quantization and routing replacement. On a single 24GB machine, Edge0 serves a 35B MoE at 20tok/s inside 3GiB of peak active memory, within a few points of its fp16 teacher on average across five public benchmarks. An 8B tier runs on the same framework, and the framework, checkpoints, and adapters are open source.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **问题定位**：论文关注“内存墙的另一半”——静态权重内存。MoE 推理在消费级硬件上主要受权重内存限制：35B 级模型即使 4-bit 量化仍需 19.5 GB；MoE 稀疏性只减少每 token 的计算量，不减少必须存放的权重字节数。
- **背景矛盾**：
  - 数据中心可通过专家并行、分片服务吸收权重内存，但本地 24 GB 机器无法容纳 35B MoE 的 4-bit 权重，更无法同时留给 KV cache 和操作系统。
  - 量化到 4-bit 已接近实际下限，再低误差不可接受。
  - 朴素 SSD 卸载无效：第 N+1 层专家选择依赖第 N 层输出，而第 N 层输出尚不存在时就需要开始加载，磁盘延迟无法隐藏在计算后面。
- **整体含义**：Edge0 将专家权重放在 SSD 上，按需 mmap 流式加载，并用“训练式路由预测”提前一个 token 预测下一层路由，从而把 SSD 读取隐藏到前向计算中。目标是让 35B 级 MoE 在单台 24 GB 消费级机器上以可接受速度运行，并尽量接近 fp16 教师模型质量。

##
