Title: Multi-Access Speculative Inference: Uplink or Downlink?

URL Source: https://arxiv.org/pdf/2608.29618v1

Published Time: Tue, 01 Sep 2026 01:11:19 GMT

Number of Pages: 13

Markdown Content:
> 1

# Multi-Access Speculative Inference: Uplink or Downlink? 

Chang Cai, Member, IEEE, Kaibin Huang, Fellow, IEEE 

Abstract —Multi-access speculative inference (Multi-SPIN) ex-tends SPIN to multi-device edge networks to accelerate coop-erative token generation. It allows on-device small language models (SLMs) to autoregressively draft multiple tokens for individual generation tasks, while an edge-server large language model (LLM) verifies them in parallel. The major communication overhead arises when a drafted token is rejected by the server, in which case sampling the correction token requires access to both the SLM-output draft distribution and the LLM-output target distribution over the full token vocabulary. Existing designs typically perform correction at the server by uploading the draft distribution, but transmitting a vocabulary-wide distri-bution creates a critical uplink (UL) bottleneck. Alternatively, the correction can be performed at the device by downloading the target distribution, leveraging the high transmission rates available on the downlink (DL). Motivated by this insight, we introduce communication-mode selection as a new design di-mension for Multi-SPIN. Specifically, each device can adaptively switch between the UL and DL modes to balance the UL bottleneck against the shared DL resource constraint, thereby relieving the overall communication burden. We formulate a sum-token-goodput maximization problem that jointly accounts for mode selection, draft-length control, and power allocation. For mode selection, we reveal a simple structure of its optimal solution, which enables an efficient search over the number of devices assigned to the UL mode, with the optimal transmit powers determined accordingly. For draft-length control, we develop a greedy-search-based algorithm that adapts the device-specific draft lengths to heterogeneous computation and commu-nication capabilities. Experimental results on both Qwen2.5 and DeepSeek-R1 model pairs across diverse tasks demonstrate that the proposed framework significantly improves token goodput by flexibly exploiting UL and DL resources. 

Index Terms —Speculative inference (SPIN), mode selection, large language models (LLMs), cooperative token generation. 

I. I NTRODUCTION 

Deploying large language model s (LLMs) at the wireless network edge enables inference to be performed closer to end users, offering a promising approach to supporting low-latency and privacy-preserving artificial intelligence (AI) ser-vices [1], [2]. On one hand, running large models directly on edge devices remains fundamentally challenging due to their stringent computation, memory, and energy constraints. On the other hand, relying exclusively on an edge server to provide LLM services can create a centralized inference bottleneck while leaving distributed computing resources at devices underutilized. This bottleneck is further exacerbated by the autoregressive nature of LLM decoding, where tokens are generated sequentially and thus a separate forward pass for          

> C. Cai and K. Huang are with the Department of Electrical and Computer Engineering, The University of Hong Kong, Hong Kong SAR (e-mail: changcai@hku.hk; huangkb@hku.hk). Corresponding author: K. Huang.

each token is needed. To address these limitations, speculative inference (SPIN) [3], [4] has recently emerged as a promising paradigm for collaborative edge LLM inference. The key idea of SPIN is to deploy a lightweight small language model 

(SLM) at the device to generate multiple draft tokens locally, and then use the server-side LLM to verify the drafted block in parallel [5], [6]. Because evaluating a sequence of drafted tokens concurrently requires only a single forward pass, SPIN significantly reduces the computational overhead at the server without sacrificing LLM-level inference quality. Furthermore, by offloading draft generation to devices, SPIN effectively har-nesses the computing resources distributed across the network edge. Existing efforts on collaborative edge inference have pri-marily focused on split inference [7], [8], where a large model is partitioned between the device and the edge server. In this paradigm, the device executes the early layers of the model and transmits intermediate features to the server, which then com-pletes the remaining computation [9]–[11]. By dynamically adjusting the split point, split inference can balance the compu-tation workloads between the devices and the server as well as control the wireless transmission overhead [12]. However, this layer-wise partitioning strategy faces inherent limitations when applied to autoregressive LLM decoding. Since the generation of each token depends on all preceding tokens, the device and the server must repeatedly execute their respective model partitions and exchange intermediate features at every single decoding step [13]. This continuous back-and-forth results in excessive computation and communication delays, limiting the suitability of split inference for latency-sensitive edge LLM services. In contrast, SPIN introduces a fundamental paradigm shift from sequential token generation to parallel token verification. By deploying a low-complexity SLM at the device to speculate on multiple future tokens, several inherently sequential decoding steps can be consolidated into a single parallel verification block performed by the server-side LLM. This paradigm shift substantially improves token-generation efficiency by minimizing the number of costly, memory-bound forward passes required at the server. Despite its computational advantages, deploying SPIN at the wireless edge introduces a new communication bottleneck. This bottleneck emerges not during the initial draft verifi-cation, but when a drafted token is rejected and a correc-tion token must be generated. During verification, acceptance checking only compares the draft probability assigned by the SLM to each drafted token against the corresponding target probability assigned by the LLM, without requiring the full draft or target distributions [3], [4]. Consequently, the device only needs to upload the drafted tokens and their arXiv:2608.29618v1 [cs.IT] 30 Aug 2026 2

associated scalar draft probabilities to the server. This incurs negligible communication overhead. If all drafted tokens are accepted, the server simply returns lightweight feedback to continue generation. However, when a drafted token is re-jected, a correction token must be sampled from a residual distribution. This residual distribution inherently depends on the full, vocabulary-wide output distributions of both the SLM and the LLM [3], [4]. Because modern LLMs feature massive vocabularies (e.g., 152,064 entries for Qwen2.5 [14] and 200,019 entries for GPT-4o [15]), exchanging these high-dimensional distributions across the wireless link to enable token correction incurs substantial communication overhead, termed token-correction overhead .Existing studies [16]–[20] mainly support the correction step in an uplink (UL) manner: the device uploads the draft distribution to the server, where the corresponding target dis-tribution is already available. We refer to this implementation as the UL mode of SPIN . To alleviate the resulting UL burden, prior works have explored uncertainty estimation to skip un-necessary UL transmissions [17], as well as quantization [18] and sparsification [19] techniques to compress the uploaded draft distributions. Despite these advances, draft-distribution uploading can still dominate the end-to-end (E2E) latency due to the limited UL rates of edge devices, thereby offsetting the acceleration gain of SPIN. In fact, sampling the correction token does not necessarily have to be performed at the server. Since the draft distribution is already available at the device, the correction step can alternatively be carried out at the device by downloading the target distribution from the server [21], [22]. We refer to this downlink (DL)-based implementation as the DL mode of SPIN . By shifting full-vocabulary distribution transmission from the UL to the DL, the DL mode can exploit the typically stronger DL capability of wireless edge systems and thereby bypass the UL bottleneck. In the point-to-point settings considered in existing studies, switching from the UL to the DL mode is straightforward. In a multi-user system, however, this DL advantage is no longer unconditional, as discussed below. In this work, we study multi-access SPIN (Multi-SPIN) to enable cooperative token generation in a multi-device edge network. In this architecture, each device employs a local SLM to draft tokens for its specific generation task, while the edge server batches the drafted token sequences from multiple devices for parallel verification using a shared LLM. Despite high DL rates, assigning all devices to the DL mode can severely overload the shared DL resources and eventually degrade the overall token-generation efficiency. This reveals a fundamental tension between the limited UL rates and the shared DL resource constraints. To resolve this tension, devices must adaptively switch between the UL and DL modes to optimally exploit heterogeneous wireless resources. This motivates us to introduce (communication) mode selection 

as a new design dimension for Multi-SPIN. Indeed, mode selection extends the core resource-utilization philosophy of Multi-SPIN from the computation domain to the communi-cation domain. Much like offloading draft generation to the devices harnesses distributed computing power, mode selection alleviates the shared DL bottleneck by adaptively shifting part of the transmission load to the UL. This perspective highlights mode selection as a natural and complementary mechanism for maximizing the E2E efficiency of Multi-SPIN. The choice of communication mode is inherently coupled with the control of draft lengths, which directly affects the token-generation efficiency. Specifically, the draft length de-termines the maximum number of tokens that can be accepted in a single verification round, the local drafting and server-side verification delays, and the probability of a draft rejection. This creates a fundamental tradeoff: while a longer draft enhances the potential speculative gain by exposing more tokens to parallel verification, it simultaneously increases the likelihood of rejection, thereby triggering the need for costly distri-bution transmissions. Consequently, the draft lengths must be determined jointly with the mode-selection mechanism, which is itself tightly intertwined with transmit-power allo-cation. These complex interdependencies necessitate a joint design of draft-length control, mode selection, and power allocation to maximize the sum token goodput, defined as the expected number of output/committed tokens per second across all devices. The introduction of this communication-mode-selection framework, together with the formulation and solution of the resulting joint design problem, constitutes the core contribution of this work, as summarized below.  

> •

UL/DL Mode Selection for Multi-SPIN: We propose a mode-selection-based Multi-SPIN protocol that enables each device with a rejected draft to perform correction via either the UL or DL mode. The resulting design problem is inherently stochastic because draft lengths must be selected at the beginning of each round, before the random verification outcomes are revealed. After the re-jected devices are identified, their communication modes and transmit powers are determined accordingly. We formulate the joint design of draft-length control, mode selection, and power allocation as a two-stage stochastic optimization problem for maximizing the expected sum token goodput. By exploiting the sequential decision structure, without loss of optimality, we decompose the problem into i) a subproblem of reactive mode selec-tion and power allocation for each realized verification outcome, and ii) a subproblem of proactive draft-length control that incorporates the optimized reactive decisions across all possible outcomes.  

> •

Optimal Reactive Mode Selection and Power Alloca-tion: We characterize the globally optimal reactive com-munication strategy for any realized verification outcome. By sorting the devices according to their minimum UL transmission delays, we prove that an optimal mode as-signment has a prefix structure: devices with smaller UL delays operate in the UL mode, while the remaining de-vices operate in the DL mode. This property reduces the original combinatorial optimization to a one-dimensional bisection search over the UL/DL split point. For each candidate mode split point, the optimal transmit powers are determined by maximum-power UL transmission and delay-equalizing DL power allocation. This yields the optimal low-complexity policy for joint mode selection and power allocation.  

> •

Proactive Draft-Length Control: We develop a greedy-search-based algorithm that optimizes device-specific 3LLM: Parallel verification SLM: Autoregressive decoding      

> Accepted Discarded
> LLM: Parallel verification SLM: Autoregressive decoding
> Accepted Discarded
> ✔✓✔✗✘
> SLM: Autoregressive decoding
> LLM: Parallel verification SLM: Autoregressive decoding
> Accepted Discarded
> Fig. 1. Autoregressive drafting and parallel verification in SPIN.

draft lengths under heterogeneous computation and com-munication conditions. Specifically, the design accounts for device-side computation heterogeneity, rejection un-certainty, and the communication latency under the op-timal reactive mode-selection and power-allocation deci-sions. To efficiently evaluate the expected token goodput, we employ a sample average approximation (SAA) ap-proach that avoids exhaustive enumeration of the expo-nentially many verification outcomes.  

> •

Experimental Results: We conduct extensive ex-periments using both Qwen2.5 [14] and DeepSeek-R1 [23] model pairs across diverse code-generation, mathematical-reasoning, conversational, and instruction-following tasks. The results show that the proposed framework consistently outperforms fixed-mode and con-ventional draft-length baselines by adaptively balancing UL/DL communication and tailoring draft lengths to heterogeneous system conditions. The remainder of this paper is organized as follows. Sec-tion II reviews the SPIN mechanism and discusses key in-sights for edge deployments. Section III presents the mode-selection-based Multi-SPIN protocol and formulates the two-stage token-goodput maximization problem. Section IV devel-ops the optimal reactive mode-selection and power-allocation solution. Section V proposes the proactive draft-length control algorithm. Section VI provides experimental results. Finally, we conclude this paper in Section VII. II. SPIN M ECHANISM AND EDGE DEPLOYMENT INSIGHTS 

In this section, we first review the core mechanism of SPIN [3], [4]. Based on this review, we then discuss key design insights for edge deployment. 

A. SPIN Mechanism 

Fig. 1 illustrates the draft-then-verify workflow of SPIN. The SLM serves as a lightweight proposal model for fast local drafting, whereas the LLM serves as the target model whose output distribution should be preserved. In particular, SPIN accelerates LLM inference by allowing the SLM to draft multiple tokens autoregressively and then using the LLM to verify the drafted block in parallel. Since the entire drafted block is available before verification, the LLM can evaluate all drafted positions through a single forward pass. The main steps of SPIN are described as follows. 1) Autoregressive drafting: Given the current prefix x, the SLM autoregressively generates a block of γ draft tokens, denoted by y ≜ (y1, . . . , y γ ). Specifically, at the j-th drafting step, the SLM takes x and y<j ≜ (y1, . . . , y j−1)

as input and performs one forward pass to obtain the draft distribution πS 

> j

(· | x, y<j ). The draft token yj is then sampled from this distribution as 

yj ∼ πS 

> j

(· | x, y<j ), j = 1 , . . . , γ. (1) 2) Parallel verification: Given the current prefix x and the drafted block y, the LLM performs a single forward pass to obtain the target distributions for all drafted positions and the next position, given by 

πL 

> j

(· | x, y<j ), j = 1 , . . . , γ + 1 . (2) After obtaining these target distributions, SPIN performs acceptance checking for the drafted tokens. Specifically, for the j-th drafted token yj , SPIN compares the target probability assigned by the LLM, denoted by πL 

> j

(yj ) ≜

πL 

> j

(yj | x, y<j ), with the draft probability assigned by the SLM, denoted by πS 

> j

(yj ) ≜ πS 

> j

(yj | x, y<j ). The token yj is accepted if 

uj < min 1, πL 

> j

(yj )

πS 

> j

(yj )

!

, uj ∼ Unif(0 , 1) , (3) where Unif(0 , 1) denotes the uniform distribution over the interval (0 , 1) .3) Bonus-/correction-token sampling: Once the verifica-tion outcomes are determined, SPIN proceeds according to the following two cases:  

> •

Case I (all drafted tokens accepted): If all γ drafted tokens are accepted, the next-position target distribu-tion πL

> γ+1

(·) ≜ πL

> γ+1

(· | x, y) is used to sample one additional bonus token as 

yγ+1 ∼ πL

> γ+1

(·). (4) The accepted draft tokens and the bonus token are ap-pended to the prefix. The current round then terminates, and the next round starts from the updated prefix.  

> •

Case II (rejection occurs): Otherwise, let r denote the position of the first rejected token. Then, all drafted tokens before position r are accepted. The drafted token at position r is replaced by a correction token sampled from the residual distribution. Specifically, 

y′ 

> r

∼ norm  max  0, π L 

> r

(·) − πS 

> r

(·) , (5) where norm( ·) denotes the normalization operator that rescales a nonnegative function over the vocabulary into a valid probability distribution. All drafted tokens after position r are discarded. The accepted draft to-kens before r and the correction token y′ 

> r

are appended to the prefix before the next round starts. In this way, SPIN preserves the exact target distribution of the LLM while allowing multiple tokens to be generated in one round. 

B. Key Insights for Edge Deployment 

We next identify the information required at different steps of SPIN, which provides the basis for characterizing the communication overhead of deploying SPIN at the wireless edge. 4Server       

> DL mode UL mode Batched verification
> All accepted
> Server
> Device 1 vice 2 Device K
> DL mode UL mode Batched verification All accepted
> Server
> Device 1 Device 2 Device K
> DL mode UL mode Batched verification All accepted Autoregressive drafting Draft Accepted Rejected Padding
> Fig. 2. A Multi-SPIN system with communication-mode selection.

Observation 1. In parallel verification, acceptance checking requires only the scalar draft and target probabilities assigned to each drafted token. If all drafted tokens are accepted, bonus-token sampling requires the next-position target distribution from the LLM, without any additional information from the SLM. By contrast, when a drafted token is rejected, correction-token sampling requires access to both the full draft and target distributions at the rejection position. The above observation suggests that, when all drafted tokens are accepted, the communication overhead is negligible. In this case, the device only needs to upload the drafted tokens and their corresponding scalar draft probabilities, while the server only needs to return the bonus token. The critical communi-cation bottleneck arises upon token rejection, since correcting the rejected token requires transmitting a vocabulary-wide distribution. 

Observation 2. Since the draft distribution is available at the device while the target distribution is available at the server, correction-token sampling can be performed either at the server via draft-distribution uploading or at the device via target-distribution downloading. We refer to the above two reciprocal implementations as the UL mode and the DL mode of SPIN, respectively. In practical cellular systems, the DL rate is often higher than the UL rate because the server typically has a larger transmit power budget than edge devices. This makes the DL mode more attractive for reducing the distribution-transmission delay. However, this DL advantage does not directly extend to multi-access systems. When multiple devices require correction-token sampling in the same round, assigning all of them to the DL mode forces them to compete for the server’s shared DL transmit power, which can substantially reduce the per-device DL rate. In this case, shifting a suitable subset of devices to the UL mode can better exploit available device-side UL capabilities and significantly relieve the shared DL burden. Therefore, adaptive UL/DL mode selection becomes essential for balancing link-rate asymmetry against shared-resource competition, which motivates the work presented in this paper. III. C OMMUNICATION -M ODE SELECTION FOR 

MULTI -SPIN As shown in Fig. 2, we consider a Multi-SPIN system for LLM inference services, where K edge devices perform independent token generation tasks with the assistance of an edge server. Specifically, each device sequentially generates multiple draft tokens using its local SLM, and the edge server verifies them in parallel using an LLM. This design exploits both intra-request and inter-request parallelism: the server verifies multiple drafted tokens within each request in parallel, while simultaneously batching requests across devices [24]– [26]. In this way, multiple tokens across multiple devices can be accepted in a single LLM forward pass. After the verification step, any device whose draft is rejected must per-form an additional correction procedure before it can proceed. Such correction can be supported by exchanging the required distribution information in either the UL mode, where the draft distribution is uploaded to the server, or the DL mode, where the target distribution is downloaded to the device. This flexibility reveals mode selection as a unique design dimension in Multi-SPIN. 

A. Mode-Selection-Based Multi-SPIN Protocol 

In this subsection, we develop the mode-selection-based Multi-SPIN protocol, which comprises a set of common steps followed by outcome-dependent operations. 

1) Common Steps: At the beginning of each token genera-tion round, the server determines the draft length γk for each device and informs the devices accordingly. The protocol then proceeds through the following common steps: 1) On-device autoregressive drafting: For each device 

k ∈ K ≜ {1, . . . , K }, given the current prefix xk, the local SLM autoregressively generates a block of γk draft tokens, denoted by yk ≜ (yk, 1, . . . , y k,γ k ). Specifically, the j-th token is sampled from the draft distribution as 

yk,j ∼ πS 

> k,j

(·) ≜ πS 

> k,j

(· | xk, yk,<j ), j = 1 , . . . , γ k, (6) where yk,<j ≜ (yk, 1, . . . , y k,j −1).2) Drafts uploading: Device k uploads the drafted token– probability pairs {yk,j , π S 

> k,j

(yk,j )}γk 

> j=1

to the server. 3) Server-side batched parallel verification: Given the current prefixes {xk}k∈K and the drafted blocks {yk}k∈K 

from all devices, the edge server uses the LLM to evaluate the drafted blocks in parallel across both devices and drafted positions. Specifically, for each device k ∈ K ,the LLM outputs the target distributions for all drafted positions and the next position as 

πL 

> k,j

(·) ≜ πL 

> k,j

(· | xk, yk,<j ), j = 1 , . . . , γ k + 1 . (7) For each drafted token yk,j , the server accepts it if 

uk,j < min 1, πL 

> k,j

(yk,j )

πS 

> k,j

(yk,j )

!

, uk,j ∼ Unif(0 , 1) . (8) Based on these token-wise acceptance decisions, we define the verification outcome for device k as 

ok =

(

0, if all drafted tokens are accepted ,

1, otherwise , (9) Collecting the device-wise outcomes yields o ≜

[o1, . . . , o K ]⊤. The corresponding set of devices with rejected drafts is then defined as R(o) ≜ {k ∈ K | ok = 1 }.For a realized verification outcome, we write R in place of 5All accepted Drafting Drafts uploading  

> Device Server
> Verification Bonus-token sampling

★ 

> Bonus-token downloading Rejection occurs Drafting Drafts uploading
> Device Server
> Verification Rejection-position downloading

★

> Correction-token sampling Draft-distribution uploading Correction-token downloading

Fig. 3. Protocol flow when all drafted tokens from a device are accepted. 

R(o) when no ambiguity arises. The subsequent operations are described separately for devices with all drafted tokens accepted, i.e., k ∈ K \ R , and devices with at least one rejected token, i.e., k ∈ R .

2) Devices with All Drafted Tokens Accepted: For each device k ∈ K\R , the protocol proceeds as illustrated in Fig. 3 and described below: 4A) Bonus-token sampling: The server samples an additional bonus token from the target distribution as 

yk,γ k +1 ∼ πL 

> k,γ k+1

(·). (10) 5A) Bonus-token downloading: The server sends the bonus token yk,γ k +1 to device k. The device and server append the accepted drafted tokens and the bonus token to their respective copies of the prefix. The current round then terminates, and the next round starts from the updated prefix. 

3) Devices with Rejected Drafts: We next describe the correction procedure for devices with rejected drafts. For each device k ∈ R , the server selects its communication mode mk

and associated transmit power pk, where 

mk =

(

0, if device k operates in the UL mode ,

1, if device k operates in the DL mode . (11) Here, pk denotes the UL transmit power of device k when 

mk = 0 , and the DL transmit power allocated by the server to device k when mk = 1 .As illustrated in Fig. 4(a), in the UL mode, correction is performed at the server after the device uploads its draft distribution. Devices assigned to this mode proceed as follows: 4U) Rejection-position downloading: The server sends the index rk of the first rejected token to device k.5U) Draft-distribution uploading: Device k sends the draft distribution πS 

> k,r k

(·) at position rk to the server via the UL. 6U) Server-side correction: The server samples the correc-tion token at position rk from the residual distribution as 

y′ 

> k,r k

∼ norm  max  0, π L 

> k,r k

(·) − πS 

> k,r k

(·) . (12) All accepted Drafting Drafts uploading  

> Device Server
> Verification Bonus-token sampling

★ 

> Bonus-token downloading Rejection occurs Drafting Drafts uploading
> Device Server
> Verification Rejection-position downloading

★

> Correction-token sampling Draft-distribution uploading Correction-token downloading

(a) UL mode Rejection occurs Drafting Drafts uploading  

> Device Server
> Verification Rejection-position downloading

★ 

> Correction-token sampling Draft-distribution uploading Correction-token downloading Rejection occurs Drafting Drafts uploading
> Device Server
> Verification Target-distribution downloading

★

> Correction-token sampling Correction-token uploading

(b) DL mode Fig. 4. Protocol flows when a drafted token from a device is rejected, with correction performed in the (a) UL mode and (b) DL mode. 

7U) Correction-token downloading: The server sends the correction token y′ 

> k,r k

to device k. The device and server append the accepted drafted tokens before position rk

and the correction token to their respective copies of the prefix. The current round then terminates, and the next round starts from the updated prefix. As illustrated in Fig. 4(b), in the DL mode, correction is performed at the device after it receives the target distribution from the server. Devices assigned to this mode proceed as follows: 4D) Target-distribution downloading: The server sends the first rejection position rk and the corresponding target distribution πL 

> k,r k

(·) to device k via the DL. 5D) On-device correction: Device k samples the correction token at position rk from the residual distribution in (12). 6D) Correction-token uploading: Device k sends the cor-rection token y′ 

> k,r k

to the server. The device and server append the accepted drafted tokens before position rk

and the correction token to their respective copies of the prefix. The current round then terminates, and the next 6Device 1 Draft Draft-dist. uploading Draft Draft Target-dist. downloading      

> Device 2 Device 3 One round Device 1 Draft Draft-dist. uploading Draft Draft Target-dist. downloading
> Verification Device 2 Device 3 Server Draft-length control Mode selection and power allocation One round
> Fig. 5. Timeline of one synchronized round in the mode-selection-based Multi-SPIN protocol. For clarity, lightweight signaling steps are omitted.

round starts from the updated prefix. Fig. 5 illustrates the round-level timing of the proposed pro-tocol under a synchronized implementation. At the beginning of each round, the server proactively selects the draft length 

γk for each device k and informs the devices accordingly. The devices then perform local drafting, while the server waits until all drafted token–probability pairs have been received before conducting batched parallel verification. After the verification outcomes are revealed, only devices with rejected drafts par-ticipate in distribution transmission, and their communication modes mk and transmit powers pk are determined reactively. For clarity, Fig. 5 omits the lightweight signaling steps, such as drafts uploading, rejection-position feedback, bonus-token downloading, and correction-token exchange, whose over-head is negligible compared with vocabulary-wide distribution transmission. This proactive–reactive timing structure forms the basis of the two-stage optimization developed later. 

B. Computation Throughput and Latency 1) Expected Number of Output Tokens: We denote the draft-length vector by γ ≜ [γ1, . . . , γ K ]⊤. Let Lk(γk) denote the number of output/committed tokens generated for device 

k in one round, including the accepted drafted tokens and one additional bonus or correction token. The total number of output tokens across all devices is 

L(γ) ≜ X

> k∈K

Lk(γk). (13) Following [3], [4], we assume that, for each device k, the acceptance events of its drafted tokens are independent and identically distributed (i.i.d.) with acceptance probability αk,and are independent across devices. Under this model, the expected number of output tokens in one Multi-SPIN round is given by [3] 

¯L(γ) ≜ E[L(γ)] = X

> k∈K

E[Lk(γk)] = X

> k∈K

1 − αγk +1 

> k

1 − αk

. (14) 

2) Computation Latency: We characterize the computation latency of local drafting and server-side verification as follows. Device k autoregressively generates γk draft tokens, incurring a drafting latency of γkT dft  

> k

, where T dft  

> k

is the average per-token generation latency. Since the server waits for all devices before verification, the overall drafting latency is 

T dft (γ) = max 

> k∈K

γkT dft  

> k

. (15) Verification is then performed by the server-side LLM in a batched manner. As shown in Fig. 2, the LLM forward pass ex-ploits parallelism along two dimensions: draft positions within each request and requests across devices. For the moderate draft lengths commonly used in SPIN, the verification latency depends only weakly on the draft length and is primarily affected by the batch size, i.e., the number of device requests verified together [24]–[26]. Following [20], we model the batched verification latency as 

T ver = T ver 0 + KT ver 1 , (16) where T ver 0 captures the fixed latency of one batched LLM verification pass, and T ver 1 captures the incremental latency introduced by each additional device in the batch. The total computation latency in one round is therefore 

T comp (γ) = T dft (γ) + T ver . (17) 

C. Communication Overhead and Latency 1) Communication Overhead: As discussed earlier, the distribution transmission required for correction-token sam-pling constitutes the dominant communication overhead in Multi-SPIN. We therefore focus on this cost and neglect the signaling overhead of other protocol operations. Upon a rejection, the selected communication mode deter-mines whether the draft distribution or the target distribution is transmitted. Since both distributions are defined over the same vocabulary, transmitting either distribution incurs the same overhead. For typical LLMs, the vocabulary size is on the order of tens of thousands or even larger; for example, Qwen2.5 has 152,064 vocabulary entries [14], while GPT-4o has 200,019 vocabulary entries [15]. In practice, since many tokens have near-zero probabilities [18], token distributions are effectively sparse after truncation. Hence, it suffices to transmit only the effective support of the distribution, whose size is typically on the order of hundreds or thousands. Let V denote the vocabulary size, V eff the effective support size, and Q the number of quantization bits used for each probability value. Each transmitted entry contains a token index and its quantized probability, requiring ⌈log 2 V ⌉ + Q bits. Accordingly, for each device with a rejected draft, the distribution-transmission overhead in one round is V eff (⌈log 2 V ⌉ + Q) bits. 

2) Communication Latency: We consider an orthogonal frequency-division multiple access (OFDMA) system in which each device k ∈ R is allocated an orthogonal bandwidth Bk.The allocated bandwidth is used for either UL or DL distri-bution transmission according to the selected communication mode. For simplicity, the bandwidth allocation is treated as given. Each device and the server are equipped with a single antenna. Let hk denote the channel coefficient between device 

k and the server. We adopt a block-fading channel model, under which hk remains constant within each Multi-SPIN round and varies independently across rounds. The channel 7

coefficients are assumed known at the server at the beginning of each round, before the draft-length decision is made. Ac-cordingly, the optimization in the sequel is conditioned on the current channel realization, whose dependence is suppressed for notational simplicity. The achievable communication rate of device k is 

Rk(mk, p k) = 



Bk log 2



1 + pk |hk |2  

> Nser 0Bk



, mk = 0 

Bk log 2



1 + pk |hk |2  

> Ndev 0,k Bk



, mk = 1 , (18) where N ser 0 and N dev 0,k denote the noise power spectral density at the server and device k, respectively. For each device k ∈ R , the latency for transmitting the required distribution is 

T comm  

> k

(mk, p k) = V eff (⌈log 2 V ⌉ + Q)

Rk(mk, p k) . (19) Since the distribution transmissions of the devices in R are performed simultaneously over orthogonal subcarriers, the overall communication latency is determined by the slowest device, i.e., 

T comm (A; o) = max  

> k∈R (o)

T comm  

> k

(mk, p k), (20) where A ≜ (mR, pR) denotes the reactive communication action, with mR ≜ [mk]k∈R and pR ≜ [pk]k∈R collecting the mode-selection and power-allocation decisions for the devices with rejected drafts, respectively. When the rejection set is clear from the context, we omit the subscript R and simply write m and p. When R(o) = ∅, all devices have their drafted tokens accepted and no distribution transmission is required. Accordingly, we define T comm (A; o) = 0 .

D. Token-Goodput Maximization Problem 

We now formulate the joint optimization of communication modes, draft lengths, and transmit powers to maximize the expected sum token goodput. As described in Section III-A, these decisions are made at different stages of each generation round. Specifically, the draft lengths are selected proactively at the beginning of each round before the verification outcomes are known. After the devices with rejected drafts are identified, their communication modes and transmit powers are deter-mined reactively. This sequential decision process naturally leads to a two-stage proactive–reactive optimization problem. For a given draft-length vector γ, verification outcome o,and reactive action A, the latency of one Multi-SPIN round is 

T (γ, A; o) = T comp (γ) + T comm (A; o). (21) Because γ is selected before verification, the round latency remains unknown when the draft-length decision is made. In particular, the verification outcome o, whose distribution depends on γ, determines which devices require correction and hence the incurred communication latency. Therefore, the performance of a draft-length decision should be evaluated in terms of the expected round latency over all possible veri-fication outcomes. To characterize this expected latency, we define a reactive policy χ that maps each realized verification outcome to a feasible communication action, i.e., A = χ(o).Then, the expected round latency under γ and χ is 

¯T (γ, χ ) ≜ Eo|γ

T  γ, χ (o); o 

= T comp (γ) + Eo|γ

T comm  χ(o); o , (22) where the expectation is taken over the random verification outcomes o induced by γ.Combining the expected number of output tokens in (14) with the expected round latency, we define the expected sum token goodput under the proactive draft-length vector γ and the reactive communication policy χ as 

G(γ, χ ) ≜ ¯L(γ)¯T (γ, χ ) . (23) We next specify the feasibility constraints on the proactive and reactive decisions. The draft length of each device must satisfy 

γk ∈ Z≥0, ∀k ∈ K , (24) where Z≥0 denotes the set of nonnegative integers. For each realized verification outcome o, the reactive policy χ selects a communication action χ(o) = ( m, p) from the feasible action region 

A(o) ≜



(m, p)

mk ∈ { 0, 1}, ∀k ∈ R (o),pk ≥ 0, ∀k ∈ R (o),

(1 − mk)pk ≤ P ul  

> k

, ∀k ∈ R (o),

P 

> k∈R

mkpk ≤ P dl .



.

(25) The third constraint limits the transmit power of device k when it operates in the UL mode, whereas the last constraint imposes the shared DL power budget at the server. Accordingly, the set of feasible reactive policies is defined as 

X ≜ χ | χ(o) ∈ A (o), ∀o ∈ { 0, 1}K . (26) The joint optimization of proactive draft lengths and reactive communication decisions is therefore formulated as the fol-lowing two-stage expected sum-token-goodput maximization problem: 

max  

> γ,χ

G(γ, χ ) (27a) 

s. t. γk ∈ Z≥0, ∀k ∈ K , (27b) 

χ ∈ X . (27c) The two stages are coupled through the verification outcomes. Specifically, the proactive draft-length vector γ determines the distribution of verification outcomes o, while each realized outcome determines the set of devices requiring correction and hence the corresponding reactive communication problem. This structure allows the original problem to be decomposed, without loss of optimality, into two subproblems. The first is a reactive mode-selection and power-allocation subproblem solved for each realized outcome. The second is a proactive draft-length control subproblem that incorporates the opti-mized reactive decisions over all possible outcomes. 8

1) Reactive Mode Selection and Power Allocation: We first derive the reactive communication problem for a predeter-mined draft-length vector γ. In this case, both the expected number of output tokens ¯L(γ) and the computation latency 

T comp (γ) are independent of the reactive policy χ. There-fore, maximizing the expected sum token goodput over χ is equivalent to minimizing the expected communication latency: 

min  

> χ∈X

Eo|γ

T comm  χ(o); o . (28) The minimization is separable across verification outcomes, and the optimal reactive action can be determined indepen-dently for each realization. It follows that 

min  

> χ∈X

Eo|γ

T comm  χ(o); o = Eo|γ



min  

> A∈A (o)

T comm  A; o

.

(29) Accordingly, for a realized outcome with R(o)̸ = ∅, the optimal reactive action can be obtained by solving 

T comm min (o) ≜ min  

> m,p

max  

> k∈R (o)

T comm  

> k

(mk, p k) (30a) 

s. t. (m, p) ∈ A (o). (30b) When R(o) = ∅, we simply define T comm min (o) = 0, and no reactive communication decision is required. The above formulation leads to a mixed-integer optimization problem, in which the binary mode-selection variables m are jointly optimized with the continuous power-allocation variables p.The shared DL power constraint further couples the decisions of all devices assigned to the DL mode. Moreover, the number of possible mode assignments grows exponentially with the number of devices, making exhaustive search computationally prohibitive. 

2) Proactive Draft-Length Control: Under the optimal re-active policy, the communication latency associated with each verification outcome o is T comm min (o). Hence, for a given draft-length vector γ, the minimum expected round latency is 

T comp (γ) + Eo|γ [T comm min (o)] . (31) Substituting (31) into the joint formulation yields the proactive draft-length control problem: 

max 

> γ

¯L(γ)

T comp (γ) + Eo|γ [T comm min (o)] (32a) 

s. t. γk ∈ Z≥0, ∀k ∈ K . (32b) This formulation captures the fundamental tradeoff in draft-length control. Longer drafts can increase the expected num-ber of output tokens, but they also reshape the verification-outcome distribution and affect both the computation latency and the expected communication latency. However, solving this stochastic optimization problem is generally challenging. In particular, direct evaluation of the objective requires av-eraging over an exponentially large outcome space, and for each outcome, solving the associated reactive communication problem to obtain T comm min (o). This tight coupling renders draft-length optimization intrinsically dependent on the underlying reactive problem. IV. R EACTIVE MODE SELECTION AND POWER 

ALLOCATION 

In this section, we solve the reactive mode-selection and power-allocation problem under a given verification outcome. We first derive the optimal power allocation for any fixed mode-selection strategy. Then, by exploiting structural prop-erties of the optimal mode selection, we reduce the original combinatorial problem to a one-dimensional bisection search, which yields the optimal mode selection and power allocation jointly. 

A. Optimal Power Allocation 

For a fixed mode selection, let U ≜ {k ∈ R | mk = 0 } and 

D ≜ {k ∈ R | mk = 1 } denote the corresponding UL and DL user sets, respectively. We have U ∪ D = R. For a nonempty UL user set U, each device transmits at its maximum available power to minimize the transmission latency, which is given by 

τ ul = max 

> k∈U



V eff (⌈log 2 V ⌉ + Q)

Bk log 2



1 + P ul     

> k|hk|2
> Nser 0Bk

 . (33) For a nonempty DL user set D, the objective is to minimize the maximum user delay through DL power allocation under the sum-power constraint: 

min 

> pD

max 

> k∈D



V eff (⌈log 2 V ⌉ + Q)

Bk log 2



1 + pk |hk |2  

> Ndev 0,k Bk

 (34a) 

s. t. pk ≥ 0, ∀k ∈ D , (34b) 

X

> k∈D

pk ≤ P dl , (34c) where pD ≜ [pk]k∈D collects the transmit powers for devices operating in the DL mode. The optimal DL power allocation admits a closed-form solution parameterized by the resulting DL delay τ dl . The result is summarized in the following lemma. 

Lemma 1 (Optimal DL Power Allocation) . For any given nonempty DL user set D̸ = ∅, problem (34) admits an optimal solution of the form 

p⋆k = N dev 0,k Bk

|hk|2



2     

> Veff (⌈log 2V⌉+Q)
> Bk τdl

− 1



, ∀k ∈ D , (35) where τ dl > 0 is the unique solution to 

X

> k∈D

N dev 0,k Bk

|hk|2



2     

> Veff (⌈log 2V⌉+Q)
> Bk τdl

− 1



= P dl . (36) The resulting minimum DL delay τ dl can be efficiently computed via a bisection search. 

Proof: The proof follows the max-delay equalization argument for power allocation under a sum-power constraint; see, e.g., classical results in convex optimization [27] and wireless max-min fairness power control [28]. The detailed derivations are omitted for brevity. 9

B. Optimal Mode Selection and Power Allocation 

We now turn to the optimization of the mode-selection vector m. Although the original problem is binary and com-binatorial, the optimal solution has a prefix structure after sorting the devices according to their minimum UL delays. Importantly, the mode-selection search is performed using the optimized delay values obtained from Lemma 1; hence the power allocation is embedded in each mode-selection evaluation. For each device k, define its minimum UL delay as 

τ ul  

> k

≜ V eff (⌈log 2 V ⌉ + Q)

Bk log 2



1 + P ul     

> k|hk|2
> Nser 0Bk

 . (37) Without loss of generality, upon observing the verification outcomes, we re-index the devices in R as {1, . . . , |R|} . This allows us to focus exclusively on the subset of devices that require correction in the subsequent analysis. We further sort them in non-decreasing order of their minimum UL delays: 

τ ul 1 ≤ τ ul 2 ≤ · · · ≤ τ ul 

> |R|

. (38) For any n ∈ { 0, 1, . . . , |R|} , define 

U(n) ≜ {1, 2, . . . , n }, (39) 

D(n) ≜ {n + 1 , n + 2 , . . . , |R|} . (40) That is, the first n devices in the above ordering are assigned to the UL mode, and the remaining |R| − n devices are assigned to the DL mode. For a given n, the minimum feasible UL delay is 

τ ul (n) ≜ max  

> k∈U (n)

τ ul  

> k

= τ ul  

> n

, n ≥ 1, (41) with the convention τ ul (0) = 0 . Similarly, let τ dl (n) denote the optimized DL delay obtained by applying Lemma 1 to the nonempty DL user set D(n), with the convention τ dl (|R| ) = 0. The resulting optimized communication delay under this partition is therefore 

τ (n) = max {τ ul (n), τ dl (n)}. (42) The following lemma shows that it suffices to restrict the search to this family of prefix-structured partitions to obtain the optimal mode-selection solution. 

Lemma 2 (Prefix Structure of Optimal Mode Selection) .

There exists an optimal mode selection such that the UL device set is of the prefix form U(n) = {1, 2, . . . , n } for some n ∈ { 0, 1, . . . , |R|} . The corresponding DL device set is D(n) = {n + 1 , n + 2 , . . . , |R|} .

Proof: Please refer to Appendix A. By Lemma 2, the original combinatorial mode-selection problem can be reduced to a one-dimensional search over the UL/DL split point n. This scalar search can be further reduced to a bisection search by exploiting the opposite monotonicity of the DL and UL delays. To characterize this structure, define the DL–UL delay difference as 

∆τ (n) ≜ τ dl (n) − τ ul (n). (43)              

> Fig. 6. Illustration of the monotonic structure of UL and DL delays for a Multi-SPIN round with |R| =8devices participating in distribution transmission. The optimal UL/DL split point is determined by the intersection of the two delay curves, where the candidate solutions n= 3 and n= 4 are compared, yielding the optimal solution n⋆= 3 .

As n increases, more devices are assigned to the UL mode. Hence, τ ul (n) is non-decreasing in n, while τ dl (n) is non-increasing in n, because the DL set becomes smaller and the same total DL power budget is shared by fewer devices. Consequently, ∆τ (n) is non-increasing in n.

Proposition 1 (Optimal Mode Selection via Bisection Search) .

Let ¯n ≜ min {n : ∆ τ (n) ≤ 0} be the first integer at which the delay difference becomes non-positive. Then the optimal UL/DL split point is given by 

n⋆ = arg min  

> n∈{ ¯n−1,¯n}∩{ 0,1,..., |R|}

τ (n). (44) Moreover, ¯n can be efficiently obtained via a bisection search over n ∈ { 0, 1, . . . , |R|} .

Proof: Please refer to Appendix B. Fig. 6 shows the UL and DL delays as functions of the number of UL-mode devices. The optimal mode split is determined by comparing the candidate values around the intersection point, as formalized in Proposition 1. 

Remark 1 (Coupling Between Optimal Mode Selection and Power Allocation) . The bisection search in Proposition 1 is performed over optimized delay values, rather than over fixed-power delay values. Specifically, for each queried value of 

n, the UL delay τ ul (n) is obtained from maximum-power UL transmission, while the DL delay τ dl (n) is obtained by solving the DL power-allocation problem in Lemma 1 over the current DL set D(n). Thus, each evaluation of ∆τ (n)

implicitly invokes the optimal power-allocation solution. After the optimal n⋆ is determined, the corresponding optimal mode selection is given by the prefix structure U(n⋆) and D(n⋆),and the associated DL powers are exactly those returned by Lemma 1. Therefore, the proposed method jointly obtains the optimal mode selection and power allocation, rather than first fixing the modes and then optimizing the powers. V. P ROACTIVE DRAFT -L ENGTH CONTROL 

In this section, we develop a greedy-search-based algorithm for draft-length control. The main idea is to start from an initial draft-length vector and progressively increase the draft 10 

lengths until no candidate update can further improve the expected sum token goodput. However, due to the stochastic nature of the objective and its dependence on the minimum expected communication latency, the resulting optimization problem is highly non-convex and cannot be solved in closed form. Moreover, the evaluation of each candidate draft-length configuration requires solving the underlying reactive commu-nication problem, which itself does not admit a closed-form solution. 

A. Algorithm Overview 

To address the above challenges, we design a greedy op-timization procedure for draft-length control. The algorithm first performs synchronized all-ones updates to rapidly exploit global improvements in draft lengths across all devices. Af-ter convergence of the first phase, a coordinate-wise greedy refinement is applied to capture residual gains by selectively increasing individual draft lengths. The two phases are detailed as follows. 

1) Synchronized All-Ones Updates: The algorithm first performs synchronized all-ones updates. Let 1 ∈ RK denote the all-ones vector of length K. Starting from the current draft-length vector γ, the algorithm evaluates the candidate update 

γ′ = γ + 1. (45) Let χ⋆ be the optimal reactive policy derived in Section IV. If 

G(γ′, χ ⋆) > G (γ, χ ⋆), the update γ ← γ′ is accepted. This procedure is repeated until no synchronized all-ones update can further improve the expected goodput. 

2) Coordinate Greedy Refinement: After the synchronized phase terminates, the algorithm proceeds with coordinate greedy refinement. Let ek ∈ RK denote the k-th unit vector. At each iteration, the algorithm evaluates the candidate updates 

γ′ 

> k

= γ + ek, k ∈ K . (46) The device yielding the largest expected goodput is selected as 

k⋆ = arg max  

> k∈K

G(γ′

> k

, χ ⋆). (47) If G(γ′ 

> k⋆

, χ ⋆) > G (γ, χ ⋆), the update γ ← γ′ 

> k⋆

is accepted; otherwise, the coordinate greedy phase terminates. The com-plete procedure is summarized in Algorithm 1. As detailed in Section V-B, every evaluation of G(γ, χ ⋆) in Algorithm 1 is implemented using the SAA estimate. 

B. Expected Goodput Evaluation 

The remaining challenge is the evaluation of the expected goodput, which requires computing the expected minimum communication latency. Formally, we have 

Eo|γ [T comm min (o)] = X

> o∈{ 0,1}K

Pr( o | γ)T comm min (o) , (48) where Pr( o | γ) = Q 

> k∈K

(αγk 

> k

)1−ok (1 − αγk 

> k

)ok . The above expression involves an exponential number of outcomes, since 

o ∈ { 0, 1}K yields 2K possible realizations. Moreover, the term T comm min (o) does not admit a closed-form expression due to its dependence on the underlying mode-selection and power-allocation decisions. 

Algorithm 1: Greedy Search for Draft-Length Control  

> 1:

Input: Optimal reactive policy χ⋆ 

> 2:

Initialize: γ = 0 

> 3:

% Synchronized all-ones updates  

> 4:

while G(γ + 1, χ ⋆) > G (γ, χ ⋆) do  

> 5:

γ ← γ + 1 

> 6:

end while  

> 7:

% Coordinate greedy refinement  

> 8:

while ∃k ∈ K such that G(γ + ek, χ ⋆) > G (γ, χ ⋆) do  

> 9:

k⋆ = arg max k∈K G(γ + ek, χ ⋆) 

> 10:

γ ← γ + ek⋆ 

> 11:

end while  

> 12:

Output: Optimized draft lengths γ

To overcome these difficulties, we adopt a SAA approach. Specifically, we draw NSAA independent samples {o(ℓ)}NSAA 

> ℓ=1

according to Pr( o | γ), and approximate the expectation as 

Eo|γ [T comm min (o)] ≈ 1

NSAA 

> NSAA

X

> ℓ=1

T comm min 

 o(ℓ). (49) By the law of large numbers, the sample average in (49) converges to the expected minimum communication latency as NSAA increases. The SAA approach therefore provides a consistent estimate while reducing the computational cost from enumerating all 2K possible verification outcomes to evaluating only NSAA sampled outcomes. The resulting SAA estimate is used to evaluate the goodput objective throughout Algorithm 1. VI. E XPERIMENTAL RESULTS 

A. Experimental Settings 1) Models and Tasks: We consider two draft–target model pairs: Qwen2.5-0.5B with Qwen2.5-14B [14], and DeepSeek-R1-Distill-Qwen-1.5B with DeepSeek-R1-Distill-Qwen-32B [23]. For both model pairs, the draft and target models share the same tokenizer, with V = 151 ,665 valid token IDs. 1 Each device is independently assigned one of four representative generation tasks uniformly drawn from Hu-manEval [29], GSM8K [30], MT-Bench [31], and IFEval [32], covering code generation, mathematical reasoning, multi-turn conversation, and instruction following, respectively. For each model pair and task, we estimate the empirical token accep-tance rate by running SPIN on a set of sampled prompts and averaging the token-wise acceptance probabilities over all decoding rounds and prompts. The resulting estimates are summarized in Table I. 

2) Computation Settings: The edge server is equipped with an NVIDIA A100 GPU and executes the target model for batched draft verification. For each target model, we measure the verification latency at different batch sizes and fit the model in (16). The fitted parameters are T ver 0 = 42 .8 ms and T ver 1 = 7.6 ms for Qwen2.5-14B, and T ver 0 = 69 .3

ms and T ver 1 = 9 .1 ms for DeepSeek-R1-Distill-Qwen-32B.               

> 1Although their model configurations report output dimensions of either 151,936 or 152,064, the additional dimensions correspond to padding intro-duced for distributed training rather than distinct tokenizer entries [14], [23]. 11
> TABLE I EMPIRICAL ACCEPTANCE RATE FOR DIFFERENT DATASETS AND MODEL
> PAIRS
> Qwen2.5 pair DeepSeek-R1 pair HumanEval 0.9453 0.8691 GSM8K 0.8580 0.8618 MT-Bench 0.8509 0.8221 IFEval 0.8816 0.8323

The reference per-token drafting latencies of Qwen2.5-0.5B and DeepSeek-R1-Distill-Qwen-1.5B, measured on the same GPU, are 15.2 ms and 19.6 ms, respectively. To capture heterogeneity in device-side computing capabilities, we in-dependently scale the drafting latency of each device by a factor uniformly drawn from [1 , 1.15] , resulting in device-specific per-token drafting latencies T dft  

> k

. For the SAA-based draft-length optimization, we use NSAA = 1000 verification-outcome samples for each goodput evaluation. 

3) Communication Settings: We consider an OFDMA sys-tem with a total of K devices sharing a bandwidth of B = 10 

MHz, where K = 20 unless otherwise specified. In each Multi-SPIN round, the available bandwidth is equally allocated among the devices with rejected drafts. We set the effective vocabulary size to V eff = 1024 and quantize each probability value using Q = 16 bits. The wireless channels experi-ence independent Rayleigh small-scale fading. The large-scale channel gain is modeled using a reference gain of −30 dB at a distance of 1 m and a path-loss exponent of 3.5. The device-to-server distances are independently drawn from the interval 

[100 , 500] m. All devices are assigned the same UL power budget, denoted by P ul ≜ P ul  

> k

for all k ∈ K , whose value is specified in the corresponding figure. The noise power spectral densities at the server and devices are set to N ser 0 = −169 

dBm/Hz and N dev 0 = −167 dBm/Hz, respectively. 

4) Benchmarks: We compare the proposed framework with several benchmark schemes to separately evaluate the perfor-mance gains from mode selection and draft-length control. To assess the effectiveness of adaptive mode selection, we consider the following benchmarks:  

> •

All UL mode: Devices with rejected drafts all operate in the UL mode for draft-distribution uploading. Each device transmits at its maximum UL power.  

> •

All DL mode: Devices with rejected drafts all operate in the DL mode for target-distribution downloading. The DL transmit power is optimally allocated according to Lemma 1.  

> •

Random mode selection: Devices with rejected drafts independently select the UL or DL mode with equal probability. Given the resulting mode assignment, the UL-mode devices transmit at their maximum powers, while the DL power is optimally allocated among the DL-mode devices. For each mode-selection scheme, the draft lengths are sepa-rately optimized under the corresponding communication pol-icy. This ensures that the comparison isolates the performance gain of adaptive mode selection rather than that of draft-length control. For draft-length control, we consider the following bench-marks:            

> (a) Draft-length behavior (b) Mode-selection behavior Fig. 7. Adaptive draft-length control and mode selection versus the DL power budget for Qwen2.5 model pair, with the per-device UL power budget fixed at Pul = 10 dBm.
> (a) Draft-length behavior (b) Mode-selection behavior Fig. 8. Adaptive draft-length control and mode selection versus the DL power budget for DeepSeek-R1 model pair, with the per-device UL power budget fixed at Pul = 10 dBm.
> •

Optimal uniform draft length: All devices use the same draft length, i.e., γk = γ for all k ∈ K . The uniform draft length γ is selected by exhaustive search over 

{0, 1, . . . , γ max } to maximize the expected sum token goodput, where we set γmax = 20 . 

> •

Computation-only draft-length control: The draft lengths are selected by considering only the computation part of the round latency, while ignoring the communi-cation delay. For both draft-length schemes, the reactive communica-tion stage employs the optimal mode-selection and power-allocation policy developed in Section IV. Therefore, their performance differences arise solely from the draft-length control strategy. 

B. Adaptive Draft-Length and Mode-Selection Behaviors 

Figs. 7 and 8 illustrate how the proactive draft-length control and the reactive mode selection adapt to the DL power budget P dl . For both model pairs, the sum token goodput increases steadily with P dl . As P dl becomes sufficiently large, communication delay becomes less dominant. The per-formance bottleneck then shifts toward local drafting and server-side verification, causing the goodput gain to gradually saturate. The same bottleneck shift also explains the draft-length behavior. The average optimized draft length generally increases with the DL power budget. A longer draft provides greater speculative gain by exposing more tokens to parallel verification, but also increases the probability of rejection and hence the likelihood of costly distribution transmission. As P dl 

increases, the communication penalty associated with rejection is reduced. The draft-length controller can therefore tolerate more frequent rejections and select longer drafts to better exploit the parallel-verification gain. 12                     

> (a) Pdl /P ul = 4 (b) Pdl /P ul = 16
> Fig. 9. Sum goodput versus the number of devices for Qwen2.5 model pair under different DL-to-UL power budget ratios.
> (a) Pdl /P ul = 4 (b) Pdl /P ul = 16
> Fig. 10. Sum goodput versus the number of devices for DeepSeek-R1 model pair under different DL-to-UL power budget ratios.

Figs. 7 and 8 further show the fraction of devices in the three possible states: all drafted tokens accepted, correction through the UL mode, and correction through the DL mode. When P dl 

is small, the shared DL resource is restrictive, and most devices requiring correction are therefore assigned to the UL mode. As 

P dl increases, the optimized DL transmission delay decreases, and the proposed scheme progressively shifts rejected devices from the UL mode to the DL mode. Nevertheless, a fraction of devices continues to use the UL even at relatively large 

P dl , since assigning every rejected device to the DL would introduce unnecessary competition for the shared DL power budget. 

C. Performance Gain of Optimal Mode Selection 

Figs. 9 and 10 compare the goodput of different mode-selection schemes as the number of devices increases. For each K, the total communication power budget is fixed as P tot (K) = ¯P dl + K ¯P ul , where ¯P dl = 30 dBm and 

¯P ul = 15 dBm are the reference DL and per-device UL power budgets, respectively. The actual budgets P dl and P ul 

are selected to satisfy both the prescribed DL-to-UL power-budget ratio P dl /P ul and the total-budget constraint P dl +

KP ul = P tot (K). The left and right subfigures correspond to P dl /P ul = 4 and P dl /P ul = 16 , respectively. In both cases, the proposed optimal mode-selection scheme consis-tently achieves the highest goodput by adaptively balancing the UL and DL modes. When the DL power is relatively limited, i.e., P dl /P ul = 4, assigning all devices to the DL mode leads to severe competition for the shared server power, while the all-UL mode is constrained by the per-device transmit-power budget. The proposed scheme avoids both extremes by assigning only a suitable subset of devices to the DL mode. When the DL power is more abundant, i.e., P dl /P ul = 16 ,the all-DL scheme becomes more competitive, but remains   

> (a) Qwen2.5 pair (b) DeepSeek-R1 pair Fig. 11. Sum goodput versus the DL-to-UL power-budget ratio for Qwen2.5 and DeepSeek-R1 model pairs.

inferior to the proposed scheme, particularly as the number of devices increases and competition for the shared DL power intensifies. These results demonstrate that neither a fixed UL nor a fixed DL policy is universally optimal, and that adaptive mode selection is essential for maintaining high goodput in Multi-SPIN. 

D. Performance Gain of Proactive Draft-Length Control 

Fig. 11 evaluates the benefit of the proposed proactive draft-length control under different DL-to-UL power-budget ratios. The proposed scheme consistently achieves higher goodput than both the optimal uniform draft length and the computation-only draft-length control. Compared with the uniform-draft baseline, the gain highlights the benefit of adapting draft lengths to device-specific latency heterogene-ity. Compared with the computation-only baseline, it further demonstrates the importance of accounting for communication conditions in draft-length optimization. VII. C ONCLUDING REMARKS 

In this paper, we introduced communication-mode selection for Multi-SPIN. The underlying philosophy of Multi-SPIN is to move beyond server-centric inference by distributing draft generation across local SLMs, thereby harnessing oth-erwise underutilized device-side computation for coopera-tive token generation. Communication-mode selection extends this philosophy from computation to communication. Rather than relying exclusively on the shared DL, the system can also exploit the distributed UL capabilities of edge devices and select the more efficient transmission direction for each correction operation. This differs fundamentally from con-ventional communication systems, in which the information source, destination, and transmission direction are typically predetermined. In Multi-SPIN, the direction of information flow can instead be adapted to system conditions, creating an additional degree of freedom for communication design. Wireless communication thus evolves from a passive data-delivery link into an active component of edge LLM inference. This perspective opens several promising research direc-tions. One is to move beyond binary UL/DL mode selection to-ward multi-mode designs that jointly choose what information to exchange, in which direction, and at what representation granularity, such as full, truncated, quantized, or compressed token distributions. Another opportunity lies in jointly de-signing communication modes with model collaboration it-self, including draft-model assignment, draft length, and early 13 

bypass of either local drafting or server verification. These developments could ultimately lead to a unified framework in which computation placement, information representation, and transmission direction are co-designed for scalable and resource-efficient edge intelligence. APPENDIX APROOF OF LEMMA 2Consider any feasible mode selection whose UL set U is not of prefix form. Then there exist two devices i < j such that i / ∈ U but j ∈ U . Since the devices are sorted according to their minimum UL delays, we have τ ul  

> i

≤ τ ul  

> j

.Now construct a new mode selection by moving device i

from DL to UL, while keeping all other devices unchanged. The resulting UL set is U′ = U ∪ { i}, and the resulting DL set is D′ = D \ { i}. Under this change, the minimum feasible UL delay does not increase, because device i has no larger UL delay than device j. Meanwhile, the minimum feasible DL delay cannot increase, because the DL power-allocation problem is solved over the smaller user set D′ under the same total DL power budget. Therefore, the resulting delay is no larger than that of the original mode selection. Repeating this operation whenever there exists a pair i < j 

such that i / ∈ U but j ∈ U eventually yields a prefix UL set 

U(n) = {1, 2, . . . , n } for some n ∈ { 0, 1, . . . , |R|} , without increasing the communication delay. Hence, there exists an optimal mode selection with the claimed prefix structure. APPENDIX BPROOF OF PROPOSITION 1Since τ ul (n) is non-decreasing in n and τ dl (n) is non-increasing in n, the delay difference ∆τ (n) is non-increasing in n. Therefore, the first index ¯n satisfying ∆τ (¯ n) ≤ 0 can be found via a bisection search. It remains to show that the global minimizer of τ (n) must lie around this crossing point. For any n < ¯n, we have 

∆τ (n) > 0, which implies τ dl (n) > τ ul (n). Hence, the overall delay is dominated by the DL part, i.e., τ (n) = τ dl (n).Since τ dl (n) is non-increasing in n, the best choice among all n < ¯n is n = ¯n − 1. Similarly, for any n ≥ ¯n, we have ∆τ (n) ≤ 0, which implies τ ul (n) ≥ τ dl (n). Hence, the overall delay is dominated by the UL part, i.e., τ (n) = τ ul (n).Since τ ul (n) is non-decreasing in n, the best choice among all n ≥ ¯n is n = ¯ n. Therefore, the optimal solution must be one of the two candidates in (44). REFERENCES [1] K. B. Letaief, Y. Shi, J. Lu, and J. Lu, “Edge artificial intelligence for 6G: Vision, enabling technologies, and applications,” IEEE J. Sel. Areas Commun. , vol. 40, no. 1, pp. 5–36, 2022. [2] J. Shao and X. Li, “AI flow at the network edge,” IEEE Netw. , vol. 40, no. 1, pp. 330–336, 2026. [3] Y. Leviathan, M. Kalman, and Y. Matias, “Fast inference from transform-ers via speculative decoding,” in Proc. Int. Conf. Mach. Learn. (ICML) ,2023, pp. 19 274–19 286. [4] C. Chen, S. Borgeaud, G. Irving, J.-B. Lespiau, L. Sifre, and J. Jumper, “Accelerating large language model decoding with speculative sam-pling,” arXiv:2302.01318 , 2023. [5] F. Chen, P. Li, T. H. Luan, Z. Su, and J. Deng, “SPIN: Accelerating large language model inference with heterogeneous speculative models,” in Proc. IEEE Conf. Comput. Commun. (INFOCOM) , 2025, pp. 1–10. [6] D. Xu, W. Yin, H. Zhang, X. Jin, Y. Zhang, S. Wei, M. Xu, and X. Liu, “EdgeLLM: Fast on-device LLM inference with speculative decoding,” 

IEEE Trans. Mobile Comput. , vol. 24, no. 4, pp. 3256–3273, 2025. [7] E. Li, L. Zeng, Z. Zhou, and X. Chen, “Edge AI: On-demand acceler-ating deep neural network inference via edge computing,” IEEE Trans. Wireless Commun. , vol. 19, no. 1, pp. 447–457, 2020. [8] J. Shao and J. Zhang, “Communication-computation trade-off in resource-constrained edge inference,” IEEE Commun. Mag. , vol. 58, no. 12, pp. 20–26, 2020. [9] C. Cai, X. Yuan, and Y.-J. A. Zhang, “Multi-device task-oriented com-munication via maximal coding rate reduction,” IEEE Trans. Wireless Commun. , vol. 23, no. 12, pp. 18 096–18 110, 2024. [10] D. Wen, P. Liu, G. Zhu, Y. Shi, J. Xu, Y. C. Eldar, and S. Cui, “Task-oriented sensing, computation, and communication integration for multi-device edge AI,” IEEE Trans. Wireless Commun. , vol. 23, no. 3, pp. 2486–2502, 2024. [11] C. Cai, X. Yuan, and Y.-J. A. Zhang, “End-to-end learning for task-oriented semantic communications over MIMO channels: An information-theoretic framework,” IEEE J. Sel. Areas Commun. , vol. 43, no. 4, pp. 1292–1307, 2025. [12] J. Yan, S. Bi, and Y.-J. A. Zhang, “Optimal model placement and online model splitting for device-edge co-inference,” IEEE Trans. Wireless Commun. , vol. 21, no. 10, pp. 8354–8367, 2022. [13] K. Zhang, H. He, S. Song, J. Zhang, and K. B. Letaief, “Communication-efficient distributed on-device LLM inference over wireless networks,” 

IEEE J. Sel. Top. Signal Process. , vol. 19, no. 7, pp. 1301–1317, 2025. [14] Qwen Team, “Qwen2.5-14B configuration file,” Hugging Face Model Repository, 2024. [Online]. Available: https://huggingface.co/Qwen/ Qwen2.5-14B/blob/main/config.json [15] OpenAI, “How to count tokens with tiktoken,” OpenAI Cookbook, 2024. [Online]. Available: https://cookbook.openai.com/examples/how to count tokens with tiktoken [16] C. Zheng and T. Yang, “Communication-efficient collaborative LLM inference via distributed speculative decoding,” in Proc. Int. Conf. Wireless Commun. Signal Process. (WCSP) , 2025, pp. 1–6. [17] S. Oh, J. Kim, J. Park, S.-W. Ko, J. Choi, T. Q. S. Quek, and S.-L. Kim, “Communication-efficient hybrid language model via uncertainty-aware opportunistic and compressed transmission,” IEEE Trans. Commun. ,vol. 74, pp. 10 658–10 675, 2026. [18] G. Zhang, Y. Cai, G. Yu, P. Popovski, and O. Simeone, “Quantize-sample-and-verify: LLM acceleration via adaptive edge-cloud specula-tive decoding,” IEEE Commun. Lett. , vol. 30, pp. 852–856, 2026. [19] P. Bhattacharjee, F. Tian, M. Zhong, G. Zhang, O. Simeone, and R. Tandon, “Conformal sparsification for bandwidth-efficient edge– cloud speculative decoding,” in Proc. NeurIPS Workshop on AI for Next-Gen. Netw. , 2025. [20] H. Zheng, Z. Wang, M. Cui, C. Cai, H. Du, and K. Huang, “Multi-SPIN: Multi-access speculative inference for cooperative token generation at the edge,” arXiv:2606.04581 , 2026. [21] J. Ning, C. Zheng, and T. Yang, “DSSD: Efficient edge-device LLM deployment and collaborative inference via distributed split speculative decoding,” in Proc. ICML Workshop Mach. Learn. Wireless Commun. Netw. , 2025. [22] Y. Jia, S. Tang, and Q. Yang, “CoVSpec: Efficient device-edge co-inference for vision-language models via speculative decoding,” 

arXiv:2605.02218 , 2026. [23] D. Guo et al. , “DeepSeek-R1: Incentivizing reasoning capability in LLMs via reinforcement learning,” arXiv:2501.12948 , 2025. [24] Q. Su, C. Giannoula, and G. Pekhimenko, “The synergy of spec-ulative decoding and batching in serving large language models,” 

arXiv:2310.18813 , 2023. [25] R. H. Zhang, S. Dey, A. Mishra, H. Wu, B. Li, and R. Zhang, “Batch speculative decoding done right,” arXiv:2510.22876 , 2025. [26] B. Zhu, Z. Chen, L. Zhao, H. Shin, and A. Nallanathan, “Efficient LLM inference over heterogeneous edge networks with speculative decoding,” 

IEEE Trans. Commun. , vol. 74, pp. 11 189–11 204, 2026. [27] S. Boyd and L. Vandenberghe, Convex optimization . Cambridge University Press, 2004. [28] D. Tse and P. Viswanath, Fundamentals of Wireless Communication .Cambridge University Press, 2005. [29] M. Chen et al. , “Evaluating large language models trained on code,” 

arXiv:2107.03374 , 2021. [30] K. Cobbe et al. , “Training verifiers to solve math word problems,” 

arXiv:2110.14168 , 2021. [31] L. Zheng et al. , “Judging LLM-as-a-judge with MT-bench and chatbot arena,” in Proc. Adv. Neural Inf. Process. Syst. , 2023. [32] J. Zhou et al. , “Instruction-following evaluation for large language models,” arXiv:2311.07911 , 2023.